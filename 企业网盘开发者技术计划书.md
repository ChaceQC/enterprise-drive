# 企业网盘开发者技术计划书（Python + uv 后端）

> 文档版本：v1.1  
> 面向读者：后端、前端、测试、DevOps、安全工程师  
> 技术基线：Python 3.12+、uv、FastAPI、SQLAlchemy 2.x、PostgreSQL 16+、Redis、S3 兼容对象存储、OpenSearch、Celery、Kubernetes  
> 文档目标：把企业网盘拆成可实现、可测试、可部署、可扩展、可拆分开发任务的工程方案。

## 0. 开发者视角的目标

这份文档不是立项汇报材料，而是开发实施说明。项目一期要实现一个“企业可试点上线”的网盘后端，核心不是界面看起来像网盘，而是下列技术闭环必须成立：

1. 文件元数据、对象存储、权限、审计、搜索和异步任务之间的数据一致性可控。
2. 上传下载链路能承载大文件、断点续传、秒传、并发上传和失败重试。
3. 权限判断必须服务端可信，支持空间角色、目录授权、继承、拒绝优先和外链策略。
4. 文件对象不能依赖用户路径存储，移动、重命名、版本管理不能复制大对象。
5. 所有关键操作可审计，审计日志不能因为异步日志平台异常而丢关键事件。
6. 后端工程必须用 uv 锁定依赖，CI 可重复构建，测试环境可一键拉起依赖。

### 0.1 领域术语

| 术语 | 含义 | 开发约束 |
| --- | --- | --- |
| tenant | 企业或租户边界 | 一期可单租户，但所有核心表必须保留 tenant_id |
| space | 文件空间 | 个人、部门、团队、项目、公共空间都抽象为 space |
| node | 文件树节点 | 目录和文件统一建模，文件节点通过 current_version_id 指向当前版本 |
| blob | 真实文件内容 | 按内容哈希去重，不包含业务文件名和目录路径 |
| version | 文件版本 | 覆盖上传产生新版本，不覆盖旧版本记录 |
| ACL | 访问控制列表 | 绑定用户、部门、用户组或空间角色的权限规则 |
| subject | 权限主体 | user、department、group、space_role、external_visitor |
| action | 权限动作 | list、preview、download、upload、update、delete、share、grant、manage |
| outbox | 事务事件表 | 业务事务内落库，异步投递到队列或外部系统 |
| ledger | 容量流水 | 用于追溯容量变化，不能只依赖 used_bytes 快照 |

### 0.2 一期开发边界

一期必须实现的工程闭环：

1. 本地账号登录、管理员 seed、BFF + HttpOnly Cookie Session、CSRF 防护。
2. 空间、文件树、文件版本、回收站、基础容量账本。
3. S3 兼容对象存储上传下载，支持 multipart upload。
4. 目录权限、空间角色、拒绝优先、权限缓存和高危动作二次校验。
5. 内部分享、外链分享、提取码、过期、次数限制、撤销。
6. 预览 Worker、搜索索引 Worker、审计 outbox dispatcher。
7. Docker Compose 本地开发环境、Alembic migration、CI 质量门禁。

一期明确不做：

1. 自研 Office 协同编辑器。
2. 完整桌面同步客户端。
3. 跨地域双活。
4. 复杂 DLP 内容识别引擎。
5. 多租户计费系统。

### 0.3 开发优先级

| 优先级 | 内容 | 原因 |
| --- | --- | --- |
| P0 | 账号、空间、文件树、上传下载、权限、审计 | 没有这些就不是企业网盘 |
| P1 | 分享、回收站、版本、容量、搜索、预览 | 试点用户日常使用必须具备 |
| P2 | 管理后台 API、统计、生命周期、批量治理 | 支撑运维和管理员效率 |
| P3 | 在线编辑、审批流、DLP、桌面同步 | 二期增强能力 |

## 1. 总体技术架构

### 1.1 一期架构选择

一期采用模块化单体，而不是一开始拆微服务。原因很直接：文件、权限、空间、容量和审计在事务上耦合很强，过早拆服务会把本来可控的数据库事务拆成大量分布式一致性问题。

模块化单体需要做到：

1. 模块边界清楚，每个模块有独立 router、schema、service、repository、policy、tests。
2. 模块之间通过 service 接口调用，不允许跨模块直接访问对方 repository。
3. 所有外部基础设施通过 infrastructure adapter 访问，便于测试替换。
4. 领域事件统一走 outbox 或任务队列，避免业务代码到处直接投递消息。

### 1.2 运行时组件

```text
Browser / H5 / Admin Console
        |
Nginx Ingress / API Gateway / WAF
        |
FastAPI App Pods
        |
-------------------------------------------------------
Auth | Org | Space | File | Upload | Permission | Share
Preview | Search | Audit | Admin | Quota | Lifecycle
-------------------------------------------------------
        |
PostgreSQL | Redis | S3 Object Storage | OpenSearch | MQ
        |
Celery Workers: preview / search / audit / lifecycle / cleanup
```

### 1.3 组件职责

| 组件 | 职责 | 强一致要求 |
| --- | --- | --- |
| PostgreSQL | 用户、空间、权限、文件树、版本、分享、容量账本、审计索引 | 是 |
| Object Storage | 原文件、分片、缩略图、预览文件、归档文件 | 与元数据最终一致 |
| Redis | 限流、权限缓存、上传临时状态、短期验证码、分布式锁 | 否，不存核心事实 |
| OpenSearch | 文件名、标签、全文内容搜索 | 最终一致 |
| Celery/MQ | 转码、索引、审计投递、生命周期清理 | 任务幂等 |
| FastAPI | 业务 API、鉴权、权限校验、元数据事务 | 是 |

## 2. 后端工程结构

### 2.1 推荐目录

```text
backend/
  pyproject.toml
  uv.lock
  alembic.ini
  Dockerfile
  README.md
  app/
    main.py
    api/
      deps.py
      errors.py
      middleware.py
      v1/
        router.py
    core/
      config.py
      logging.py
      security.py
      pagination.py
      idempotency.py
      time.py
    db/
      base.py
      session.py
      models.py
      transactions.py
    modules/
      auth/
        router.py
        schemas.py
        service.py
        repository.py
        models.py
        tokens.py
        tests/
      org/
      space/
      file/
      upload/
      permission/
      share/
      preview/
      search/
      audit/
      quota/
      admin/
    infrastructure/
      storage/
        base.py
        s3.py
        local.py
      cache/
        redis.py
      queue/
        celery_app.py
      search/
        opensearch.py
      preview/
        libreoffice.py
        ffmpeg.py
    workers/
      preview_tasks.py
      search_tasks.py
      audit_tasks.py
      lifecycle_tasks.py
    tests/
      conftest.py
      factories.py
  migrations/
    versions/
  scripts/
    seed_admin.py
    rebuild_search_index.py
    reconcile_storage.py
```

### 2.2 分层规则

```text
router -> service -> domain/policy -> repository -> db/infrastructure
```

1. router 只负责参数解析、依赖注入、HTTP 状态码和响应模型。
2. service 负责编排事务、调用权限策略、容量账本、对象存储和领域事件。
3. policy 只负责规则判断，不直接访问 HTTP request，不直接写数据库。
4. repository 负责 SQL 查询和数据持久化，不包含业务流程。
5. infrastructure 负责外部系统适配，例如 S3、Redis、OpenSearch、LibreOffice。

禁止事项：

1. 禁止在 router 中写复杂 SQL。
2. 禁止在 repository 中读取当前登录用户。
3. 禁止跨模块直接 import 对方 repository。
4. 禁止把 Redis 缓存当作权限或容量的唯一事实来源。
5. 禁止在数据库事务里执行长时间对象存储上传、文档转码或网络调用。

### 2.3 模块交付清单

每个模块必须交付代码、迁移、测试和文档，不允许只交付接口空壳。

| 模块 | 必交付文件 | 必交付测试 |
| --- | --- | --- |
| auth | model、router、service、tokens、password、session repository | 登录成功、密码错误、令牌刷新、令牌复用检测 |
| org | user、department、group、membership repository | 部门树、用户禁用、用户组展开 |
| space | space model、space_member、quota account | 创建空间、成员角色、空间禁用 |
| file | node、version、blob、folder service、trash service | 同名冲突、移动、删除、恢复、版本回滚 |
| upload | upload_session、storage adapter、complete service | 秒传、分片、断点续传、幂等 complete |
| permission | acl_entry、policy evaluator、cache invalidator | 继承、拒绝优先、空间角色、策略拒绝 |
| share | share、share_recipient、share token、passcode | 外链过期、提取码错误、撤销、次数耗尽 |
| preview | worker、converter、artifact writer | 支持格式、失败状态、超时处理 |
| search | indexer、query builder、acl token builder | 权限过滤、重建索引、删除同步 |
| audit | audit_log、outbox、dispatcher | 业务事务写日志、投递失败重试 |
| quota | quota_account、ledger、reconcile job | 并发扣减、回滚、定期校准 |
| admin | admin router、audit query、stats query | 管理权限、分页、筛选、导出 |

### 2.4 代码命名约定

1. 数据库模型使用单数类名、复数表名，例如 `Node` -> `nodes`。
2. Pydantic schema 按用途后缀：`FileListResponse`、`CreateFolderRequest`、`UploadInitResponse`。
3. Service 方法使用业务动词：`create_folder`、`init_upload`、`complete_upload`、`grant_permission`。
4. Repository 方法使用数据语义：`get_by_id_for_update`、`list_children`、`insert_version`。
5. 权限动作使用固定字符串枚举，不在代码里散落魔法字符串。
6. 审计 action 使用点分命名：`file.upload.completed`、`share.external.accessed`。

## 3. uv 与依赖管理

### 3.1 常用命令

```bash
uv python install 3.12
uv sync --all-extras --dev
uv run fastapi dev app/main.py
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
uv run celery -A app.infrastructure.queue.celery_app worker -Q preview,search,audit -l info
uv run alembic upgrade head
uv run pytest
uv run ruff check .
uv run ruff format .
uv run mypy app
```

### 3.2 pyproject.toml 基线

```toml
[project]
name = "enterprise-drive-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.29",
  "alembic>=1.13",
  "pydantic-settings>=2.4",
  "redis>=5.0",
  "argon2-cffi>=23.1",
  "celery[redis]>=5.4",
  "minio>=7.2",
  "opensearch-py>=2.6",
  "structlog>=24.1",
  "opentelemetry-api>=1.26",
  "opentelemetry-sdk>=1.26",
  "opentelemetry-instrumentation-fastapi>=0.47b0",
]

[dependency-groups]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "httpx>=0.27",
  "ruff>=0.5",
  "mypy>=1.10",
  "testcontainers>=4.7",
  "factory-boy>=3.3",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["sqlalchemy.ext.mypy.plugin"]
```

CI 中必须使用：

```bash
uv sync --frozen --all-extras --dev
```

这样可以保证 CI、开发机和生产构建使用同一套锁定依赖。

## 4. 核心领域模型

### 4.1 聚合边界

| 聚合 | 根实体 | 说明 |
| --- | --- | --- |
| Tenant | tenant | 单企业部署也保留 tenant_id，便于未来多租户 |
| Org | user、department、group | 用户、部门、用户组、岗位信息 |
| Space | space | 个人空间、部门空间、团队空间、项目空间 |
| FileTree | node | 文件夹和文件节点，维护树结构 |
| FileContent | file_blob、file_version | 文件对象和版本，不等同于路径节点 |
| Permission | role_binding、acl_entry、policy_rule | 空间角色、目录 ACL、策略规则 |
| Upload | upload_session、upload_part | 分片上传状态机 |
| Share | share、share_item、share_access_log | 内部分享和外链分享 |
| Audit | audit_log、outbox_event | 审计查询和异步投递 |
| Quota | quota_account、quota_ledger | 容量配额和容量流水 |

### 4.2 关键设计选择

1. `node` 表表示目录树中的一个位置，可以是文件夹或文件。
2. `file_blob` 表表示真实对象内容，按内容哈希去重。
3. `file_version` 表表示某个文件节点的某个版本，指向一个 blob。
4. 文件重命名和移动只改 `node`，不改 `file_blob`。
5. 覆盖上传不是覆盖 blob，而是新增 `file_version` 并更新 `node.current_version_id`。
6. 删除默认是逻辑删除，进入回收站；彻底删除后再按引用计数和生命周期清理 blob。

### 4.3 状态机总览

上传、预览、搜索、分享和删除都必须按状态机实现，避免布尔字段堆叠后不可维护。

| 对象 | 状态 | 触发动作 |
| --- | --- | --- |
| upload_session | initiated、uploading、completing、completed、aborted、expired、failed | init、part_uploaded、complete、abort、expire、fail |
| file_version.preview_status | pending、processing、ready、failed、unsupported、skipped | enqueue、start、finish、fail、skip |
| file_version.search_status | pending、extracting、indexed、failed、skipped | enqueue、extract、index、fail |
| share.status | active、expired、revoked、disabled | create、expire、revoke、risk_disable |
| node lifecycle | active、deleted、purged、archived | create、delete、restore、purge、archive |
| outbox_event.status | pending、processing、sent、failed、dead | create、dispatch、ack、retry、dead_letter |

状态机实现要求：

1. 状态流转必须集中在 service 或 domain method，不允许散落 update。
2. 非法状态流转抛出业务异常并写入 debug 日志。
3. 任务重试时，如果目标状态已经完成，应直接返回成功，保证幂等。
4. 管理后台展示状态时使用后端枚举，不由前端硬编码中文状态。

### 4.4 领域事件清单

| 事件 | 产生时机 | 消费者 |
| --- | --- | --- |
| file.created | 新文件创建完成 | preview、search、audit、quota stats |
| file.version.created | 覆盖上传或新版本完成 | preview、search、audit |
| file.moved | 文件或目录移动 | search、permission cache、audit |
| file.deleted | 删除到回收站 | search、audit、quota stats |
| file.restored | 从回收站恢复 | search、audit |
| file.purged | 彻底删除 | blob cleanup、search、audit、quota |
| permission.changed | ACL 或角色变更 | permission cache、search acl rebuild、audit |
| share.created | 分享创建 | audit、risk engine |
| share.accessed | 分享访问 | audit、rate limit stats |
| upload.completed | 上传完成 | audit、preview、search |

## 5. PostgreSQL 表设计

以下 DDL 是开发基线，不是最终完整迁移脚本。实际实现通过 SQLAlchemy model + Alembic migration 管理。

### 5.1 nodes 文件树

```sql
create table nodes (
  id uuid primary key,
  tenant_id uuid not null,
  space_id uuid not null,
  parent_id uuid null references nodes(id),
  node_type varchar(16) not null check (node_type in ('folder', 'file')),
  name varchar(255) not null,
  normalized_name varchar(255) not null,
  path_hash varchar(64) not null,
  owner_id uuid not null,
  current_version_id uuid null,
  is_deleted boolean not null default false,
  deleted_at timestamptz null,
  deleted_by uuid null,
  created_by uuid not null,
  updated_by uuid not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create unique index uq_nodes_sibling_name_alive
  on nodes(tenant_id, space_id, parent_id, lower(normalized_name))
  where is_deleted = false and parent_id is not null;

create unique index uq_nodes_space_root_alive
  on nodes(tenant_id, space_id)
  where is_deleted = false and parent_id is null;

create index idx_nodes_parent
  on nodes(tenant_id, space_id, parent_id, is_deleted);

create index idx_nodes_space_updated
  on nodes(tenant_id, space_id, updated_at desc);
```

说明：

1. `normalized_name` 用于同目录重名判断，需要统一大小写、Unicode normalization 和空白处理策略。
2. `path_hash` 用于快速判断路径变化和搜索索引同步，不作为唯一业务主键。
3. PostgreSQL 中 `NULL` 不参与普通唯一约束冲突，根目录 `parent_id` 为空时必须用单独的 partial unique index 保证每个 space 只有一个根目录。

### 5.2 file_blobs 内容对象

```sql
create table file_blobs (
  id uuid primary key,
  tenant_id uuid not null,
  content_hash varchar(128) not null,
  hash_algo varchar(16) not null default 'sha256',
  size_bytes bigint not null check (size_bytes >= 0),
  storage_bucket varchar(128) not null,
  storage_key text not null,
  ref_count bigint not null default 0,
  status varchar(32) not null default 'active',
  encryption_mode varchar(32) not null default 'sse',
  scan_status varchar(32) not null default 'pending',
  created_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create unique index uq_file_blobs_hash
  on file_blobs(tenant_id, hash_algo, content_hash, size_bytes);

create index idx_file_blobs_cleanup
  on file_blobs(tenant_id, status, ref_count, created_at);
```

说明：

1. `content_hash + size_bytes` 用于秒传和去重。
2. `ref_count` 不是权限依据，只用于生命周期清理和成本治理。
3. `status=active` 表示可复用内容对象，`status=deleting` 表示对象生命周期任务已声明清理，上传链路必须返回重试错误而不是复用或新建同 hash blob。
4. 对象存储 key 不使用用户文件名，避免路径泄露。

### 5.3 file_versions 文件版本

```sql
create table file_versions (
  id uuid primary key,
  tenant_id uuid not null,
  node_id uuid not null references nodes(id),
  blob_id uuid not null references file_blobs(id),
  version_no integer not null,
  size_bytes bigint not null,
  mime_type varchar(255) not null,
  extension varchar(32) null,
  preview_status varchar(32) not null default 'pending',
  search_status varchar(32) not null default 'pending',
  created_by uuid not null,
  created_at timestamptz not null default now(),
  comment text null,
  metadata jsonb not null default '{}'::jsonb
);

create unique index uq_file_versions_node_version
  on file_versions(tenant_id, node_id, version_no);

create index idx_file_versions_node_created
  on file_versions(tenant_id, node_id, created_at desc);
```

### 5.4 upload_sessions 上传会话

```sql
create table upload_sessions (
  id uuid primary key,
  tenant_id uuid not null,
  space_id uuid not null,
  parent_id uuid not null references nodes(id),
  uploader_id uuid not null,
  file_name varchar(255) not null,
  normalized_name varchar(255) not null,
  size_bytes bigint not null,
  content_hash varchar(128) null,
  hash_algo varchar(16) null,
  mime_type varchar(255) null,
  part_size bigint not null,
  total_parts integer not null,
  object_key text not null,
  provider_upload_id text null,
  status varchar(32) not null,
  expires_at timestamptz not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create index idx_upload_sessions_user_status
  on upload_sessions(tenant_id, uploader_id, status, created_at desc);
```

状态枚举：`initiated`、`uploading`、`completing`、`completed`、`aborted`、`expired`、`failed`。

### 5.5 shares 分享

```sql
create table shares (
  id uuid primary key,
  tenant_id uuid not null,
  share_type varchar(16) not null check (share_type in ('internal', 'external')),
  token_hash varchar(128) null,
  passcode_hash varchar(255) null,
  created_by uuid not null,
  root_node_id uuid not null references nodes(id),
  permission varchar(32) not null,
  expires_at timestamptz null,
  max_views integer null,
  max_downloads integer null,
  view_count integer not null default 0,
  download_count integer not null default 0,
  status varchar(32) not null default 'active',
  created_at timestamptz not null default now(),
  revoked_at timestamptz null,
  revoked_by uuid null,
  metadata jsonb not null default '{}'::jsonb
);

create index idx_shares_creator
  on shares(tenant_id, created_by, created_at desc);

create unique index idx_shares_external_token_hash
  on shares(token_hash)
  where share_type = 'external';
```

外链 token 只能保存 hash，不能明文保存。分享 URL 中的 token 要使用 256-bit 随机值，不能用自增 ID 或 UUID 裸露业务关系。

当前实现已落地 `shares`、`share_items`、`share_recipients` 和 `share_access_logs` 迁移与 SQLAlchemy 模型。服务层支持内部分享和外链分享创建、提取码哈希、过期时间、访问/下载次数上限、撤销状态和审计/outbox 写入；外链原始 token 只在创建响应中返回一次，数据库只保存全局唯一 `token_hash`，提取码只保存 Argon2id hash。创建分享会校验 root 节点和全部分享项的节点级 `share` 权限，并限制分享项与 root 节点属于同一空间。当前已开放 `POST /api/v1/shares`、`GET /api/v1/shares/{share_id}`、`POST /api/v1/shares/{share_id}/revoke` 和 `POST /api/v1/public/shares/access`，并接入 Cookie Session、CSRF、创建者边界、`tenant_slug + raw_token` 外链访问契约、提取码校验、状态/过期判断、访问次数原子扣减、IP 总量和 `token + IP` 维度限流、`share_access_logs` 和 `share.external.accessed` 审计；尚未接入外链下载、下载次数并发扣减和 external subject 下载策略。

### 5.6 audit_logs 审计日志

```sql
create table audit_logs (
  id uuid primary key,
  tenant_id uuid not null,
  actor_id uuid null,
  actor_type varchar(32) not null,
  action varchar(64) not null,
  resource_type varchar(64) not null,
  resource_id uuid null,
  space_id uuid null,
  result varchar(16) not null,
  risk_level varchar(16) not null default 'low',
  ip inet null,
  user_agent text null,
  request_id varchar(64) not null,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
) partition by range (created_at);

create index idx_audit_logs_actor_time
  on audit_logs(tenant_id, actor_id, created_at desc);

create index idx_audit_logs_resource_time
  on audit_logs(tenant_id, resource_type, resource_id, created_at desc);
```

审计日志建议按月分区。关键审计写入和业务事务必须强关联；投递到外部日志平台可异步，但本地审计索引不能丢。

### 5.7 组织与空间表补充

```sql
create table tenants (
  id uuid primary key,
  name varchar(255) not null,
  status varchar(32) not null default 'active',
  permission_version bigint not null default 1,
  policy_version bigint not null default 1,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table users (
  id uuid primary key,
  tenant_id uuid not null references tenants(id),
  username varchar(128) not null,
  email varchar(255) null,
  display_name varchar(255) not null,
  password_hash varchar(255) null,
  status varchar(32) not null default 'active',
  department_id uuid null,
  last_login_at timestamptz null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_users_username on users(tenant_id, lower(username));
create unique index uq_users_email on users(tenant_id, lower(email)) where email is not null;

create table departments (
  id uuid primary key,
  tenant_id uuid not null references tenants(id),
  parent_id uuid null references departments(id),
  name varchar(255) not null,
  path text not null,
  sort_order integer not null default 0,
  status varchar(32) not null default 'active',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index idx_departments_parent on departments(tenant_id, parent_id, sort_order);

create table spaces (
  id uuid primary key,
  tenant_id uuid not null references tenants(id),
  space_type varchar(32) not null,
  name varchar(255) not null,
  owner_id uuid null references users(id),
  department_id uuid null references departments(id),
  root_node_id uuid null,
  status varchar(32) not null default 'active',
  permission_version bigint not null default 1,
  quota_account_id uuid null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create index idx_spaces_type_owner on spaces(tenant_id, space_type, owner_id);
```

### 5.8 权限表补充

```sql
create table space_members (
  id uuid primary key,
  tenant_id uuid not null,
  space_id uuid not null references spaces(id),
  user_id uuid not null references users(id),
  role varchar(32) not null check (role in ('owner', 'admin', 'editor', 'viewer')),
  created_by uuid,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_space_members_user
  on space_members(tenant_id, space_id, user_id);

create index idx_space_members_user
  on space_members(tenant_id, user_id, role);

create index idx_space_members_space_role
  on space_members(tenant_id, space_id, role);

create table acl_entries (
  id uuid primary key,
  tenant_id uuid not null,
  node_id uuid not null references nodes(id),
  subject_type varchar(32) not null,
  subject_id uuid not null,
  effect varchar(16) not null check (effect in ('allow', 'deny')),
  actions jsonb not null,
  inherit boolean not null default true,
  created_by uuid not null,
  created_at timestamptz not null default now()
);

create index idx_acl_node_subject
  on acl_entries(tenant_id, node_id, subject_type, subject_id);

create table role_permissions (
  id uuid primary key,
  tenant_id uuid not null,
  role varchar(64) not null,
  action varchar(64) not null,
  scope varchar(64) not null,
  created_at timestamptz not null default now()
);

create unique index uq_role_permissions
  on role_permissions(tenant_id, role, action, scope);
```

当前 Sprint 4 第一阶段先落地用户维度 `space_members`，创建空间时自动写入创建者的 `owner` 成员关系，并用 `PermissionService` 按空间角色检查空间列表、文件树、上传和下载等空间级动作；空间成员管理 API 已支持 owner/admin 添加、查看、调整和移除成员，变更时递增 `spaces.permission_version`，保护最后一个 owner，并写入授权审计。节点 ACL 基础能力已落地到 `acl_entries`，支持 `user`、`department`、`group` 三类主体、allow/deny、继承开关和 deny 优先；权限判断会通过 Org 模块的 `departments`、`department_members`、`user_groups`、`user_group_members` 事实表展开当前用户所属活跃部门和用户组，文件列表、文件夹创建、上传初始化、multipart complete 和下载入口都会做节点级权限判断。文件列表已通过 `batch_check_nodes` 为当前页子节点批量返回常用动作权限，高危动作仍由对应接口单独检查。外部主体会在 share 模块接入后扩展为额外 subject 映射层，避免在尚无主体模型时提前堆复杂泛化。

ACL 查询性能注意点：目录继承权限会涉及祖先节点。可选实现有三种：

1. 直接递归 CTE 查祖先：实现简单，适合一期目录深度可控场景。
2. closure table：查询快，移动目录时维护成本高。
3. materialized path：查询中等，移动目录需批量更新路径。

一期建议使用递归 CTE + 限制最大目录深度 32。若后续目录量巨大，再引入 closure table。

### 5.9 上传分片、幂等与容量表补充

```sql
create table upload_parts (
  id uuid primary key,
  tenant_id uuid not null,
  upload_session_id uuid not null references upload_sessions(id),
  part_no integer not null,
  size_bytes bigint not null,
  etag text not null,
  checksum varchar(128) null,
  created_at timestamptz not null default now()
);

create unique index uq_upload_parts_session_part
  on upload_parts(tenant_id, upload_session_id, part_no);

create table idempotency_keys (
  id uuid primary key,
  tenant_id uuid not null,
  user_id uuid not null,
  key varchar(128) not null,
  request_hash varchar(128) not null,
  response_code integer null,
  response_body jsonb null,
  status varchar(32) not null default 'processing',
  expires_at timestamptz not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_idempotency_user_key
  on idempotency_keys(tenant_id, user_id, key);

create table quota_accounts (
  id uuid primary key,
  tenant_id uuid not null,
  owner_type varchar(32) not null,
  owner_id uuid not null,
  limit_bytes bigint not null,
  used_bytes bigint not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_quota_accounts_owner
  on quota_accounts(tenant_id, owner_type, owner_id);
```

### 5.10 索引、分区和迁移规则

1. `audit_logs` 按月分区，生产必须提前创建未来 3 到 6 个月分区。
2. `nodes` 必须有同目录唯一索引，保证并发创建同名文件时由数据库兜底。
3. 所有多租户表查询必须带 `tenant_id` 条件，禁止跨租户扫描。
4. 大表 migration 禁止一次性加非空列并填充默认值，应分三步：加 nullable 列、后台回填、加 not null 约束。
5. 删除字段必须至少跨两个版本：先停止写入和读取，再删除字段。
6. JSONB 字段只能放低频扩展属性，高频筛选字段必须列化并建索引。

## 6. 对象存储设计

### 6.1 Key 规则

```text
objects/{tenant_id}/{hash_prefix}/{content_hash}
uploads/{tenant_id}/{upload_session_id}/{part_no}
previews/{tenant_id}/{file_id}/{version_id}/preview.pdf
thumbnails/{tenant_id}/{file_id}/{version_id}/{size}.webp
exports/{tenant_id}/{export_id}/audit.csv
```

原则：

1. 原始文件对象 key 与文件名、目录名无关。
2. 预览文件和缩略图可以按 `file_id/version_id` 组织，因为它们是派生物。
3. 临时上传对象必须设置生命周期，避免失败上传长期占用容量。
4. 对象存储 bucket 必须私有，禁止公共读。

### 6.2 StorageAdapter 协议

```python
from typing import Protocol

class StorageAdapter(Protocol):
    async def create_multipart_upload(self, key: str, content_type: str | None) -> str: ...

    async def presign_upload_part(
        self,
        key: str,
        upload_id: str,
        part_no: int,
        expires_seconds: int,
    ) -> str: ...

    async def complete_multipart_upload(
        self,
        key: str,
        upload_id: str,
        parts: list[dict[str, str | int]],
    ) -> None: ...

    async def abort_multipart_upload(self, key: str, upload_id: str) -> None: ...

    async def presign_download(
        self,
        key: str,
        filename: str,
        content_type: str | None,
        expires_seconds: int,
    ) -> str: ...

    async def head_object(self, key: str) -> dict[str, str | int]: ...
```

默认对象存储适配使用非云厂商专有的开源 S3 兼容客户端，例如 MinIO Python SDK。业务代码只能依赖 `StorageAdapter` 协议，不直接 import 具体 SDK；如果 SDK 对 multipart 能力缺少公开 API，必要的客户端细节只能封装在 `infrastructure` 适配层，并在审计或进度文档中记录风险和替换触发条件。

## 7. 上传链路实现

### 7.1 接口设计

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/v1/uploads/init` | 初始化上传，检查权限、容量、秒传 |
| GET | `/api/v1/uploads/{session_id}` | 查询上传状态和已上传分片 |
| POST | `/api/v1/uploads/{session_id}/parts/{part_no}/presign` | 获取分片上传 URL |
| POST | `/api/v1/uploads/{session_id}/complete` | 完成上传，创建文件或新版本 |
| POST | `/api/v1/uploads/{session_id}/abort` | 取消上传并清理临时对象 |

当前实现已落地 `init`、状态查询、分片 `presign`、`complete`、`abort`、文件下载预签名 URL、空间容量账本初版、multipart complete 后服务端 `sha256` 校验、最终对象 key 规整、过期上传清理任务、上传/下载基础限流、彻底删除容量释放和空间容量校准维护任务。

初始化请求示例：

```json
{
  "space_id": "uuid",
  "parent_id": "uuid",
  "file_name": "产品方案.docx",
  "size_bytes": 104857600,
  "content_hash": "64位sha256十六进制摘要",
  "hash_algo": "sha256",
  "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
  "conflict_policy": "fail"
}
```

初始化响应示例：

```json
{
  "session_id": "uuid",
  "mode": "multipart",
  "part_size_bytes": 8388608,
  "total_parts": 13,
  "expires_at": "2026-06-30T10:00:00Z"
}
```

秒传响应示例：

```json
{
  "mode": "instant",
  "node_id": "uuid",
  "version_id": "uuid",
  "blob_id": "uuid"
}
```

### 7.2 初始化算法

```text
init_upload(user, parent_id, file_name, size, hash):
  1. validate file_name, extension, size, tenant policy
  2. load parent node with lock-free read
  3. assert parent is folder and not deleted
  4. check permission: upload on parent
  5. check quota: user/space/tenant remaining >= size
  6. if active content_hash exists in file_blobs:
       create node/version in db transaction
       increase blob ref_count
       write quota ledger
       write audit log
       enqueue preview/search if needed
       return instant result
  7. if same content_hash blob is deleting:
       return BLOB_DELETING for client retry
  8. create multipart upload in object storage
  9. create upload_session in db with provider_upload_id
  10. return session metadata
```

对象存储 multipart upload 不应放在数据库事务内部长时间执行。当前实现先创建 provider upload，再在短事务中写入 upload session；如果数据库写入失败，调用 abort 清理 provider upload。后续如需保留失败 session 可扩展为先写 DB session，再创建 provider upload 并更新 `provider_upload_id`。

### 7.3 完成上传算法

```text
complete_upload(user, session_id, parts):
  1. load upload_session for update
  2. assert uploader_id == user.id and status in initiated/uploading
  3. set status = completing
  4. verify parts count, part_no order, etag present
  5. call storage.complete_multipart_upload
  6. head object and verify size
  7. if client provided hash, enqueue or perform hash verification
  8. in db transaction:
       insert or reuse file_blob
       resolve name conflict
       create node if new file, or create new version if overwrite
       update node.current_version_id
       increase blob ref_count
       write quota ledger
       mark upload_session completed
       write audit log
       insert outbox events
  9. enqueue preview/search/scan tasks from outbox
```

重要细节：

1. S3 multipart ETag 不是文件 MD5，不能把它当内容完整性哈希。
2. 由客户端提供 SHA-256 时，服务端必须在 multipart complete 后重新计算对象内容 hash；当前实现只支持 `sha256`，不支持的算法返回 `UPLOAD_HASH_ALGO_UNSUPPORTED`。
3. 当前实现已完成合并、大小校验、服务端 hash 校验、最终对象 key 归档、分片记录、文件节点/版本写入、完成审计和空间容量账本写入；新 blob 会写入 `objects/{tenant_id}/{hash_prefix}/{content_hash}`，若 complete 前已有同租户同 hash、同大小 blob 则复用既有对象并清理本次临时对象。hash 不匹配返回 `UPLOAD_HASH_MISMATCH`，上传会话标记为 `failed` 并写 `upload.failed` 审计，不创建文件版本和容量流水。过期会话由 `upload.expire_sessions` 任务标记为 `expired` 并清理临时对象；彻底删除回收站节点会释放对应文件版本容量并写入负向容量流水；`quota.reconcile_space_usage` 任务按批次和 cursor 扫描租户空间，可报告并修复空间容量快照和账本漂移。
4. 容量扣减以最终创建版本为准，不以分片临时对象为准；临时容量可单独限制。
5. `complete` 接口必须幂等。如果 session 已 completed，返回已有 file_id/version_id。

### 7.4 推荐分片大小

| 文件大小 | 分片大小 | 客户端并发 |
| --- | --- | --- |
| < 100 MB | 8 MB | 2 到 3 |
| 100 MB 到 1 GB | 16 MB | 3 到 4 |
| 1 GB 到 20 GB | 32 MB | 4 到 6 |
| > 20 GB | 64 MB | 需企业策略允许 |

### 7.5 上传状态机细化

```text
initiated
  -> uploading        客户端获取分片签名或上报分片
  -> completing       客户端请求 complete
  -> completed        对象存储完成合并且元数据写入成功
  -> aborted          用户取消或管理员清理
  -> expired          超过 expires_at 且未完成
  -> failed           对象存储或元数据写入不可恢复失败
```

状态流转约束：

1. `completed`、`aborted`、`expired` 是终态，不能再上传分片。
2. `completing` 中再次调用 complete，应返回当前处理状态或已有结果，不能重复创建版本。
3. `expired` session 的临时对象由 `upload.expire_sessions` 清理任务异步 abort 和删除；任务只删除 `uploads/...` 临时 key，避免误删最终 `objects/...` 内容。
4. 客户端恢复上传时，只允许恢复 `initiated` 或 `uploading` 状态。

### 7.6 断点续传细节

查询上传状态响应：

```json
{
  "session_id": "uuid",
  "status": "uploading",
  "part_size": 8388608,
  "total_parts": 128,
  "uploaded_parts": [
    {"part_no": 1, "size_bytes": 8388608, "etag": "..."},
    {"part_no": 2, "size_bytes": 8388608, "etag": "..."}
  ],
  "expires_at": "2026-06-30T10:00:00Z"
}
```

前端恢复逻辑：

1. 根据 session_id 查询已上传分片。
2. 对比本地文件大小、文件名、hash，确认仍是同一文件。
3. 跳过已上传分片，只请求缺失分片的 presign URL。
4. 所有分片完成后调用 complete。

### 7.7 幂等 complete 设计

`complete_upload` 需要处理网络重试。推荐在 `upload_sessions.metadata` 中记录完成结果：

```json
{
  "completed_result": {
    "node_id": "uuid",
    "version_id": "uuid",
    "blob_id": "uuid"
  }
}
```

如果 session 已经是 `completed`，直接返回该结果。如果是 `completing`，可以返回 409 + `UPLOAD_COMPLETING`，前端稍后轮询；也可以阻塞短时间等待，建议一期采用轮询，避免接口长时间占用连接。

### 7.8 上传安全检查

1. 初始化时检查扩展名黑名单，例如 exe、bat、cmd、ps1 可按企业策略禁用。
2. 完成后以服务端嗅探 MIME 为准，客户端 MIME 只作参考。
3. 文件 hash 不匹配时标记上传失败并写审计，不创建文件版本。
4. 上传临时对象定期清理，避免恶意用户创建大量未完成上传占用存储。
5. 同一用户并发上传会话数需要限制，例如默认 20 个。

## 8. 下载链路实现

### 8.1 下载模式

| 模式 | 使用场景 | 优点 | 缺点 |
| --- | --- | --- | --- |
| 预签名 URL | 普通下载、大文件下载 | API 不承载文件流量 | URL 泄露窗口内可访问 |
| 后端代理 | 高密级、水印、强审计 | 控制最强 | 消耗 API 带宽和连接 |
| 内部网关代理 | 企业内网统一出口 | 兼顾控制和性能 | 部署复杂度更高 |

一期默认使用预签名 URL，高密级文件和外链下载可切换后端代理。当前实现已提供 `GET /api/v1/files/{node_id}/download`，基于当前文件版本生成短期私有对象下载地址，并返回 URL、过期时间、文件名、版本、大小和 MIME。

### 8.2 下载流程

```text
request_download(user, file_id):
  1. load node + current_version + blob
  2. assert node is file and not deleted
  3. check permission: download
  4. check policy: device/ip/time/classification/share limits
  5. create audit log with result=allowed
  6. generate short-lived presigned URL, 60-300 seconds
  7. return url, filename, size, mime_type
```

如果权限拒绝，也要写审计日志，但注意不要在错误信息里泄露文件是否存在。对用户统一返回“文件不存在或无权访问”。

当前实现中，下载成功和拒绝均写入 `file.downloaded` 审计事件与 outbox event；下载已通过 `PermissionService` 校验空间级 `download` 权限，后续由 Sprint 4 权限模块继续补齐目录 ACL、继承权限和拒绝优先策略。

### 8.3 Range 下载与文件名处理

大文件下载应支持 HTTP Range。使用预签名 URL 时通常由对象存储支持 Range；使用后端代理时需要透传 `Range` 头并正确返回 206。

文件名响应要求：

1. `Content-Disposition` 同时设置 `filename` 和 `filename*`，兼容中文文件名。
2. 文件名来自元数据 `node.name`，不能来自对象存储 key。
3. 外链下载可按策略追加水印后的导出文件名，例如 `方案_水印版.pdf`。

### 8.4 文件操作算法

#### 创建文件夹

```text
create_folder(user, parent_id, name):
  1. validate name and normalize
  2. load parent and check upload permission
  3. insert node(node_type='folder')
  4. if unique conflict, apply conflict policy or return FILE_CONFLICT
  5. write audit and outbox file.folder_created
```

#### 移动节点

```text
move_node(user, node_id, target_parent_id, new_name):
  1. load source node and target parent
  2. assert source not root and not deleted
  3. check update permission on source
  4. check upload permission on target parent
  5. if source is folder, ensure target is not descendant of source
  6. resolve name conflict in target directory
  7. update parent_id/name/normalized_name/updated_at
  8. increment node permission/path version if used
  9. write audit and outbox file.moved
```

#### 删除到回收站

```text
delete_node(user, node_id):
  1. load node
  2. check delete permission
  3. mark node and descendants is_deleted=true if folder
  4. record deleted_at/deleted_by
  5. remove from search index by event
  6. write audit
```

文件夹递归删除不建议在接口请求中一次性更新海量子节点。目录很大时，应先标记根节点 deleted，再后台任务递归处理子节点索引和状态。列表查询通过祖先删除状态过滤或冗余 `deleted_root_id` 实现。

#### 恢复节点

恢复时必须检查原父目录是否存在且未删除。如果原父目录不存在，允许恢复到用户选择的新目录。恢复时还要处理同名冲突，不能覆盖现有文件，除非用户显式选择覆盖为新版本。

#### 彻底删除节点

彻底删除只允许作用于已在回收站的节点。当前实现会删除目标节点下全部已删除后代的节点元数据和文件版本，扣减相关 blob 引用计数，按版本大小合计释放空间容量，写入 `reason=file_purged`、`ref_type=node` 的负向容量流水，并记录 `file.purged` 审计。删除到回收站不释放容量；对象存储最终对象不在接口事务内同步删除，由 `file.cleanup_unreferenced_blobs` 维护任务按 DB 引用状态异步清理。

## 9. 权限系统实现

### 9.1 权限来源

| 来源 | 示例 | 优先级 |
| --- | --- | --- |
| 系统角色 | 超级管理员、审计员 | 最高，但仍受安全策略限制 |
| 空间角色 | space_admin、editor、viewer | 高 |
| 目录 ACL | 指定用户/部门/组可读写 | 中 |
| 继承权限 | 父目录权限向子目录继承 | 中 |
| 文件策略 | 密级、标签、外链策略 | 高，可拒绝 |
| 上下文策略 | IP、设备、时间、地域 | 高，可拒绝 |

权限冲突原则：显式 deny 优先于 allow；文件密级和安全策略优先于普通角色授权；超级管理员操作也必须审计。

### 9.2 权限动作

```text
list      查看目录中文件名
preview   在线预览
read_meta 读取元数据
download  下载原文件
upload    上传新文件
update    重命名、移动、覆盖上传
delete    删除到回收站
restore   从回收站恢复
share     创建分享
grant     授权给其他人
manage    管理空间配置和成员
```

### 9.3 权限判断伪代码

```python
def can_access(ctx: PermissionContext, node: Node, action: str) -> Decision:
    if node.is_deleted and action not in {"restore", "manage"}:
        return deny("node_deleted")

    system_decision = evaluate_system_role(ctx, action)
    policy_decision = evaluate_abac_policy(ctx, node, action)
    if policy_decision.is_deny:
        return policy_decision

    acl_decision = evaluate_node_acl(ctx.subjects, node.ancestor_ids, action)
    if acl_decision.is_deny:
        return acl_decision
    if acl_decision.is_allow:
        return allow("acl")

    space_decision = evaluate_space_role(ctx.subjects, node.space_id, action)
    if space_decision.is_allow:
        return allow("space_role")

    if system_decision.is_allow:
        return allow("system_role")

    return deny("no_matching_grant")
```

### 9.4 权限缓存

缓存 key 建议：

```text
perm:v1:{tenant_id}:{user_id}:{node_id}:{action}:{perm_version}:{policy_version}
```

版本号来源：

1. `tenant.permission_version`：任意角色、部门、组、空间成员变更后递增。
2. `space.permission_version`：空间成员或空间策略变更后递增。
3. `node.permission_version`：目录 ACL 变更后递增。
4. `policy_version`：安全策略、密级策略、外链策略变更后递增。

缓存策略：

1. 普通 list/preview 可缓存 30 到 120 秒。
2. download/delete/grant/manage 等高危动作要短缓存或二次查库。
3. 权限变更必须发布失效事件，Redis 缓存只是加速，不是事实来源。
4. 搜索结果权限过滤不得只依赖旧缓存。

### 9.5 权限数据加载策略

一次权限判断需要的数据：

1. 用户主体集合：`user:{id}`、`department:{id}`、上级部门、`group:{id}`、空间角色。
2. 节点上下文：node、space、ancestor ids、file classification、deleted 状态。
3. 授权规则：space_members、acl_entries、role_permissions、policy rules。
4. 环境上下文：IP、设备、时间、认证强度、是否外链访问。

性能策略：

1. 用户主体集合登录后可缓存，但部门和用户组变更要失效。
2. 祖先节点 ids 可在列表接口批量加载，避免每个文件递归查一次。
3. 文件列表页返回每个节点的常用权限时，应批量评估，不能 N+1 查询。
4. 高危动作仍单独调用权限引擎确认。

### 9.6 批量权限评估接口

内部 service 建议提供：

```python
class PermissionService:
    async def check(
        self,
        ctx: PermissionContext,
        node_id: UUID,
        action: PermissionAction,
    ) -> Decision: ...

    async def batch_check(
        self,
        ctx: PermissionContext,
        node_ids: list[UUID],
        actions: list[PermissionAction],
    ) -> dict[UUID, dict[PermissionAction, Decision]]: ...
```

文件列表接口使用 `batch_check`，下载、删除、分享、授权使用 `check`。

### 9.7 ACL 继承规则

1. 子目录默认继承父目录 ACL。
2. 子目录可以追加 allow 或 deny。
3. deny 优先于 allow。
4. 可以支持“停止继承”字段，但一期建议不开放给普通用户，只保留管理员能力。
5. 文件继承所在目录权限，文件级 ACL 一期可支持但 UI 可暂不暴露。

### 9.8 权限变更后的副作用

权限变更不是只写一张表，还要处理：

1. 递增 `space.permission_version` 或 `node.permission_version`。
2. 删除 Redis 权限缓存。
3. 写审计日志。
4. 发出 `permission.changed` 事件。
5. 发出 `search.acl_rebuild_requested` 事件，搜索索引重建受影响目录的 `acl_tokens`。
6. 如果移除了用户的 share/manage 权限，可选地撤销其创建的高风险外链。

当前空间成员管理 API 已在成员新增、角色调整和移除时递增 `spaces.permission_version` 并写入 `permission.space_member.*` 审计；节点 ACL 管理 API 已在规则新增、调整和删除时递增 `nodes.permission_version` 并写入 `permission.node_acl.*` 审计。两类变更都会在同一业务事务中写入 `permission.changed` outbox event，payload 包含 `scope`、`resource_id`、`permission_version`、`reason`、`actor_id`、主体信息和变更摘要；用户主体变更会携带 `affected_user_id` 精确失效，部门/用户组主体变更当前保守失效租户内节点权限缓存。`permission.invalidate_cache` worker 已消费该事件并失效匹配的 Redis 权限缓存 key。当前已新增 ACL token builder、`search.acl_rebuild_requested` outbox event、`search.index_requested` 文件索引事件和 `search.dispatch_outbox` 队列入口；上传完成、重命名、移动、删除、恢复和彻底删除后的文件索引会从 PostgreSQL 重新加载事实后写入或删除 OpenSearch 文档，ACL 变更后会按 space 或 node 子树保守重建索引 token，`GET /api/v1/search` 查询会使用 allow/deny token 做查询层过滤并通过 `read_meta` 做应用层二次权限校验。

### 9.9 越权防护清单

1. 所有按 ID 查询文件的接口都返回“文件不存在或无权访问”，不要区分 404 和 403 泄露存在性。
2. 下载接口不能只校验分享权限，还要校验文件当前状态、分享状态和次数限制。
3. 搜索结果必须二次校验，尤其是权限刚变更但索引未更新时。
4. 管理后台接口必须校验管理员权限，不能只依赖前端路由。
5. 外链访问不能复用内部用户权限逻辑，必须使用 external subject。

## 10. 分享系统实现

### 10.1 分享类型

1. 内部分享：绑定企业用户、部门或用户组，需要登录。
2. 外链分享：通过随机 token 访问，可选提取码、有效期、访问次数、下载次数。
3. 上传收集：外部访客只能上传到指定目录，不能看到目录已有文件。

### 10.2 外链 token

```text
raw_token = base64url(random_32_bytes)
token_hash = sha256(raw_token)
```

数据库只保存 `token_hash`，并对外链 token hash 建全局唯一索引。用户访问时提交 `tenant_slug + raw_token`，后端先解析租户，再按 `tenant_id + token_hash` 查询。提取码使用 Argon2id 或 bcrypt 哈希，不保存明文。

当前服务层已按该策略生成 256-bit 级随机外链 token，并通过统一 token hash 存储；提取码复用项目 Argon2id 密码哈希能力。外链访问入口已接入 `tenant_slug + raw_token` 租户边界、提取码校验、状态/过期/访问次数判断、IP 总量和 `token + IP` 维度限流，以及 `external` actor 访问日志；外链下载入口仍需加入下载次数原子扣减、下载限流和独立 external subject 权限处理。

### 10.3 访问计数并发控制

外链访问次数和下载次数必须原子更新。推荐数据库条件更新：

```sql
update shares
set download_count = download_count + 1
where id = :share_id
  and status = 'active'
  and (expires_at is null or expires_at > now())
  and (max_downloads is null or download_count < max_downloads)
returning id;
```

如果返回 0 行，说明链接过期、撤销或次数耗尽。

## 11. 预览与转码

### 11.1 任务状态

`preview_status`：`pending`、`processing`、`ready`、`failed`、`unsupported`、`skipped`。

### 11.2 处理流程

```text
file_uploaded event
  -> create preview task
  -> worker downloads object to sandbox temp dir
  -> validate file size and mime type
  -> convert:
       office -> pdf by LibreOffice headless
       image -> thumbnail webp
       video -> metadata + optional h264 preview
       text -> utf-8 normalized preview
  -> upload preview artifacts to object storage
  -> update file_versions.preview_status
  -> emit preview_ready event
```

### 11.3 安全限制

1. 转码必须在临时目录或容器沙箱内执行，不允许使用用户文件名拼 shell 命令。
2. LibreOffice、FFmpeg 等外部命令必须设置超时、内存限制和文件大小限制。
3. 压缩包预览必须防 zip bomb，限制展开文件数量、总大小和路径穿越。
4. 转码失败只影响预览，不影响原文件下载。
5. Worker 节点和 API 节点可分离，避免转码消耗影响核心接口。

## 12. 搜索系统实现

### 12.1 索引结构

OpenSearch index：`drive_files_v1`。

```json
{
  "mappings": {
    "properties": {
      "tenant_id": {"type": "keyword"},
      "space_id": {"type": "keyword"},
      "node_id": {"type": "keyword"},
      "parent_id": {"type": "keyword"},
      "name": {"type": "text", "analyzer": "ik_max_word"},
      "name_keyword": {"type": "keyword"},
      "extension": {"type": "keyword"},
      "mime_type": {"type": "keyword"},
      "size_bytes": {"type": "long"},
      "owner_id": {"type": "keyword"},
      "created_by": {"type": "keyword"},
      "updated_at": {"type": "date"},
      "tags": {"type": "keyword"},
      "acl_tokens": {"type": "keyword"},
      "deny_acl_tokens": {"type": "keyword"},
      "content": {"type": "text", "analyzer": "ik_max_word"},
      "is_deleted": {"type": "boolean"}
    }
  }
}
```

### 12.2 权限过滤方案

搜索不能先返回所有结果再在应用层过滤，因为这样会泄露总数、性能也差。建议索引中写入 `acl_tokens` 和 `deny_acl_tokens`：

```text
user:{user_id}
group:{group_id}
department:{department_id}
space:{space_id}:role:{role}
```

公共空间、外链访问和 external subject 应在分享模块建模后再加入独立 token，不在当前内部用户 ACL token builder 中默认生成：

```text
public:{tenant_id}
```

查询时根据当前用户展开 subject tokens，加入 filter：

```json
{
  "bool": {
    "must": [{"match": {"content": "合同"}}],
    "filter": [
      {"term": {"tenant_id": "..."}},
      {"term": {"is_deleted": false}},
      {"terms": {"acl_tokens": ["space:s1:role:viewer", "user:u1", "department:d1", "group:g1"]}},
      {"bool": {"must_not": [{"terms": {"deny_acl_tokens": ["user:u1", "department:d1", "group:g1"]}}]}}
    ]
  }
}
```

当前实现已提供 `GET /api/v1/search?q=...&limit=...&cursor=...`，查询时会根据当前用户的空间成员角色、用户主体、部门主体和用户组主体生成查询 token，并在 OpenSearch 查询层加入 `tenant_id`、`is_deleted=false`、`acl_tokens` allow 和 `deny_acl_tokens` 排除过滤。分页使用 OpenSearch `search_after` 和服务端签名 cursor，cursor 绑定查询词和排序值，避免客户端篡改或跨查询复用；结果返回 HTML 编码的 `<mark>` highlight 片段。应用层仍会从 PostgreSQL 重新加载节点路径并按 `read_meta` 做二次权限校验，尤其是权限刚变更但索引尚未刷新时。上传完成会额外写入 `search.extract_requested`，搜索 worker 对安全的小型 UTF-8 文本类文件读取对象内容，更新 `file_versions.search_status/search_error/search_text`，再刷新索引 `content` 字段；不支持的 MIME/扩展名标记为 `skipped`，UTF-8 解码失败标记为 `failed` 且不重试，对象存储读取失败标记为 `failed` 并由 outbox 退避重试。PDF、Office 和图片 OCR 等复杂抽取不做自研解析器，后续应接入成熟开源工具或标准服务适配层。

### 12.3 索引一致性

1. 文件上传完成、重命名、移动、删除、恢复和彻底删除后写 outbox event：`search.index_requested`。
2. 文件上传完成后写 outbox event：`search.extract_requested`，抽取任务独立于元数据索引，避免上传事务等待对象读取或文本解析。
3. Worker 从 outbox 消费索引事件，重新加载 PostgreSQL 文件元数据、当前版本、blob、空间成员和 ACL 后写 OpenSearch；如果文件已删除、已彻底删除或不再活跃，则删除对应索引文档。
4. Worker 从 outbox 消费抽取事件，按 `DRIVE_SEARCH_TEXT_EXTRACT_MAX_BYTES` 限制读取文本内容，写入版本抽取状态并刷新索引正文。
5. 权限变更后同事务写入 `permission.changed` 和 `search.acl_rebuild_requested`；搜索 worker 消费后按 space 或 node 子树保守重建受影响文件的 `acl_tokens` / `deny_acl_tokens`。
6. 定时任务比对 PostgreSQL 和 OpenSearch 的更新时间，修复漏索引。
7. 提供 `scripts/rebuild_search_index.py --space-id ...` 用于手动重建。

## 13. 审计与 Outbox

### 13.1 审计事件结构

```json
{
  "event_id": "uuid",
  "tenant_id": "uuid",
  "actor_id": "uuid",
  "actor_type": "user",
  "action": "file.download",
  "resource_type": "file",
  "resource_id": "uuid",
  "space_id": "uuid",
  "result": "allowed",
  "risk_level": "low",
  "request_id": "req_...",
  "ip": "10.0.0.1",
  "user_agent": "...",
  "metadata": {
    "version_id": "uuid",
    "share_id": null
  },
  "created_at": "2026-06-30T10:00:00Z"
}
```

### 13.2 Outbox 表

```sql
create table outbox_events (
  id uuid primary key,
  tenant_id uuid not null,
  event_type varchar(128) not null,
  aggregate_type varchar(64) not null,
  aggregate_id uuid not null,
  payload jsonb not null,
  status varchar(32) not null default 'pending',
  retry_count integer not null default 0,
  next_retry_at timestamptz not null default now(),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index idx_outbox_pending
  on outbox_events(status, next_retry_at, created_at);
```

业务事务内插入 outbox。后台 dispatcher 负责投递到 Celery、日志平台或搜索任务。这样可以避免“数据库写成功但消息没发出去”的经典问题。

## 14. 异步任务与队列

### 14.1 队列划分

| 队列 | 任务 | 优先级 |
| --- | --- | --- |
| `audit` | 审计投递、风险事件 | 高 |
| `permission` | 权限缓存失效、搜索 ACL 更新 | 高 |
| `preview` | Office 转 PDF、图片缩略图、音视频元信息 | 中 |
| `search` | 文本抽取、索引写入、索引重建 | 中 |
| `maintenance` | 过期上传清理、回收站清理、过期外链、容量校准 | 低 |

### 14.2 任务规范

1. 每个任务必须有全局唯一 task_id。
2. 任务参数只传 ID，不传大对象内容。
3. 任务执行前从数据库重新加载最新状态。
4. 任务必须幂等，重复执行不会产生重复副作用。
5. 失败使用指数退避，超过最大重试进入 dead-letter 状态。
6. 任务日志必须包含 task_id、tenant_id、resource_id、request_id。

## 15. API 契约

### 15.1 通用规则

1. API 前缀：`/api/v1`。
2. 分页：列表接口统一使用 cursor pagination，不使用深 offset。
3. 幂等：创建、完成上传、批量操作支持 `Idempotency-Key`。
4. 错误响应统一包含 `code`、`message`、`request_id`、`details`。
5. 所有返回时间使用 ISO 8601 UTC。
6. 用户可见错误不泄露内部对象存储 key、SQL 错误和文件真实存在性。

### 15.2 核心接口清单

| 方法 | 路径 | 权限动作 | 说明 |
| --- | --- | --- | --- |
| GET | `/spaces` | list | 当前用户可访问空间 |
| POST | `/spaces` | manage | 创建团队或项目空间 |
| GET | `/spaces/{space_id}/members` | manage | 查看空间成员 |
| POST | `/spaces/{space_id}/members` | grant | 添加空间成员 |
| PATCH | `/spaces/{space_id}/members/{user_id}` | grant | 调整空间成员角色 |
| DELETE | `/spaces/{space_id}/members/{user_id}` | grant | 移除空间成员 |
| GET | `/files/{node_id}/acl` | grant | 查看节点 ACL |
| POST | `/files/{node_id}/acl` | grant | 创建节点 ACL |
| PATCH | `/files/{node_id}/acl/{entry_id}` | grant | 调整节点 ACL |
| DELETE | `/files/{node_id}/acl/{entry_id}` | grant | 删除节点 ACL |
| GET | `/files` | list | 查询目录子节点 |
| POST | `/files/folders` | upload | 创建文件夹 |
| PATCH | `/files/{node_id}` | update | 重命名、移动、标签更新 |
| DELETE | `/files/{node_id}` | delete | 删除到回收站 |
| DELETE | `/files/{node_id}/purge` | delete | 彻底删除回收站节点 |
| POST | `/files/{node_id}/restore` | restore | 恢复文件 |
| GET | `/files/{node_id}/versions` | read_meta | 文件版本列表 |
| POST | `/uploads/init` | upload | 初始化上传 |
| POST | `/uploads/{id}/complete` | upload | 完成上传 |
| GET | `/files/{node_id}/download` | download | 获取下载地址 |
| POST | `/shares` | share | 创建分享 |
| GET | `/shares/{id}` | read_meta | 分享详情 |
| POST | `/shares/{id}/revoke` | share | 撤销分享 |
| GET | `/search` | list/preview | 搜索文件 |
| GET | `/admin/audit-logs` | audit | 查询审计日志 |

### 15.3 详细接口契约示例

#### 文件列表

```http
GET /api/v1/files?space_id={space_id}&parent_id={parent_id}&limit=100&cursor={cursor}
```

响应：

```json
{
  "items": [
    {
      "id": "uuid",
      "type": "file",
      "name": "合同.docx",
      "size_bytes": 1048576,
      "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
      "extension": "docx",
      "updated_at": "2026-06-30T10:00:00Z",
      "owner": {"id": "uuid", "display_name": "张三"},
      "current_version_id": "uuid",
      "preview_status": "ready",
      "permissions": {
        "list": true,
        "preview": true,
        "download": true,
        "upload": false,
        "update": true,
        "delete": false,
        "share": true,
        "grant": false
      }
    }
  ],
  "next_cursor": "opaque-cursor"
}
```

#### 创建文件夹

```http
POST /api/v1/files/folders
Idempotency-Key: client-generated-key
```

```json
{
  "space_id": "uuid",
  "parent_id": "uuid",
  "name": "项目资料",
  "conflict_policy": "fail"
}
```

`conflict_policy` 计划支持：`fail`、`keep_both`、`replace`。当前最小实现为 fail-only，同名时返回冲突。

#### 重命名节点

```http
PATCH /api/v1/files/{node_id}
```

```json
{
  "name": "新名称"
}
```

根目录不允许重命名。同目录同名由数据库唯一索引兜底。

#### 移动文件

```http
POST /api/v1/files/{node_id}/move
Idempotency-Key: client-generated-key
```

```json
{
  "target_parent_id": "uuid",
  "new_name": "新名称.docx",
  "conflict_policy": "keep_both"
}
```

`new_name` 可省略，省略时保留原名称。根目录不允许移动，目录不能移动到自身或自身子目录。

#### 删除与恢复

```http
DELETE /api/v1/files/{node_id}
POST /api/v1/files/{node_id}/restore
```

```json
{
  "target_parent_id": "uuid",
  "new_name": "恢复后的名称"
}
```

删除进入回收站并写入 `deleted_at`、`deleted_by`。恢复时若原父目录已删除或不存在，必须指定新的 `target_parent_id`；同名冲突不覆盖已有节点。

#### 批量操作

```http
POST /api/v1/files/batch-delete
```

```json
{
  "node_ids": ["uuid", "uuid"],
  "mode": "trash"
}
```

批量接口返回每个对象的结果，不应一个失败导致全部无响应：

```json
{
  "results": [
    {"node_id": "uuid", "status": "success"},
    {"node_id": "uuid", "status": "failed", "code": "PERMISSION_DENIED"}
  ]
}
```

### 15.4 Cursor 分页规范

cursor 必须是不透明字符串，前端不能解析。建议内容：

```json
{
  "sort": "updated_at_desc",
  "last_updated_at": "2026-06-30T10:00:00Z",
  "last_id": "uuid"
}
```

服务端 base64url 编码并签名，防止客户端篡改分页条件。文件列表默认排序：文件夹在前，更新时间倒序，id 作为稳定 tie-breaker。

### 15.5 错误码细化

| 错误码 | HTTP | 场景 |
| --- | --- | --- |
| AUTH_REQUIRED | 401 | 未登录 |
| SESSION_EXPIRED | 401 | 登录会话过期 |
| CSRF_TOKEN_INVALID | 403 | CSRF 校验失败 |
| PERMISSION_DENIED | 403 | 权限不足，但不涉及文件存在性隐藏的场景 |
| FILE_NOT_FOUND | 404 | 文件不存在或无权访问 |
| FILE_CONFLICT | 409 | 同目录重名冲突 |
| UPLOAD_SESSION_EXPIRED | 409 | 上传会话过期 |
| UPLOAD_COMPLETING | 409 | 上传正在完成中 |
| HASH_MISMATCH | 422 | 文件哈希不匹配 |
| QUOTA_EXCEEDED | 422 | 容量不足 |
| SHARE_EXPIRED | 410 | 分享过期 |
| SHARE_REVOKED | 410 | 分享撤销 |
| SHARE_PASSCODE_INVALID | 403 | 分享提取码错误 |
| SHARE_VIEW_LIMIT_EXCEEDED | 410 | 分享访问次数耗尽 |
| RATE_LIMITED | 429 | 触发限流 |
| STORAGE_UNAVAILABLE | 503 | 对象存储不可用 |

## 16. 认证与安全实现

### 16.1 登录与会话

一期支持本地账号，预留 OIDC/LDAP。浏览器认证采用 BFF + HttpOnly Cookie Session：

1. 服务端生成 opaque random session token，只保存哈希到 `auth_sessions`，原始值只写入 `drive_session` HttpOnly Cookie。
2. CSRF token 写入可读 Cookie `drive_csrf`，所有 `POST`、`PUT`、`PATCH`、`DELETE` 请求必须通过 `X-CSRF-Token` 请求头回传。
3. 会话轮换通过 `POST /api/v1/auth/session/rotate` 签发新的 session token 和 CSRF token，旧 session 立即标记为 replaced。
4. 检测到旧 session 被复用时，吊销整个 session family。
5. 项目未上线阶段不保留 JWT、Bearer token、refresh token 或 `/auth/refresh` 兼容路径。
6. 后续接入第三方身份认证时，优先使用 OIDC/OAuth 2.1 + PKCE 和成熟开源库，在 BFF 层完成 code flow 后继续签发本项目服务端 session cookie。

密码哈希：Argon2id。管理员重置密码后必须强制用户下次登录修改。

### 16.2 输入安全

1. 文件名最大 255 字符，禁止 `/`、`\`、控制字符、NUL、路径穿越片段。
2. Unicode 文件名统一 normalize，避免肉眼相同但二进制不同导致绕过重名检查。
3. MIME 类型以服务端嗅探为准，客户端传值只作为参考。
4. 压缩包预览必须限制展开数量、总大小、递归深度和路径。
5. 所有 SQL 使用 SQLAlchemy 参数化，禁止字符串拼接 SQL。
6. 外部 URL 预览或远程拉取默认不做，避免 SSRF。

### 16.3 限流

| 场景 | 建议限制 |
| --- | --- |
| 登录失败 | 用户 + IP 维度，连续失败锁定或验证码 |
| 初始化上传 | 用户维度每分钟限流 |
| 分片签名 | session + 用户维度限流 |
| 下载 | 用户、IP、文件维度限流 |
| 外链访问 | IP 总量和 token + IP 维度限流 |
| 搜索 | 用户维度限流，复杂查询限制频率 |

当前实现已完成上传初始化、分片签名、下载预签名、搜索查询和外链访问的 Redis 固定窗口基础限流：上传初始化按 `tenant + user` 计数，分片签名按 `tenant + user + upload_session` 计数，下载预签名按 `tenant + user + node + IP` 计数，搜索查询按 `tenant + user + search + IP` 计数，外链访问按 IP 总量和 `token + IP` 计数；触发限流返回 HTTP 429 和 `RATE_LIMITED`。登录失败限流仍按认证模块后续接入。

## 17. 容量与配额

### 17.1 容量账本

不要只在 `spaces.used_bytes` 上直接加减。建议使用容量流水：

```sql
create table quota_ledger (
  id uuid primary key,
  tenant_id uuid not null,
  account_type varchar(32) not null,
  account_id uuid not null,
  delta_bytes bigint not null,
  reason varchar(64) not null,
  ref_type varchar(64) not null,
  ref_id uuid not null,
  created_at timestamptz not null default now()
);
```

`quota_accounts.used_bytes` 是当前快照，`quota_ledger` 是可审计流水。高并发上传完成时，使用行锁或原子 update 防止超配额。

### 17.2 检查逻辑

```sql
update quota_accounts
set used_bytes = used_bytes + :size
where id = :account_id
  and used_bytes + :size <= limit_bytes
returning used_bytes;
```

返回 0 行表示容量不足。当前策略是删除到回收站不释放容量，彻底删除回收站节点时释放对应文件版本容量并写入负向流水；blob 引用计数同步扣减，但最终对象物理删除仍交给后续垃圾回收和对象生命周期任务。

## 18. 前端协作契约

### 18.1 上传队列前端要求

1. 上传队列支持暂停、继续、取消、失败重试。
2. 每个文件显示初始化、上传中、合并中、处理中、完成、失败状态。
3. 分片上传失败时只重试失败分片。
4. 刷新页面后可通过 upload session 恢复未完成上传。
5. 秒传时不要显示长时间进度条，直接进入完成状态。

### 18.2 文件列表交互契约

后端列表接口返回：

```json
{
  "items": [
    {
      "id": "uuid",
      "type": "file",
      "name": "方案.docx",
      "size_bytes": 1234,
      "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
      "updated_at": "2026-06-30T10:00:00Z",
      "permissions": {
        "preview": true,
        "download": true,
        "upload": false,
        "delete": false,
        "share": true
      },
      "preview_status": "ready"
    }
  ],
  "next_cursor": "..."
}
```

前端按钮状态必须以后端返回的 `permissions` 为准，但这只是体验控制，安全仍由后端接口校验。

## 19. 测试策略

### 19.1 单元测试重点

1. 文件名 normalization 和重名冲突策略。
2. 权限决策：allow、deny、继承、拒绝优先、空间角色、目录 ACL。
3. 容量检查：并发扣减、回滚、删除释放、版本容量。
4. 分享策略：过期、次数耗尽、撤销、提取码错误。
5. 上传状态机：init、complete、abort、expired、idempotent complete。

### 19.2 集成测试重点

使用 testcontainers 拉起 PostgreSQL、Redis、MinIO、OpenSearch。

1. 上传初始化创建 multipart upload。
2. 完成上传后写入 blob、version、node、audit、outbox。
3. 权限变更后缓存失效。
4. 搜索索引写入后按权限过滤。
5. 审计 outbox 投递失败后可重试。

### 19.3 并发测试重点

1. 同目录并发上传同名文件，不能破坏唯一约束。
2. 同一 upload session 并发 complete，只能创建一个版本。
3. 多用户同时写入同一空间，容量不能超扣或漏扣。
4. 外链下载次数并发扣减不能超过 max_downloads。
5. 权限变更与下载并发时，高危下载必须重新校验。

### 19.4 测试用例矩阵

| 模块 | 用例 | 期望 |
| --- | --- | --- |
| 文件树 | 同目录创建同名文件夹 | 返回 FILE_CONFLICT 或按策略重命名 |
| 文件树 | 移动目录到自身子目录 | 返回非法操作 |
| 文件树 | 删除父目录后访问子文件 | 不可访问且搜索不可见 |
| 上传 | complete 接口重复调用 | 只创建一个版本，返回相同结果 |
| 上传 | 分片缺失时 complete | 返回 PART_MISSING，不创建文件 |
| 上传 | 容量不足时 init | 返回 QUOTA_EXCEEDED，不创建 session |
| 下载 | 无权限用户猜测 file_id | 返回 FILE_NOT_FOUND 或无权访问统一错误 |
| 权限 | 父目录 allow、子目录 deny | 子目录 deny 生效 |
| 权限 | 用户从部门移除 | 权限缓存失效后不可访问 |
| 分享 | 外链过期 | 返回 SHARE_EXPIRED |
| 分享 | 下载次数并发耗尽 | 下载次数不超过 max_downloads |
| 搜索 | 权限撤销后搜索 | 不返回无权限文件，或二次校验过滤 |
| 审计 | 下载失败 | 记录 denied 审计日志 |
| 容量 | 并发上传超过剩余额度 | 只有部分成功，总容量不超限 |

### 19.5 契约测试

前后端接口变更必须通过契约测试。建议流程：

1. 后端生成 OpenAPI JSON。
2. CI 比较 OpenAPI diff，发现 breaking change 时阻断合并。
3. 前端基于 OpenAPI 生成 TypeScript client。
4. 后端为核心接口提供示例响应 snapshot。
5. 错误码和枚举值变更必须同步更新文档和前端类型。

### 19.6 性能基准测试

一期最低基准：

| 场景 | 数据规模 | 目标 |
| --- | --- | --- |
| 文件列表 | 单目录 10,000 节点 | P95 < 500 ms |
| 权限批量评估 | 100 个节点 x 6 个动作 | P95 < 200 ms |
| 初始化上传 | 100 RPS | P95 < 300 ms |
| 完成上传 | 50 RPS | P95 < 800 ms，不含对象存储合并耗时 |
| 搜索 | 100 万索引文档 | P95 < 800 ms |
| 审计查询 | 1,000 万日志 | 常见筛选 P95 < 1 s |

## 20. CI/CD 与部署

### 20.1 CI 流程

```text
checkout
  -> setup uv
  -> uv sync --frozen --all-extras --dev
  -> ruff check
  -> ruff format --check
  -> mypy app
  -> pytest
  -> build docker image
  -> dependency vulnerability scan
```

### 20.2 Dockerfile 要点

1. 使用多阶段构建。
2. 构建阶段运行 `uv sync --frozen`。
3. 运行镜像使用非 root 用户。
4. 不把 `.env`、测试文件、缓存目录复制到生产镜像。
5. 健康检查使用 `/healthz` 和 `/readyz`。

### 20.3 Kubernetes 资源

至少拆分：

1. `api-deployment`：FastAPI。
2. `worker-preview-deployment`：重 CPU/内存转码任务。
3. `worker-search-deployment`：文本抽取和索引。
4. `worker-audit-deployment`：审计投递。
5. `cronjob-maintenance`：过期外链、回收站、容量校准、blob/object 清理。
6. `migration-job`：发布时执行 Alembic migration。

API 和 Worker 分开扩容。预览 Worker 必须设置 CPU、内存和临时磁盘限制。

### 20.4 本地 Docker Compose 服务

本地开发至少包含：

```text
postgres:16
redis:7
minio
opensearch
opensearch-dashboards optional
mailhog optional
```

本地启动目标：

```bash
make dev-up
uv sync --all-extras --dev
uv run alembic upgrade head
uv run python scripts/seed_admin.py
uv run fastapi dev app/main.py
```

### 20.5 发布顺序

1. 合并代码前：ruff、mypy、pytest、OpenAPI diff。
2. 构建镜像：固定 uv.lock，生成 SBOM，可选漏洞扫描。
3. 执行 migration job：只允许向前兼容 migration。
4. 部署 API：滚动发布，readiness 通过后接流量。
5. 部署 Worker：按队列逐类发布，避免任务中断扩大。
6. 观察指标：错误率、延迟、队列积压、数据库慢查询。
7. 发布完成：标记版本、归档 OpenAPI、记录 migration 版本。

### 20.6 回滚原则

1. 应用可快速回滚镜像。
2. 数据库 migration 原则上只做向前兼容，避免依赖回滚 DDL。
3. 如果必须执行破坏性迁移，必须先做影子字段和双写验证。
4. Worker 任务 payload 必须兼容至少一个旧版本，避免发布过程中旧任务无法消费。

## 21. 可观测性

### 21.1 日志字段

每条结构化日志至少包含：

```text
timestamp, level, service, env, request_id, trace_id, tenant_id,
user_id, module, action, resource_type, resource_id, status, latency_ms
```

### 21.2 指标

| 指标 | 类型 | 说明 |
| --- | --- | --- |
| `http_requests_total` | counter | API 请求数 |
| `http_request_duration_seconds` | histogram | API 延迟 |
| `upload_sessions_total` | counter | 上传会话数量 |
| `upload_failures_total` | counter | 上传失败数 |
| `download_requests_total` | counter | 下载次数 |
| `permission_decision_duration_seconds` | histogram | 权限判断耗时 |
| `worker_tasks_total` | counter | 任务数量 |
| `worker_task_duration_seconds` | histogram | 任务耗时 |
| `preview_failures_total` | counter | 预览失败数 |
| `search_index_lag_seconds` | gauge | 搜索索引延迟 |
| `outbox_pending_total` | gauge | 待投递事件数 |

### 21.3 告警

1. API 5xx 错误率 5 分钟超过 2%。
2. 上传完成失败率 10 分钟超过 5%。
3. outbox pending 数量持续增长。
4. preview 队列积压超过阈值。
5. PostgreSQL 慢查询或连接池耗尽。
6. 对象存储 5xx 或延迟异常。
7. 磁盘、临时目录或对象存储容量超过 80%。

## 22. 开发里程碑

### 22.1 Sprint 1：工程底座

1. uv 工程、FastAPI app、配置、日志、错误响应、request_id middleware。
2. SQLAlchemy、Alembic、PostgreSQL、Redis、MinIO 本地 compose。
3. 用户登录、BFF Cookie 会话、基础用户表和管理员 seed。
4. CI：ruff、mypy、pytest。

完成标准：本地 `uv sync` 后一条命令启动 API 和依赖，健康检查通过。

### 22.2 Sprint 2：空间和文件树

1. space、node、file_blob、file_version model 和 migration。
2. 文件夹创建、文件列表、重命名、移动、删除到回收站。
3. 同目录重名策略和 cursor pagination。
4. 基础审计日志。

完成标准：可通过 API 创建空间、建目录、列目录、删除恢复。

### 22.3 Sprint 3：上传下载

1. upload_session、multipart presign、complete、abort。（已完成接口闭环）
2. 秒传、对象存储适配、容量初版、服务端 hash 校验和最终对象 key 规整。（已完成秒传、对象存储适配、空间容量账本初版、multipart complete 后 `sha256` 校验和 `objects/{tenant_id}/{hash_prefix}/{content_hash}` 归档）
3. 过期上传清理任务。（已完成 `upload.expire_sessions`，按租户扫描过期会话并清理临时对象）
4. 基础限流。（已完成上传初始化、分片签名和下载预签名的 Redis 固定窗口限流）
5. 容量校准和 blob/object 清理。（已完成 `quota.reconcile_space_usage` 和 `file.cleanup_unreferenced_blobs` 维护任务）
6. 下载预签名 URL。（已完成当前版本下载签名）
7. 上传下载审计。（已完成上传初始化、秒传、complete、abort、expired 和下载成功/拒绝审计）

完成标准：1 GB 文件可稳定分片上传、刷新后继续、完成后下载。

### 22.4 Sprint 4：权限系统

1. 空间成员和角色。（已完成用户维度 `space_members` 基础表、创建空间 owner 成员写入、空间级角色检查和 owner/admin 成员管理 API）
2. 目录 ACL、权限继承、拒绝优先。（已完成用户、部门、用户组三类节点 ACL 主体，管理 API、继承开关、deny 优先和文件/上传/下载关键入口校验）
3. 权限缓存和失效事件。（已完成文件列表批量权限评估、`permission.changed` outbox 事件写入和 Redis 缓存失效 worker）
4. 部门/用户组 ACL 主体。（已完成 org 基础事实表、repository 和权限判断主体展开）
5. 搜索 ACL 事件、文件索引写入和查询过滤。（已完成 ACL token builder、`search.acl_rebuild_requested` outbox event、上传完成、重命名、移动、删除、恢复和彻底删除后的 `search.index_requested` 事件、上传完成后的 `search.extract_requested` 文本抽取入口、OpenSearch 文件索引写入/删除入口、ACL 变更后的保守范围重建，以及 `GET /api/v1/search` 查询层 allow/deny token 过滤、搜索限流和应用层二次权限校验）
6. 高危操作二次查库。

完成标准：权限专项测试覆盖核心动作，越权下载失败。

### 22.5 Sprint 5：分享、预览、搜索

1. 内部分享和外链分享。
2. 提取码、过期、次数限制、撤销。
3. 预览 Worker、Office 转 PDF、图片缩略图。
4. OpenSearch 索引和权限过滤。

完成标准：分享链接可控访问，主流文档可预览，搜索不泄露无权限文件。

### 22.6 Sprint 6：管理、治理、上线

1. 管理后台 API：用户、部门、空间、容量、审计。
2. 运维监控、告警、备份恢复脚本。
3. 压测、安全测试、UAT 修复。
4. Kubernetes 部署和上线手册。

完成标准：试点环境上线，监控告警可用，备份恢复演练通过。

### 22.7 详细工程任务拆分

以下任务可直接拆成 issue。估时按熟练工程师计算，实际根据团队经验调整。

| 编号 | 任务 | 依赖 | 估时 | 验收 |
| --- | --- | --- | --- | --- |
| BE-001 | 初始化 uv + FastAPI 工程 | 无 | 1d | healthz、readyz、OpenAPI 可访问 |
| BE-002 | 配置系统和结构化日志 | BE-001 | 1d | request_id 贯穿日志和响应 |
| BE-003 | PostgreSQL/Redis/MinIO compose | BE-001 | 1d | 本地一键启动依赖 |
| BE-004 | SQLAlchemy session 和 Alembic | BE-003 | 1d | migration 可升级回滚测试库 |
| BE-005 | 用户、密码、Cookie 会话认证 | BE-004 | 3d | 登录、会话轮换、登出、CSRF 和复用检测测试通过 |
| BE-006 | tenant/user/department/group 模型 | BE-004 | 2d | seed 用户和部门树 |
| BE-007 | space 和 space_member 模型 | BE-006 | 2d | 创建个人/团队空间 |
| BE-008 | node/file_blob/file_version 模型 | BE-007 | 2d | migration 和基础 repository 测试 |
| BE-009 | 文件夹创建和列表 | BE-008 | 3d | cursor 分页、重名冲突测试 |
| BE-010 | 文件移动/重命名/删除/恢复 | BE-009 | 4d | 目录移动和回收站测试 |
| BE-011 | S3 StorageAdapter | BE-003 | 3d | MinIO multipart 集成测试 |
| BE-012 | upload_session 初始化 | BE-011 | 3d | 权限、容量、秒传分支测试 |
| BE-013 | 分片签名和状态查询 | BE-012 | 2d | 断点续传测试 |
| BE-014 | complete upload | BE-013 | 4d | 幂等、缺片、对象合并测试 |
| BE-015 | 下载预签名 URL | BE-014 | 2d | 权限和审计测试 |
| BE-016 | quota_account 和 ledger | BE-012 | 3d | 并发容量扣减测试 |
| BE-017 | 权限角色和 ACL 表 | BE-010 | 3d | 角色授权和目录 ACL 测试 |
| BE-018 | PermissionService check/batch_check | BE-017 | 5d | 继承、deny 优先、批量评估测试 |
| BE-019 | 权限缓存和失效事件 | BE-018 | 3d | 权限变更后缓存失效 |
| BE-020 | share 外链 token 和提取码 | BE-018 | 4d | 过期、撤销、次数限制测试 |
| BE-021 | audit_logs 和 outbox | BE-004 | 3d | 业务事务内写审计和 outbox |
| BE-022 | outbox dispatcher | BE-021 | 3d | 失败重试和 dead-letter 测试 |
| BE-023 | preview worker | BE-014 | 5d | Office/PDF/图片预览状态 |
| BE-024 | OpenSearch indexer | BE-014,BE-018 | 5d | 权限过滤搜索测试 |
| BE-025 | admin audit query | BE-021 | 2d | 按用户、文件、时间筛选 |
| BE-026 | lifecycle cleanup jobs | BE-010,BE-016 | 3d | 过期上传、blob 清理、回收站清理 |
| BE-027 | metrics/tracing 接入 | BE-001 | 3d | Prometheus 指标可见 |
| BE-028 | K8s manifests/Helm | BE-001 | 4d | test 环境部署成功 |
| BE-029 | 性能压测脚本 | BE-014,BE-018 | 3d | 输出基准报告 |
| BE-030 | 安全测试修复 | 全部核心模块 | 5d | 高危问题清零 |

### 22.8 每个 Sprint 的技术验收物

| Sprint | 技术验收物 |
| --- | --- |
| Sprint 1 | 可运行工程、依赖服务、登录、CI、基础日志 |
| Sprint 2 | 核心表 migration、空间、文件树、目录操作 API |
| Sprint 3 | S3 上传下载、秒传、断点续传、容量账本、服务端 hash 校验、最终对象 key 规整、过期上传清理、容量校准、blob/object 清理、基础限流 |
| Sprint 4 | 权限服务、ACL、缓存失效、批量权限评估 |
| Sprint 5 | 分享、预览、搜索、Outbox、异步任务 |
| Sprint 6 | 管理 API、压测、安全修复、K8s 部署、运维文档 |

### 22.9 Issue 模板

```markdown
## 背景
说明业务场景和技术目标。

## 范围
- 本 issue 要做什么
- 本 issue 不做什么

## 接口/数据变更
列出 API、schema、migration、事件和配置变更。

## 验收标准
- 单元测试
- 集成测试
- 权限测试
- 审计日志
- OpenAPI 文档

## 风险
并发、一致性、权限、安全、性能风险。
```

## 23. 技术风险清单

| 风险 | 具体表现 | 开发应对 |
| --- | --- | --- |
| 元数据与对象不一致 | 对象上传成功但 DB 失败 | 补偿任务、孤儿对象扫描、事务边界清晰 |
| 权限缓存过期 | 用户权限被撤销后仍可访问 | 版本化缓存、高危操作二次查库、失效事件 |
| 搜索泄露 | 搜到无权限文件名或内容 | 索引 ACL token + 应用层二次校验 |
| 目录并发冲突 | 同名文件并发创建 | 数据库唯一索引 + 冲突重试 |
| 大文件压垮 API | 后端代理所有文件流量 | 默认客户端直传对象存储，API 只签名 |
| 转码拖垮系统 | LibreOffice 占满 CPU/内存 | Worker 隔离、资源限制、队列限流 |
| 审计丢失 | 日志平台不可用 | 本地 audit_logs + outbox 重试 |
| 容量统计漂移 | 并发上传删除导致用量不准 | 容量账本 + 原子扣减 + 定期校准 |

## 24. Definition of Done

一个后端功能模块完成，必须同时满足：

1. 有 migration、model、schema、service、repository、router 和测试。
2. 有 OpenAPI 文档和错误码。
3. 关键路径写审计日志。
4. 权限校验在 service 层执行，并有越权测试。
5. 事务边界明确，外部调用有超时和错误处理。
6. 日志包含 request_id，异常不会泄露内部实现。
7. 单元测试和必要集成测试通过。
8. ruff、mypy、pytest 在 CI 通过。

## 25. 附录：配置样例

```env
APP_ENV=local
APP_NAME=enterprise-drive
DRIVE_DATABASE_URL=postgresql+asyncpg://drive:drive@localhost:5432/drive
DRIVE_REDIS_URL=redis://localhost:6379/0
DRIVE_S3_ENDPOINT_URL=http://localhost:9000
DRIVE_S3_ACCESS_KEY_ID=minioadmin
DRIVE_S3_SECRET_ACCESS_KEY=minioadmin
DRIVE_S3_REGION=us-east-1
DRIVE_S3_BUCKET=enterprise-drive
DRIVE_SESSION_COOKIE_NAME=drive_session
DRIVE_CSRF_COOKIE_NAME=drive_csrf
DRIVE_CSRF_HEADER_NAME=X-CSRF-Token
DRIVE_SESSION_COOKIE_SAMESITE=lax
DRIVE_SESSION_COOKIE_SECURE=false
DRIVE_SESSION_DAYS=30
DRIVE_UPLOAD_SESSION_TTL_MINUTES=1440
DRIVE_UPLOAD_PART_SIZE_BYTES=8388608
UPLOAD_MAX_FILE_SIZE=21474836480
DRIVE_UPLOAD_PRESIGN_EXPIRES_SECONDS=900
PRESIGN_DOWNLOAD_EXPIRES_SECONDS=120
DRIVE_OPENSEARCH_URL=http://localhost:9200
DRIVE_CELERY_BROKER_URL=redis://localhost:6379/1
DRIVE_CELERY_RESULT_BACKEND=redis://localhost:6379/2
```

## 26. 附录：开发检查清单

### 26.1 接口开发检查

1. 是否有权限校验。
2. 是否写审计日志。
3. 是否有 request_id。
4. 是否有统一错误码。
5. 是否支持幂等键，若为写操作。
6. 是否有分页限制，若为列表接口。
7. 是否避免泄露文件存在性。
8. 是否有单元测试和至少一个异常分支测试。

### 26.2 数据库开发检查

1. 查询是否带 tenant_id。
2. 大表查询是否命中索引。
3. 是否存在 N+1 查询。
4. migration 是否向前兼容。
5. 是否需要分区或后台回填。
6. JSONB 字段是否被滥用于高频查询。
7. 并发写是否有唯一索引或行锁兜底。

### 26.3 对象存储开发检查

1. bucket 是否私有。
2. key 是否避免暴露文件名和目录路径。
3. presign URL 是否短期有效。
4. multipart upload 失败是否能 abort。
5. 临时对象是否有生命周期清理。
6. 对象存储异常是否有清晰错误码和重试策略。

### 26.4 权限开发检查

1. 是否服务端校验，不依赖前端按钮隐藏。
2. 是否考虑目录继承和 deny 优先。
3. 是否考虑用户部门、用户组和空间角色。
4. 权限变更后是否失效缓存。
5. 搜索和分享是否做了单独权限处理。
6. 下载、删除、授权等高危动作是否二次确认。

### 26.5 Worker 开发检查

1. 任务是否幂等。
2. 是否有最大重试和 dead-letter。
3. 是否只传 ID，不传大 payload。
4. 是否设置外部命令超时。
5. 是否记录 task_id、resource_id、tenant_id。
6. 是否避免 preview 队列阻塞 audit 或 permission 队列。

## 27. 结论
开发实现时，最重要的不是先把页面功能堆满，而是把文件对象、元数据、权限、容量、审计和异步任务这些底层关系做稳。企业网盘的复杂度主要来自“大文件 + 权限 + 审计 + 一致性”，而不是普通 CRUD。

一期建议严格按模块化单体推进：先打牢 uv 工程底座和数据模型，再完成文件树、上传下载、权限、分享、预览搜索和管理能力。每个模块都必须有明确事务边界、权限测试、审计事件和可观测指标。这样后续无论是做在线协同、DLP、桌面同步还是多租户扩展，都有可靠的工程地基。
