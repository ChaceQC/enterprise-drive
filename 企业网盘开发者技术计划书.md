# 企业网盘开发者技术计划书（Python + uv 后端）

> 文档版本：v1.3
> 当前代码基线：v1.0.0 候选发布
> 面向读者：后端、前端、测试、DevOps、安全工程师  
> 技术基线：Python 3.12+、uv、FastAPI、SQLAlchemy 2.x、PostgreSQL 16+、Redis、S3 兼容对象存储、OpenSearch、Celery、Windows 11 Docker Desktop（WSL2/Linux containers）
> 文档目标：把企业网盘拆成可实现、可测试、可部署、可扩展、可拆分开发任务的工程方案。

## 0. 开发者视角的目标

这份文档不是立项汇报材料，而是开发实施说明。项目一期要实现一个“企业可试点上线”的网盘后端，核心不是界面看起来像网盘，而是下列技术闭环必须成立：

1. 文件元数据、对象存储、权限、审计、搜索和异步任务之间的数据一致性可控。
2. 上传下载链路能承载大文件、断点续传、秒传、并发上传和失败重试。
3. 权限判断必须服务端可信，支持空间角色、目录授权、继承、拒绝优先和外链策略。
4. 文件对象不能依赖用户路径存储，移动、重命名、版本管理不能复制大对象。
5. 所有关键操作可审计，审计日志不能因为异步日志平台异常而丢关键事件。
6. 后端工程必须用 uv 锁定依赖，CI 可重复构建，测试环境可一键拉起依赖。

### 0.1 领域术语

| 术语 | 含义 | 开发约束 |
| --- | --- | --- |
| tenant | 企业或租户边界 | 一期可单租户，但所有核心表必须保留 tenant_id |
| space | 文件空间 | 个人、部门、团队、项目、公共空间都抽象为 space |
| node | 文件树节点 | 目录和文件统一建模，文件节点通过 current_version_id 指向当前版本 |
| blob | 真实文件内容 | 按内容哈希去重，不包含业务文件名和目录路径 |
| version | 文件版本 | 覆盖上传产生新版本，不覆盖旧版本记录 |
| ACL | 访问控制列表 | 绑定用户、部门、用户组或空间角色的权限规则 |
| subject | 权限主体 | user、department、group、space_role、external_visitor |
| action | 权限动作 | list、preview、download、upload、update、delete、share、grant、manage |
| outbox | 事务事件表 | 业务事务内落库，异步投递到队列或外部系统 |
| ledger | 容量流水 | 用于追溯容量变化，不能只依赖 used_bytes 快照 |

### 0.2 一期开发边界

一期必须实现的工程闭环：

1. 本地账号登录、管理员 seed、BFF + HttpOnly Cookie Session、CSRF 防护。
2. 空间、文件树、文件版本、回收站、基础容量账本。
3. S3 兼容对象存储上传下载，支持 multipart upload。
4. 目录权限、空间角色、拒绝优先、权限缓存和高危动作二次校验。
5. 内部分享、外链分享、提取码、过期、次数限制、撤销。
6. 预览 Worker、搜索索引 Worker、审计 outbox dispatcher。
7. Docker Compose 本地开发环境、Windows 11 Docker 正式部署、Alembic migration、CI 质量门禁。

一期明确不做：

1. 自研 Office 协同编辑器。
2. 完整桌面同步客户端不进入一期交付；已纳入二期 Rust 桌面客户端 Sprint 7 和 Sprint 8。
3. Web 用户端与管理后台不进入一期后端交付；已纳入 Sprint 10。
4. 跨地域双活。
5. 复杂 DLP 内容识别引擎。
6. 多租户计费系统。

### 0.3 开发优先级

| 优先级 | 内容 | 原因 |
| --- | --- | --- |
| P0 | 账号、空间、文件树、上传下载、权限、审计 | 没有这些就不是企业网盘 |
| P1 | 分享、回收站、版本、容量、搜索、预览 | 试点用户日常使用必须具备 |
| P2 | 管理后台 API、Web 用户端、统计、生命周期、批量治理、Rust 桌面客户端 | 支撑终端使用、运维、管理员效率和文件同步 |
| P3 | 在线编辑、审批流、复杂 DLP | 二期后续增强能力 |

## 1. 总体技术架构

### 1.1 一期架构选择

一期采用模块化单体，而不是一开始拆微服务。原因很直接：文件、权限、空间、容量和审计在事务上耦合很强，过早拆服务会把本来可控的数据库事务拆成大量分布式一致性问题。

模块化单体需要做到：

1. 模块边界清楚，每个模块有独立 router、schema、service、repository、policy、tests。
2. 模块之间通过 service 接口调用，不允许跨模块直接访问对方 repository。
3. 所有外部基础设施通过 infrastructure adapter 访问，便于测试替换。
4. 领域事件统一走 outbox 或任务队列，避免业务代码到处直接投递消息。

### 1.2 运行时组件

```text
Browser / H5 / Admin Console
        |
Windows 11 Docker Desktop / Nginx Gateway
        |
FastAPI App Containers
        |
-------------------------------------------------------
Auth | Org | Space | File | Upload | Permission | Share
Preview | Search | Audit | Admin | Quota | Lifecycle
-------------------------------------------------------
        |
PostgreSQL | Redis | S3 Object Storage | OpenSearch | MQ
        |
Celery Workers: preview / search / audit / lifecycle / cleanup
```

### 1.3 组件职责

| 组件 | 职责 | 强一致要求 |
| --- | --- | --- |
| PostgreSQL | 用户、空间、权限、文件树、版本、分享、容量账本、审计索引 | 是 |
| Object Storage | 原文件、分片、缩略图、预览文件、归档文件 | 与元数据最终一致 |
| Redis | 限流、权限缓存、上传临时状态、短期验证码、分布式锁 | 否，不存核心事实 |
| OpenSearch | 文件名、标签、全文内容搜索 | 最终一致 |
| Celery/MQ | 转码、索引、审计投递、生命周期清理 | 任务幂等 |
| FastAPI | 业务 API、鉴权、权限校验、元数据事务 | 是 |

## 2. 后端工程结构

### 2.1 推荐目录

```text
backend/
  pyproject.toml
  uv.lock
  alembic.ini
  Dockerfile
  README.md
  app/
    main.py
    api/
      deps.py
      errors.py
      middleware.py
      v1/
        router.py
    core/
      config.py
      logging.py
      security.py
      pagination.py
      idempotency.py
      time.py
    db/
      base.py
      session.py
      models.py
      transactions.py
    modules/
      auth/
        router.py
        schemas.py
        service.py
        repository.py
        models.py
        tokens.py
        tests/
      org/
      space/
      file/
      upload/
      permission/
      share/
      preview/
      search/
      audit/
      quota/
      admin/
    infrastructure/
      storage/
        base.py
        s3.py
        local.py
      cache/
        redis.py
      queue/
        celery_app.py
      search/
        opensearch.py
      preview/
        libreoffice.py
        ffmpeg.py
    workers/
      preview_tasks.py
      search_tasks.py
      audit_tasks.py
      lifecycle_tasks.py
    tests/
      conftest.py
      factories.py
  migrations/
    versions/
  scripts/
    seed_admin.py
    rebuild_search_index.py
    reconcile_storage.py
```

### 2.2 分层规则

```text
router -> service -> domain/policy -> repository -> db/infrastructure
```

1. router 只负责参数解析、依赖注入、HTTP 状态码和响应模型。
2. service 负责编排事务、调用权限策略、容量账本、对象存储和领域事件。
3. policy 只负责规则判断，不直接访问 HTTP request，不直接写数据库。
4. repository 负责 SQL 查询和数据持久化，不包含业务流程。
5. infrastructure 负责外部系统适配，例如 S3、Redis、OpenSearch、LibreOffice。

禁止事项：

1. 禁止在 router 中写复杂 SQL。
2. 禁止在 repository 中读取当前登录用户。
3. 禁止跨模块直接 import 对方 repository。
4. 禁止把 Redis 缓存当作权限或容量的唯一事实来源。
5. 禁止在数据库事务里执行长时间对象存储上传、文档转码或网络调用。

### 2.3 模块交付清单

每个模块必须交付代码、迁移、测试和文档，不允许只交付接口空壳。

| 模块 | 必交付文件 | 必交付测试 |
| --- | --- | --- |
| auth | model、router、service、tokens、password、session repository | 登录成功、密码错误、令牌刷新、令牌复用检测 |
| org | user、department、group、membership repository | 部门树、用户禁用、用户组展开 |
| space | space model、space_member、quota account | 创建空间、成员角色、空间禁用 |
| file | node、version、blob、folder service、trash service | 同名冲突、移动、删除、恢复、版本回滚 |
| upload | upload_session、storage adapter、complete service | 秒传、分片、断点续传、幂等 complete |
| permission | acl_entry、policy evaluator、cache invalidator | 继承、拒绝优先、空间角色、策略拒绝 |
| share | share、share_recipient、share token、passcode | 外链过期、提取码错误、撤销、次数耗尽 |
| preview | worker、converter、artifact writer | 支持格式、失败状态、超时处理 |
| search | indexer、query builder、acl token builder | 权限过滤、重建索引、删除同步 |
| audit | audit_log、outbox、dispatcher | 业务事务写日志、投递失败重试 |
| quota | quota_account、ledger、reconcile job | 并发扣减、回滚、定期校准 |
| admin | admin router、audit query、stats query | 管理权限、分页、筛选、导出 |

### 2.4 代码命名约定

1. 数据库模型使用单数类名、复数表名，例如 `Node` -> `nodes`。
2. Pydantic schema 按用途后缀：`FileListResponse`、`CreateFolderRequest`、`UploadInitResponse`。
3. Service 方法使用业务动词：`create_folder`、`init_upload`、`complete_upload`、`grant_permission`。
4. Repository 方法使用数据语义：`get_by_id_for_update`、`list_children`、`insert_version`。
5. 权限动作使用固定字符串枚举，不在代码里散落魔法字符串。
6. 审计 action 使用点分命名：`file.upload.completed`、`share.external.accessed`。

## 3. uv 与依赖管理

### 3.1 常用命令

```bash
uv python install 3.12
uv sync --all-extras --dev
uv run fastapi dev app/main.py
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
uv run celery -A app.infrastructure.queue.celery_app worker -Q preview,search,audit -l info
uv run alembic upgrade head
uv run pytest
uv run ruff check .
uv run ruff format .
uv run mypy app
```

### 3.2 pyproject.toml 基线

```toml
[project]
name = "enterprise-drive-backend"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.29",
  "alembic>=1.13",
  "pydantic-settings>=2.4",
  "redis>=5.0",
  "argon2-cffi>=23.1",
  "authlib>=1.7",
  "celery[redis]>=5.4",
  "httpx>=0.28",
  "ldap3>=2.9",
  "minio>=7.2",
  "opensearch-py>=2.6",
  "structlog>=24.1",
  "opentelemetry-api>=1.26",
  "opentelemetry-sdk>=1.26",
  "opentelemetry-instrumentation-fastapi>=0.47b0",
]

[dependency-groups]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "httpx>=0.27",
  "ruff>=0.5",
  "mypy>=1.10",
  "testcontainers>=4.7",
  "factory-boy>=3.3",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["sqlalchemy.ext.mypy.plugin"]
```

CI 中必须使用：

```bash
uv sync --frozen --all-extras --dev
```

这样可以保证 CI、开发机和生产构建使用同一套锁定依赖。

## 4. 核心领域模型

### 4.1 聚合边界

| 聚合 | 根实体 | 说明 |
| --- | --- | --- |
| Tenant | tenant | 单企业部署也保留 tenant_id，便于未来多租户 |
| Org | user、department、group | 用户、部门、用户组、岗位信息 |
| Space | space | 个人空间、部门空间、团队空间、项目空间 |
| FileTree | node | 文件夹和文件节点，维护树结构 |
| FileContent | file_blob、file_version | 文件对象和版本，不等同于路径节点 |
| Permission | role_binding、acl_entry、policy_rule | 空间角色、目录 ACL、策略规则 |
| Upload | upload_session、upload_part | 分片上传状态机 |
| Share | share、share_item、share_access_log | 内部分享和外链分享 |
| Audit | audit_log、outbox_event | 审计查询和异步投递 |
| Quota | quota_account、quota_ledger | 容量配额和容量流水 |

### 4.2 关键设计选择

1. `node` 表表示目录树中的一个位置，可以是文件夹或文件。
2. `file_blob` 表表示真实对象内容，按内容哈希去重。
3. `file_version` 表表示某个文件节点的某个版本，指向一个 blob。
4. 文件重命名和移动只改 `node`，不改 `file_blob`。
5. 覆盖上传不是覆盖 blob，而是新增 `file_version` 并更新 `node.current_version_id`。
6. 删除默认是逻辑删除，进入回收站；彻底删除后再按引用计数和生命周期清理 blob。

### 4.3 状态机总览

上传、预览、搜索、分享和删除都必须按状态机实现，避免布尔字段堆叠后不可维护。

| 对象 | 状态 | 触发动作 |
| --- | --- | --- |
| upload_session | initiated、uploading、completing、completed、aborted、expired、failed | init、part_uploaded、complete、abort、expire、fail |
| file_version.preview_status | pending、processing、ready、failed、unsupported、skipped | enqueue、start、finish、fail、skip |
| file_version.search_status | pending、extracting、indexed、failed、skipped | enqueue、extract、index、fail |
| share.status | active、expired、revoked、disabled | create、expire、revoke、risk_disable |
| node lifecycle | active、deleted、purged、archived | create、delete、restore、purge、archive |
| outbox_event.status | pending、processing、sent、failed、dead | create、dispatch、ack、retry、dead_letter |

状态机实现要求：

1. 状态流转必须集中在 service 或 domain method，不允许散落 update。
2. 非法状态流转抛出业务异常并写入 debug 日志。
3. 任务重试时，如果目标状态已经完成，应直接返回成功，保证幂等。
4. 管理后台展示状态时使用后端枚举，不由前端硬编码中文状态。

### 4.4 领域事件清单

| 事件 | 产生时机 | 消费者 |
| --- | --- | --- |
| file.created | 新文件创建完成 | preview、search、audit、quota stats |
| file.version.created | 覆盖上传或新版本完成 | preview、search、audit |
| file.moved | 文件或目录移动 | search、permission cache、audit |
| file.deleted | 删除到回收站 | search、audit、quota stats |
| file.restored | 从回收站恢复 | search、audit |
| file.purged | 彻底删除 | blob cleanup、search、audit、quota |
| permission.changed | ACL 或角色变更 | permission cache、search acl rebuild、audit |
| share.created | 分享创建 | audit、risk engine |
| share.accessed | 分享访问 | audit、rate limit stats |
| upload.completed | 上传完成 | audit、preview、search |

## 5. PostgreSQL 表设计

以下 DDL 是开发基线，不是最终完整迁移脚本。实际实现通过 SQLAlchemy model + Alembic migration 管理。

### 5.1 nodes 文件树

```sql
create table nodes (
  id uuid primary key,
  tenant_id uuid not null,
  space_id uuid not null,
  parent_id uuid null references nodes(id),
  node_type varchar(16) not null check (node_type in ('folder', 'file')),
  name varchar(255) not null,
  normalized_name varchar(255) not null,
  path_hash varchar(64) not null,
  owner_id uuid not null,
  current_version_id uuid null,
  is_deleted boolean not null default false,
  deleted_at timestamptz null,
  deleted_by uuid null,
  created_by uuid not null,
  updated_by uuid not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create unique index uq_nodes_sibling_name_alive
  on nodes(tenant_id, space_id, parent_id, lower(normalized_name))
  where is_deleted = false and parent_id is not null;

create unique index uq_nodes_space_root_alive
  on nodes(tenant_id, space_id)
  where is_deleted = false and parent_id is null;

create index idx_nodes_parent
  on nodes(tenant_id, space_id, parent_id, is_deleted);

create index idx_nodes_space_updated
  on nodes(tenant_id, space_id, updated_at desc);

create index idx_nodes_trash_cleanup
  on nodes(tenant_id, is_deleted, deleted_at, id);
```

说明：

1. `normalized_name` 用于同目录重名判断，需要统一大小写、Unicode normalization 和空白处理策略。
2. `path_hash` 用于快速判断路径变化和搜索索引同步，不作为唯一业务主键。
3. PostgreSQL 中 `NULL` 不参与普通唯一约束冲突，根目录 `parent_id` 为空时必须用单独的 partial unique index 保证每个 space 只有一个根目录。

### 5.2 file_blobs 内容对象

```sql
create table file_blobs (
  id uuid primary key,
  tenant_id uuid not null,
  content_hash varchar(128) not null,
  hash_algo varchar(16) not null default 'sha256',
  size_bytes bigint not null check (size_bytes >= 0),
  storage_bucket varchar(128) not null,
  storage_key text not null,
  ref_count bigint not null default 0,
  status varchar(32) not null default 'active',
  encryption_mode varchar(32) not null default 'sse',
  scan_status varchar(32) not null default 'pending',
  created_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create unique index uq_file_blobs_hash
  on file_blobs(tenant_id, hash_algo, content_hash, size_bytes);

create index idx_file_blobs_cleanup
  on file_blobs(tenant_id, status, ref_count, created_at);
```

说明：

1. `content_hash + size_bytes` 用于秒传和去重。
2. `ref_count` 不是权限依据，只用于生命周期清理和成本治理。
3. `status=active` 表示可复用内容对象，`status=deleting` 表示对象生命周期任务已声明清理，上传链路必须返回重试错误而不是复用或新建同 hash blob。
4. 对象存储 key 不使用用户文件名，避免路径泄露。

### 5.3 file_versions 文件版本

```sql
create table file_versions (
  id uuid primary key,
  tenant_id uuid not null,
  node_id uuid not null references nodes(id),
  blob_id uuid not null references file_blobs(id),
  version_no integer not null,
  size_bytes bigint not null,
  mime_type varchar(255) not null,
  extension varchar(32) null,
  preview_status varchar(32) not null default 'pending',
  search_status varchar(32) not null default 'pending',
  created_by uuid not null,
  created_at timestamptz not null default now(),
  comment text null,
  metadata jsonb not null default '{}'::jsonb
);

create unique index uq_file_versions_node_version
  on file_versions(tenant_id, node_id, version_no);

create index idx_file_versions_node_created
  on file_versions(tenant_id, node_id, created_at desc);
```

`BE-036` 已实现文件版本运行时契约：列表按 `created_at DESC, id DESC` 使用服务端签名 cursor，并返回当前版本标记；指定版本下载只允许访问同一租户、同一节点的 active blob，返回 DTP/1 预签名地址；回滚在锁定 `node` 后校验可选当前版本前置条件，复用目标历史版本的 blob 创建递增新版本，增加引用计数、执行多维配额扣减、更新 `current_version_id`，并写入版本审计、搜索索引/抽取和预览事件。历史记录和原版本 ID 始终保持不可变。

### 5.4 upload_sessions 上传会话

```sql
create table upload_sessions (
  id uuid primary key,
  tenant_id uuid not null,
  space_id uuid not null,
  parent_id uuid not null references nodes(id),
  uploader_id uuid not null,
  file_name varchar(255) not null,
  normalized_name varchar(255) not null,
  size_bytes bigint not null,
  content_hash varchar(128) null,
  hash_algo varchar(16) null,
  mime_type varchar(255) null,
  part_size bigint not null,
  total_parts integer not null,
  object_key text not null,
  provider_upload_id text null,
  status varchar(32) not null,
  expires_at timestamptz not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create index idx_upload_sessions_user_status
  on upload_sessions(tenant_id, uploader_id, status, created_at desc);
```

状态枚举：`initiated`、`uploading`、`completing`、`completed`、`aborted`、`expired`、`failed`。

### 5.5 shares 分享

```sql
create table shares (
  id uuid primary key,
  tenant_id uuid not null,
  share_type varchar(16) not null check (share_type in ('internal', 'external')),
  token_hash varchar(128) null,
  passcode_hash varchar(255) null,
  created_by uuid not null,
  root_node_id uuid not null references nodes(id),
  permission varchar(32) not null,
  expires_at timestamptz null,
  max_views integer null,
  max_downloads integer null,
  view_count integer not null default 0,
  download_count integer not null default 0,
  status varchar(32) not null default 'active',
  created_at timestamptz not null default now(),
  revoked_at timestamptz null,
  revoked_by uuid null,
  metadata jsonb not null default '{}'::jsonb
);

create index idx_shares_creator
  on shares(tenant_id, created_by, created_at desc);

create unique index idx_shares_external_token_hash
  on shares(token_hash)
  where share_type = 'external';
```

外链 token 只能保存 hash，不能明文保存。分享 URL 中的 token 要使用 256-bit 随机值，不能用自增 ID 或 UUID 裸露业务关系。

当前实现已落地 `shares`、`share_items`、`share_recipients` 和 `share_access_logs` 迁移与 SQLAlchemy 模型。服务层支持内部分享和外链分享创建、提取码哈希、过期时间、访问/下载次数上限、撤销状态和审计/outbox 写入；外链原始 token 只在创建响应中返回一次，数据库只保存全局唯一 `token_hash`，提取码只保存 Argon2id hash。创建分享会校验 root 节点和全部分享项的节点级 `share` 权限，并限制分享项与 root 节点属于同一空间。当前已开放 `POST /api/v1/shares`、`GET /api/v1/shares/{share_id}`、`POST /api/v1/shares/{share_id}/revoke`、`POST /api/v1/public/shares/access` 和 `POST /api/v1/public/shares/download`，并接入 Cookie Session、CSRF、创建者边界、`tenant_slug + raw_token` 外链契约、提取码校验、状态/过期判断、访问次数和下载次数原子扣减、IP 总量和 `token + IP` / `token + node + IP` 维度限流、`share_access_logs`、`share.external.accessed` 和 `share.external.downloaded` 审计。外链下载会独立校验分享权限、分享项范围、文件当前版本、blob 状态和文件安全策略；支持普通预签名或图片/PDF 水印派生对象，并使用派生文件的真实文件名、MIME 和大小记录响应、访问日志与审计。高密级外链的后端代理流继续后续治理。

### 5.6 audit_logs 审计日志

```sql
create table audit_logs (
  id uuid primary key,
  tenant_id uuid not null,
  actor_id uuid null,
  actor_type varchar(32) not null,
  action varchar(64) not null,
  resource_type varchar(64) not null,
  resource_id uuid null,
  space_id uuid null,
  result varchar(16) not null,
  risk_level varchar(16) not null default 'low',
  ip inet null,
  user_agent text null,
  request_id varchar(64) not null,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
) partition by range (created_at);

create index idx_audit_logs_actor_time
  on audit_logs(tenant_id, actor_id, created_at desc);

create index idx_audit_logs_resource_time
  on audit_logs(tenant_id, resource_type, resource_id, created_at desc);
```

审计日志建议按月分区。关键审计写入和业务事务必须强关联；投递到外部日志平台可异步，但本地审计索引不能丢。

### 5.7 组织与空间表补充

```sql
create table tenants (
  id uuid primary key,
  name varchar(255) not null,
  status varchar(32) not null default 'active',
  permission_version bigint not null default 1,
  policy_version bigint not null default 1,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table users (
  id uuid primary key,
  tenant_id uuid not null references tenants(id),
  username varchar(128) not null,
  email varchar(255) null,
  display_name varchar(255) not null,
  password_hash varchar(255) null,
  status varchar(32) not null default 'active',
  department_id uuid null,
  last_login_at timestamptz null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_users_username on users(tenant_id, lower(username));
create unique index uq_users_email on users(tenant_id, lower(email)) where email is not null;

create table departments (
  id uuid primary key,
  tenant_id uuid not null references tenants(id),
  parent_id uuid null references departments(id),
  name varchar(255) not null,
  path text not null,
  sort_order integer not null default 0,
  status varchar(32) not null default 'active',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index idx_departments_parent on departments(tenant_id, parent_id, sort_order);

create table spaces (
  id uuid primary key,
  tenant_id uuid not null references tenants(id),
  space_type varchar(32) not null,
  name varchar(255) not null,
  owner_id uuid null references users(id),
  department_id uuid null references departments(id),
  root_node_id uuid null,
  status varchar(32) not null default 'active',
  permission_version bigint not null default 1,
  quota_account_id uuid null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  metadata jsonb not null default '{}'::jsonb
);

create index idx_spaces_type_owner on spaces(tenant_id, space_type, owner_id);
```

### 5.8 权限表补充

```sql
create table space_members (
  id uuid primary key,
  tenant_id uuid not null,
  space_id uuid not null references spaces(id),
  user_id uuid not null references users(id),
  role varchar(32) not null check (role in ('owner', 'admin', 'editor', 'viewer')),
  created_by uuid,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_space_members_user
  on space_members(tenant_id, space_id, user_id);

create index idx_space_members_user
  on space_members(tenant_id, user_id, role);

create index idx_space_members_space_role
  on space_members(tenant_id, space_id, role);

create table acl_entries (
  id uuid primary key,
  tenant_id uuid not null,
  node_id uuid not null references nodes(id),
  subject_type varchar(32) not null,
  subject_id uuid not null,
  effect varchar(16) not null check (effect in ('allow', 'deny')),
  actions jsonb not null,
  inherit boolean not null default true,
  created_by uuid not null,
  created_at timestamptz not null default now()
);

create index idx_acl_node_subject
  on acl_entries(tenant_id, node_id, subject_type, subject_id);

create table role_permissions (
  id uuid primary key,
  tenant_id uuid not null,
  role varchar(64) not null,
  action varchar(64) not null,
  scope varchar(64) not null,
  created_at timestamptz not null default now()
);

create unique index uq_role_permissions
  on role_permissions(tenant_id, role, action, scope);
```

Sprint 4 已落地用户维度 `space_members`，创建空间时自动写入创建者的 `owner` 成员关系，并用 `PermissionService` 按空间角色检查空间列表、文件树、上传和下载等空间级动作；空间成员管理 API 已支持 owner/admin 添加、查看、调整和移除成员，变更时递增 `spaces.permission_version`，保护最后一个 owner，并写入授权审计。节点 ACL 已落地到 `acl_entries`，支持 `user`、`department`、`group` 三类主体、allow/deny、继承开关和 deny 优先；权限判断会通过 Org 模块的 `departments`、`department_members`、`user_groups`、`user_group_members` 事实表展开当前用户所属活跃部门和用户组，文件列表、文件夹创建、上传初始化、multipart complete 和下载入口都会做节点级权限判断。文件列表已通过 `batch_check_nodes` 为当前页子节点批量返回常用动作权限，高危动作仍由对应接口单独检查。系统管理员还可通过 `/api/v1/admin/users`、`/api/v1/admin/departments`、`/api/v1/admin/groups` 完成三类实体的 cursor 列表、详情、创建、更新、停用及部门/用户组成员管理；所有入口按租户隔离，写操作使用实体 `version` 前置条件并写入审计，组织成员或状态变化递增租户权限版本并触发租户级缓存失效。外部主体会在 share 模块接入后扩展为额外 subject 映射层，避免在尚无主体模型时提前堆复杂泛化。

ACL 查询性能注意点：目录继承权限会涉及祖先节点。可选实现有三种：

1. 直接递归 CTE 查祖先：实现简单，适合一期目录深度可控场景。
2. closure table：查询快，移动目录时维护成本高。
3. materialized path：查询中等，移动目录需批量更新路径。

一期建议使用递归 CTE + 限制最大目录深度 32。若后续目录量巨大，再引入 closure table。

### 5.9 上传分片、幂等与容量表补充

```sql
create table upload_parts (
  id uuid primary key,
  tenant_id uuid not null,
  upload_session_id uuid not null references upload_sessions(id),
  part_no integer not null,
  size_bytes bigint not null,
  etag text not null,
  checksum varchar(128) null,
  created_at timestamptz not null default now()
);

create unique index uq_upload_parts_session_part
  on upload_parts(tenant_id, upload_session_id, part_no);

create table idempotency_keys (
  id uuid primary key,
  tenant_id uuid not null,
  user_id uuid not null,
  key varchar(128) not null,
  request_hash varchar(128) not null,
  response_code integer null,
  response_body jsonb null,
  status varchar(32) not null default 'processing',
  expires_at timestamptz not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_idempotency_user_key
  on idempotency_keys(tenant_id, user_id, key);

create table quota_accounts (
  id uuid primary key,
  tenant_id uuid not null,
  owner_type varchar(32) not null,
  owner_id uuid not null,
  limit_bytes bigint not null,
  used_bytes bigint not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index uq_quota_accounts_owner
  on quota_accounts(tenant_id, owner_type, owner_id);

create table quota_policies (
  id uuid primary key,
  tenant_id uuid not null,
  name varchar(128) not null,
  priority integer not null default 100,
  limit_bytes bigint not null,
  max_file_size_bytes bigint null,
  extensions jsonb not null,
  mime_prefixes jsonb not null,
  is_active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

### 5.10 索引、分区和迁移规则

1. `audit_logs` 按月分区，生产必须提前创建未来 3 到 6 个月分区。
2. `nodes` 必须有同目录唯一索引，保证并发创建同名文件时由数据库兜底。
3. 所有多租户表查询必须带 `tenant_id` 条件，禁止跨租户扫描。
4. 大表 migration 禁止一次性加非空列并填充默认值，应分三步：加 nullable 列、后台回填、加 not null 约束。
5. 删除字段必须至少跨两个版本：先停止写入和读取，再删除字段。
6. JSONB 字段只能放低频扩展属性，高频筛选字段必须列化并建索引。

### 5.11 身份与账号安全表

Sprint 11 migration `20260804_0024` 已把账号安全和企业身份加入正式模型：

- `users` 增加 `local_password_enabled`、`failed_login_attempts`、`last_failed_login_at`、`locked_until`、`lock_reason`、`password_changed_at`。
- `auth_sessions` 增加 IP、User-Agent、`auth_method`、`oidc_provider_id`、`last_seen_at` 和用户活跃会话索引。
- `oidc_providers` 保存租户 provider 配置、scope、secret 引用、启用状态和乐观版本。
- `oidc_flows` 保存一次性登录/绑定 flow 的 state/nonce 哈希、PKCE verifier、回调路径、到期和消费时间。
- `oidc_identity_links` 使用 `tenant + issuer + subject` 稳定绑定本地用户，不按 email 自动接管账号。
- `ldap_sources` 保存目录范围、filter、属性映射、secret 引用、cursor 和配置版本。
- `ldap_sync_runs`、`ldap_sync_conflicts` 保存异步 run、模式、统计、错误和冲突。
- `ldap_object_bindings` 保存 Source、对象类型、external ID 与本地对象 ID 的稳定映射。
- `ldap_membership_relations`、`ldap_membership_claims` 区分核心成员边、管理员手工所有权和各 LDAP Source 声明。

身份凭据只保存 `env:VARIABLE_NAME` 引用，响应只返回是否已配置。LDAP external ID 建立后不得因 DN、用户名、邮箱、部门路径或组名变化而重建映射；冲突必须进入隔离记录，不猜测绑定。

## 6. 对象存储设计

### 6.1 Key 规则

```text
objects/{tenant_id}/{hash_prefix}/{content_hash}
uploads/{tenant_id}/{upload_session_id}/{part_no}
previews/{tenant_id}/{file_id}/{version_id}/preview.pdf
thumbnails/{tenant_id}/{file_id}/{version_id}/{size}.webp
exports/{tenant_id}/{export_id}/audit.csv
```

原则：

1. 原始文件对象 key 与文件名、目录名无关。
2. 预览文件和缩略图可以按 `file_id/version_id` 组织，因为它们是派生物。
3. 临时上传对象必须设置生命周期，避免失败上传长期占用容量。
4. 对象存储 bucket 必须私有，禁止公共读。

### 6.2 StorageAdapter 协议

```python
from typing import Protocol

class StorageAdapter(Protocol):
    async def create_multipart_upload(self, key: str, content_type: str | None) -> str: ...

    async def presign_upload_part(
        self,
        key: str,
        upload_id: str,
        part_no: int,
        expires_seconds: int,
    ) -> str: ...

    async def complete_multipart_upload(
        self,
        key: str,
        upload_id: str,
        parts: list[dict[str, str | int]],
    ) -> None: ...

    async def abort_multipart_upload(self, key: str, upload_id: str) -> None: ...

    async def presign_download(
        self,
        key: str,
        filename: str,
        content_type: str | None,
        expires_seconds: int,
    ) -> str: ...

    async def head_object(self, key: str) -> dict[str, str | int]: ...
```

正式对象存储服务端使用固定 digest 的 SeaweedFS `4.40`，后端继续使用 MinIO Python
SDK 作为通用 S3 客户端。业务代码只能依赖 `StorageAdapter` 协议，不直接 import 或
绑定具体服务端实现；如果 SDK 对 multipart 能力缺少公开 API，必要的客户端细节只能
封装在 `infrastructure` 适配层，并在审计或进度文档中记录风险和替换触发条件。

## 7. 上传链路实现

### 7.1 接口设计

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/v1/uploads/init` | 初始化上传，检查权限、容量、秒传 |
| GET | `/api/v1/uploads/{session_id}` | 查询上传状态和已上传分片 |
| POST | `/api/v1/uploads/{session_id}/parts/{part_no}/presign` | 获取分片上传 URL |
| POST | `/api/v1/uploads/{session_id}/complete` | 完成上传，创建文件或新版本 |
| POST | `/api/v1/uploads/{session_id}/abort` | 取消上传并清理临时对象 |

当前实现已落地 `init`、状态查询、分片 `presign`、`complete`、`abort`、文件下载预签名 URL、空间容量账本初版、multipart complete 后服务端 `sha256` 校验、最终对象 key 规整、过期上传清理任务、上传/下载基础限流、彻底删除容量释放、回收站保留期自动清理和空间容量校准维护任务。该链路已经满足一期主路径验证，但企业级上线前仍需按 7.9 补齐策略化治理、失败告警、安全下载和更完整的真实依赖集成矩阵。

初始化请求示例：

```json
{
  "space_id": "uuid",
  "parent_id": "uuid",
  "file_name": "产品方案.docx",
  "size_bytes": 104857600,
  "content_hash": "64位sha256十六进制摘要",
  "hash_algo": "sha256",
  "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
  "conflict_policy": "fail"
}
```

初始化响应示例：

```json
{
  "session_id": "uuid",
  "mode": "multipart",
  "part_size_bytes": 8388608,
  "total_parts": 13,
  "expires_at": "2026-06-30T10:00:00Z"
}
```

秒传响应示例：

```json
{
  "mode": "instant",
  "node_id": "uuid",
  "version_id": "uuid",
  "blob_id": "uuid"
}
```

### 7.2 初始化算法

```text
init_upload(user, parent_id, file_name, size, hash):
  1. validate file_name, extension, size, tenant policy
  2. load parent node with lock-free read
  3. assert parent is folder and not deleted
  4. check permission: upload on parent
  5. check quota: user/space/tenant remaining >= size
  6. if active content_hash exists in file_blobs:
       create node/version in db transaction
       increase blob ref_count
       write quota ledger
       write audit log
       enqueue preview/search if needed
       return instant result
  7. if same content_hash blob is deleting:
       return BLOB_DELETING for client retry
  8. create multipart upload in object storage
  9. create upload_session in db with provider_upload_id
  10. return session metadata
```

对象存储 multipart upload 不应放在数据库事务内部长时间执行。当前实现先创建 provider upload，再在短事务中写入 upload session；如果数据库写入失败，调用 abort 清理 provider upload。后续如需保留失败 session 可扩展为先写 DB session，再创建 provider upload 并更新 `provider_upload_id`。

### 7.3 完成上传算法

```text
complete_upload(user, session_id, parts):
  1. load upload_session for update
  2. assert uploader_id == user.id and status in initiated/uploading
  3. set status = completing
  4. verify parts count, part_no order, etag present
  5. call storage.complete_multipart_upload
  6. head object and verify size
  7. if client provided hash, enqueue or perform hash verification
  8. in db transaction:
       insert or reuse file_blob
       resolve name conflict
       create node if new file, or create new version if overwrite
       update node.current_version_id
       increase blob ref_count
       write quota ledger
       mark upload_session completed
       write audit log
       insert outbox events
  9. enqueue preview/search/scan tasks from outbox
```

重要细节：

1. S3 multipart ETag 不是文件 MD5，不能把它当内容完整性哈希。
2. 由客户端提供 SHA-256 时，服务端必须在 multipart complete 后重新计算对象内容 hash；当前实现只支持 `sha256`，不支持的算法返回 `UPLOAD_HASH_ALGO_UNSUPPORTED`。
3. 当前实现已完成合并、大小校验、服务端 hash 校验、最终对象 key 归档、分片记录、文件节点/版本写入、完成审计和多维容量账本写入；新 blob 会写入 `objects/{tenant_id}/{hash_prefix}/{content_hash}`，若 complete 前已有同租户同 hash、同大小 blob 则复用既有对象并清理本次临时对象。hash 不匹配返回 `UPLOAD_HASH_MISMATCH`，上传会话标记为 `failed` 并写 `upload.failed` 审计，不创建文件版本和容量流水。空间账户始终扣减，用户/租户默认额度大于 `0` 时启用对应维度，匹配的文件策略账户同时扣减；过期会话由 `upload.expire_sessions` 任务标记为 `expired` 并清理临时对象；彻底删除回收站节点会按版本流水释放全部关联维度；`quota.reconcile_space_usage` 任务仍按批次和 cursor 报告并修复空间账户漂移。
4. 容量扣减以最终创建版本为准，不以分片临时对象为准；临时容量可单独限制。
5. `complete` 接口必须幂等。如果 session 已 completed，返回已有 file_id/version_id。

### 7.4 推荐分片大小

| 文件大小 | 分片大小 | 客户端并发 |
| --- | --- | --- |
| < 100 MB | 8 MB | 2 到 3 |
| 100 MB 到 1 GB | 16 MB | 3 到 4 |
| 1 GB 到 20 GB | 32 MB | 4 到 6 |
| > 20 GB | 64 MB | 需企业策略允许 |

### 7.5 上传状态机细化

```text
initiated
  -> uploading        客户端获取分片签名或上报分片
  -> completing       客户端请求 complete
  -> completed        对象存储完成合并且元数据写入成功
  -> aborted          用户取消或管理员清理
  -> expired          超过 expires_at 且未完成
  -> failed           对象存储或元数据写入不可恢复失败
```

状态流转约束：

1. `completed`、`aborted`、`expired` 是终态，不能再上传分片。
2. `completing` 中再次调用 complete，应返回当前处理状态或已有结果，不能重复创建版本。
3. `expired` session 的临时对象由 `upload.expire_sessions` 清理任务异步 abort 和删除；任务只删除 `uploads/...` 临时 key，避免误删最终 `objects/...` 内容。
4. 客户端恢复上传时，只允许恢复 `initiated` 或 `uploading` 状态。

### 7.6 断点续传细节

查询上传状态响应：

```json
{
  "session_id": "uuid",
  "status": "uploading",
  "part_size": 8388608,
  "total_parts": 128,
  "uploaded_parts": [
    {"part_no": 1, "size_bytes": 8388608, "etag": "..."},
    {"part_no": 2, "size_bytes": 8388608, "etag": "..."}
  ],
  "expires_at": "2026-06-30T10:00:00Z"
}
```

前端恢复逻辑：

1. 根据 session_id 查询已上传分片。
2. 对比本地文件大小、文件名、hash，确认仍是同一文件。
3. 跳过已上传分片，只请求缺失分片的 presign URL。
4. 所有分片完成后调用 complete。

### 7.7 幂等 complete 设计

`complete_upload` 需要处理网络重试。推荐在 `upload_sessions.metadata` 中记录完成结果：

```json
{
  "completed_result": {
    "node_id": "uuid",
    "version_id": "uuid",
    "blob_id": "uuid"
  }
}
```

如果 session 已经是 `completed`，直接返回该结果。如果是 `completing`，可以返回 409 + `UPLOAD_COMPLETING`，前端稍后轮询；也可以阻塞短时间等待，建议一期采用轮询，避免接口长时间占用连接。

### 7.8 上传安全检查

1. 初始化时检查扩展名黑名单，例如 exe、bat、cmd、ps1 可按企业策略禁用。
2. 完成后以服务端嗅探 MIME 为准，客户端 MIME 只作参考。
3. 文件 hash 不匹配时标记上传失败并写审计，不创建文件版本。
4. 上传临时对象定期清理，避免恶意用户创建大量未完成上传占用存储。
5. 同一用户并发上传会话数需要限制，例如默认 20 个。

### 7.9 企业级补强计划

当前上传/下载链路、秒传、multipart、服务端 hash 校验、容量账本和彻底删除容量释放已完成阶段闭环。以下记录已落地的补强结果，以及继续保留在后续治理中的边界：

1. multipart create/complete/abort 已在 `infrastructure` 层改为 MinIO Python SDK 公共
   `get_presigned_url` + 标准 S3 HTTP POST/DELETE/XML 控制请求，不再调用
   `_create_multipart_upload`、`_complete_multipart_upload`、
   `_abort_multipart_upload`。客户端依赖范围锁定为 `minio>=7.2.20,<8`，控制请求
   超时和 URL 有效期可配置，升级必须通过真实 S3 兼容门禁。
2. 对象复制到 `objects/{tenant_id}/{hash_prefix}/{content_hash}` 成功，但数据库最终化失败时，可能留下没有 `file_blobs` 元数据引用的孤儿最终对象。`file.cleanup_orphaned_objects` 已完成对象存储反向扫描任务，只扫描受控 `objects/{tenant_id}/{hash_prefix}/{sha256}` key 形态，跳过 `uploads/`、`previews/` 等前缀，默认 dry-run，显式确认后才删除，并通过审计和指标作为安全护栏；`BE-031`/`BE-032` 已在 CI 和本机真实 S3 中覆盖 list/delete 主路径，`BE-035` 已补连续失败/stale 指标和 Prometheus 规则。Alertmanager 企业 webhook 路由与 Grafana overview/maintenance 看板已纳入可选 `monitoring` profile，实际接收端和值班流程由生产环境配置。
3. `BE-033` 已补齐空间、用户、租户和文件策略账户的统一扣减/释放；策略支持扩展名、MIME 前缀、累计额度和单文件上限。系统管理员已可分页查询账户、创建或更新空间/租户/用户额度，并列表、创建、更新和停用策略；更新使用乐观前置条件且额度不得低于已用容量。部门额度、临时上传占用上限、服务端内容分类和多维通用校准继续后续治理。
4. `BE-035` 已建立维护任务的 Redis 连续失败状态、结构化阈值告警、stale/时间戳 Gauge、通用结果计数和 Prometheus 规则文件；`share.expire_shares` 与 `preview.cleanup_artifacts` 已接入同一调度和健康状态。可选 `monitoring` profile 已补齐 Prometheus、Alertmanager webhook 路由和 Grafana overview/maintenance 看板，生产部署只需注入真实接收端并执行告警链路验收。
5. 下载默认仍可使用短期预签名直连对象存储；内部高密级场景已支持后端代理、HTTP Range、增强审计、图片/PDF 动态水印和关键字 DLP。租户文件安全策略可自动选择 `presigned/proxy/watermark/blocked`，并支持 DLP audit/block 与 fail-closed。公开外链支持普通预签名或水印派生对象；搜索正文已支持图片/扫描 PDF OCR。外链代理流、历史版本专用水印、legal hold 和高级内容分类继续后续治理。
6. 同 hash 首次上传竞争已使用数据库 savepoint 解析唯一约束竞争；真实 PostgreSQL 双会话测试确认只创建一个 blob，两个文件引用最终形成 `ref_count=2`。complete 响应丢失时通过对象 stat 恢复结果，失败会清理 multipart 和临时对象，复制后数据库失败形成的最终对象孤儿继续由反向扫描兜底。
7. `BE-032` 对象存储门禁已在 CI 和本机 Docker 中覆盖标准 S3 HTTP multipart、预签名 PUT/GET、copy、delete、list、hash 校验和孤儿最终对象扫描；单元门禁覆盖幂等 abort、异常 complete 恢复和失败存储清理。
8. `BE-029` 已完成 10,000 节点、100 万 OpenSearch 文档、1,000 万审计日志和最终 target `upload_complete` 门禁；计入 1,936 个 complete、0 失败，吞吐 `58.364 RPS`，不含 storage merge 的 API P95 `790 ms`，清理 2,000/2,000 个准备节点。

## 8. 下载链路实现

### 8.1 下载模式

| 模式 | 使用场景 | 优点 | 缺点 |
| --- | --- | --- | --- |
| 预签名 URL | 普通下载、大文件下载 | API 不承载文件流量 | URL 泄露窗口内可访问 |
| 后端代理 | 高密级、水印、强审计 | 控制最强 | 消耗 API 带宽和连接 |
| 内部网关代理 | 企业内网统一出口 | 兼顾控制和性能 | 部署复杂度更高 |

一期默认使用预签名 URL，内部高密级或强审计场景可切换后端代理。当前实现提供 `GET /api/v1/files/{node_id}/download` 生成短期私有对象地址，提供 `GET /api/v1/files/{node_id}/content` 重新校验权限后按 offset/length 流式代理当前版本，支持完整响应与单段 HTTP Range，也提供 `GET /api/v1/files/{node_id}/watermarked-content` 为配置上限内的图片/PDF 生成动态水印。`file_security_policies` 可按扩展名/MIME 自动要求预签名、代理、水印或阻断，并执行关键字 DLP audit/block 与 fail-closed；历史版本下载同样执行安全策略。公开外链下载支持 `delivery_mode=presigned|watermark`，搜索正文已支持图片/扫描 PDF OCR。外链代理流、历史版本专用水印、legal hold 和高级内容分类继续后续治理。

### 8.2 下载流程

```text
request_download(user, file_id):
  1. load node + current_version + blob
  2. assert node is file and not deleted
  3. check permission: download
  4. check policy: device/ip/time/classification/share limits
  5. create audit log with result=allowed
  6. generate short-lived presigned URL, 60-300 seconds
  7. return url, filename, size, mime_type
```

如果权限拒绝，也要写审计日志，但注意不要在错误信息里泄露文件是否存在。对用户统一返回“文件不存在或无权访问”。

当前实现中，下载成功和拒绝均写入 `file.downloaded` 审计事件与 outbox event；下载通过 `PermissionService` 同时校验空间角色和节点 ACL 的 `download` 权限，目录继承与 deny 优先策略已纳入判断。

### 8.3 Range 下载与文件名处理

大文件下载应支持 HTTP Range。使用预签名 URL 时通常由对象存储支持 Range；使用后端代理时需要透传 `Range` 头并正确返回 206。

文件名响应要求：

1. `Content-Disposition` 同时设置 `filename` 和 `filename*`，兼容中文文件名。
2. 文件名来自元数据 `node.name`，不能来自对象存储 key。
3. 外链下载可按策略追加水印后的导出文件名，例如 `方案_水印版.pdf`。

### 8.4 文件操作算法

#### 创建文件夹

```text
create_folder(user, parent_id, name):
  1. validate name and normalize
  2. load parent and check upload permission
  3. insert node(node_type='folder')
  4. if unique conflict, apply conflict policy or return FILE_CONFLICT
  5. write audit and outbox file.folder_created
```

#### 移动节点

```text
move_node(user, node_id, target_parent_id, new_name):
  1. load source node and target parent
  2. assert source not root and not deleted
  3. check update permission on source
  4. check upload permission on target parent
  5. if source is folder, ensure target is not descendant of source
  6. resolve name conflict in target directory
  7. update parent_id/name/normalized_name/updated_at
  8. increment node permission/path version if used
  9. write audit and outbox file.moved
```

#### 删除到回收站

```text
delete_node(user, node_id):
  1. load node
  2. check delete permission
  3. mark node and descendants is_deleted=true if folder
  4. record deleted_at/deleted_by
  5. remove from search index by event
  6. write audit
```

文件夹递归删除不在接口请求中一次性更新海量子节点。当前实现按 `DRIVE_FILE_TREE_ASYNC_THRESHOLD` 判断：超过阈值时先标记根节点并创建 `file_tree_operations`，后代使用 `deleted_root_id`、递归 CTE 和固定批次由 `file.process_tree_operations` 分段提交；恢复和彻底删除使用同一状态模型。状态可查询，失败可重试，Worker 中断后从 PostgreSQL 中剩余节点继续。

#### 恢复节点

恢复时必须检查原父目录是否存在且未删除。如果原父目录不存在，允许恢复到用户选择的新目录。恢复时还要处理同名冲突，不能覆盖现有文件，除非用户显式选择覆盖为新版本。

#### 彻底删除节点

彻底删除只允许作用于已在回收站的节点。当前实现会删除目标节点下全部已删除后代的节点元数据和文件版本，扣减相关 blob 引用计数，按版本大小合计释放空间容量，写入 `reason=file_purged`、`ref_type=node` 的负向容量流水，并记录 `file.purged` 审计。删除到回收站不释放容量；`file.cleanup_expired_trash` 默认按 30 天保留期扫描删除批次根节点，排除同一 `deleted_at/deleted_by` 批次中的子节点，锁定根节点和子树后执行同样的彻底删除、容量、blob、搜索和系统审计语义。对象存储最终对象不在该事务内同步删除，由 `file.cleanup_unreferenced_blobs` 按 DB 引用状态异步清理。

## 9. 权限系统实现

### 9.1 权限来源

| 来源 | 示例 | 优先级 |
| --- | --- | --- |
| 系统角色 | 超级管理员、审计员 | 最高，但仍受安全策略限制 |
| 空间角色 | space_admin、editor、viewer | 高 |
| 目录 ACL | 指定用户/部门/组可读写 | 中 |
| 继承权限 | 父目录权限向子目录继承 | 中 |
| 文件策略 | 密级、标签、外链策略 | 高，可拒绝 |
| 上下文策略 | IP、设备、时间、地域 | 高，可拒绝 |

权限冲突原则：显式 deny 优先于 allow；文件密级和安全策略优先于普通角色授权；超级管理员操作也必须审计。

### 9.2 权限动作

```text
list      查看目录中文件名
preview   在线预览
read_meta 读取元数据
download  下载原文件
upload    上传新文件
update    重命名、移动、覆盖上传
delete    删除到回收站
restore   从回收站恢复
share     创建分享
grant     授权给其他人
manage    管理空间配置和成员
```

### 9.3 权限判断伪代码

```python
def can_access(ctx: PermissionContext, node: Node, action: str) -> Decision:
    if node.is_deleted and action not in {"restore", "manage"}:
        return deny("node_deleted")

    system_decision = evaluate_system_role(ctx, action)
    policy_decision = evaluate_abac_policy(ctx, node, action)
    if policy_decision.is_deny:
        return policy_decision

    acl_decision = evaluate_node_acl(ctx.subjects, node.ancestor_ids, action)
    if acl_decision.is_deny:
        return acl_decision
    if acl_decision.is_allow:
        return allow("acl")

    space_decision = evaluate_space_role(ctx.subjects, node.space_id, action)
    if space_decision.is_allow:
        return allow("space_role")

    if system_decision.is_allow:
        return allow("system_role")

    return deny("no_matching_grant")
```

### 9.4 权限缓存

缓存 key 建议：

```text
perm:v1:{tenant_id}:{user_id}:{node_id}:{action}:{perm_version}:{policy_version}
```

版本号来源：

1. `tenant.permission_version`：任意角色、部门、组、空间成员变更后递增。
2. `space.permission_version`：空间成员或空间策略变更后递增。
3. `node.permission_version`：目录 ACL 变更后递增。
4. `policy_version`：安全策略、密级策略、外链策略变更后递增。

缓存策略：

1. 普通 list/preview 可缓存 30 到 120 秒。
2. download/delete/grant/manage 等高危动作要短缓存或二次查库。
3. 权限变更必须发布失效事件，Redis 缓存只是加速，不是事实来源。
4. 搜索结果权限过滤不得只依赖旧缓存。

### 9.5 权限数据加载策略

一次权限判断需要的数据：

1. 用户主体集合：`user:{id}`、`department:{id}`、上级部门、`group:{id}`、空间角色。
2. 节点上下文：node、space、ancestor ids、file classification、deleted 状态。
3. 授权规则：space_members、acl_entries、role_permissions、policy rules。
4. 环境上下文：IP、设备、时间、认证强度、是否外链访问。

性能策略：

1. 用户主体集合登录后可缓存，但部门和用户组变更要失效。
2. 祖先节点 ids 可在列表接口批量加载，避免每个文件递归查一次。
3. 文件列表页返回每个节点的常用权限时，应批量评估，不能 N+1 查询。
4. 高危动作仍单独调用权限引擎确认。

### 9.6 批量权限评估接口

内部 service 建议提供：

```python
class PermissionService:
    async def check(
        self,
        ctx: PermissionContext,
        node_id: UUID,
        action: PermissionAction,
    ) -> Decision: ...

    async def batch_check(
        self,
        ctx: PermissionContext,
        node_ids: list[UUID],
        actions: list[PermissionAction],
    ) -> dict[UUID, dict[PermissionAction, Decision]]: ...
```

文件列表接口使用 `batch_check`，下载、删除、分享、授权使用 `check`。

### 9.7 ACL 继承规则

1. 子目录默认继承父目录 ACL。
2. 子目录可以追加 allow 或 deny。
3. deny 优先于 allow。
4. 可以支持“停止继承”字段，但一期建议不开放给普通用户，只保留管理员能力。
5. 文件继承所在目录权限，文件级 ACL 一期可支持但 UI 可暂不暴露。

### 9.8 权限变更后的副作用

权限变更不是只写一张表，还要处理：

1. 递增 `space.permission_version` 或 `node.permission_version`。
2. 删除 Redis 权限缓存。
3. 写审计日志。
4. 发出 `permission.changed` 事件。
5. 发出 `search.acl_rebuild_requested` 事件，搜索索引重建受影响目录的 `acl_tokens`。
6. 如果移除了用户的 share/manage 权限，可选地撤销其创建的高风险外链。

当前空间成员管理 API 已在成员新增、角色调整和移除时递增 `spaces.permission_version` 并写入 `permission.space_member.*` 审计；节点 ACL 管理 API 已在规则新增、调整和删除时递增 `nodes.permission_version` 并写入 `permission.node_acl.*` 审计。两类变更都会在同一业务事务中写入 `permission.changed` outbox event，payload 包含 `scope`、`resource_id`、`permission_version`、`reason`、`actor_id`、主体信息和变更摘要；用户主体变更会携带 `affected_user_id` 精确失效，部门/用户组主体变更当前保守失效租户内节点权限缓存。`permission.invalidate_cache` worker 已消费该事件并失效匹配的 Redis 权限缓存 key。当前已新增 ACL token builder、`search.acl_rebuild_requested` outbox event、`search.index_requested` 文件索引事件和 `search.dispatch_outbox` 队列入口；上传完成、重命名、移动、删除、恢复和彻底删除后的文件索引会从 PostgreSQL 重新加载事实后写入或删除 OpenSearch 文档，ACL 变更后会按 space 或 node 子树保守重建索引 token，`GET /api/v1/search` 查询会使用 allow/deny token 做查询层过滤并通过 `read_meta` 做应用层二次权限校验。

### 9.9 越权防护清单

1. 所有按 ID 查询文件的接口都返回“文件不存在或无权访问”，不要区分 404 和 403 泄露存在性。
2. 下载接口不能只校验分享权限，还要校验文件当前状态、分享状态和次数限制。
3. 搜索结果必须二次校验，尤其是权限刚变更但索引未更新时。
4. 管理后台接口必须校验管理员权限，不能只依赖前端路由。
5. 外链访问不能复用内部用户权限逻辑，必须使用 external subject。

## 10. 分享系统实现

### 10.1 分享类型

1. 内部分享：绑定企业用户、部门或用户组，需要登录。
2. 外链分享：通过随机 token 访问，可选提取码、有效期、访问次数、下载次数。
3. 上传收集：外部访客只能上传到指定目录，不能看到目录已有文件。

### 10.2 外链 token

```text
raw_token = base64url(random_32_bytes)
token_hash = sha256(raw_token)
```

数据库只保存 `token_hash`，并对外链 token hash 建全局唯一索引。用户访问时提交 `tenant_slug + raw_token`，后端先解析租户，再按 `tenant_id + token_hash` 查询。提取码使用 Argon2id 或 bcrypt 哈希，不保存明文。

当前服务层已按该策略生成 256-bit 级随机外链 token，并通过统一 token hash 存储；提取码复用项目 Argon2id 密码哈希能力。外链访问入口已接入 `tenant_slug + raw_token` 租户边界、提取码校验、状态/过期/访问次数判断、IP 总量和 `token + IP` 维度限流，以及 `external` actor 访问日志。外链下载入口已接入同一租户边界、提取码校验、状态/过期/下载次数判断、分享权限和分享项范围校验、IP 总量和 `token + node + IP` 维度限流，并以 `external` actor 写入下载访问日志和审计；当前支持普通短期预签名或图片/PDF 水印派生对象，并以实际派生大小计量。后端代理流继续后续高密级治理。

### 10.3 访问计数并发控制

外链访问次数和下载次数必须原子更新。推荐数据库条件更新：

```sql
update shares
set download_count = download_count + 1
where id = :share_id
  and status = 'active'
  and (expires_at is null or expires_at > now())
  and (max_downloads is null or download_count < max_downloads)
returning id;
```

如果返回 0 行，说明链接过期、撤销或次数耗尽。

## 11. 预览与转码

### 11.1 任务状态

`preview_status`：`pending`、`processing`、`ready`、`failed`、`unsupported`、`skipped`。

### 11.2 处理流程

```text
file_uploaded event
  -> create preview task
  -> worker downloads object to sandbox temp dir
  -> validate file size and mime type
  -> convert:
       office -> pdf by LibreOffice headless
       pdf -> first page image by Poppler
       image -> thumbnail webp
       video -> metadata + optional h264 preview
       text -> utf-8 normalized preview
  -> upload preview artifacts to object storage
  -> update file_versions.preview_status
  -> emit preview_ready event
```

### 11.3 安全限制

1. 转码必须在临时目录或容器沙箱内执行，不允许使用用户文件名拼 shell 命令。
2. LibreOffice、Poppler、FFmpeg 等外部命令必须设置超时、内存限制、文件大小限制和输出大小限制。
3. 压缩包预览必须防 zip bomb，限制展开文件数量、总大小和路径穿越。
4. 转码失败只影响预览，不影响原文件下载。
5. Worker 节点和 API 节点可分离，避免转码消耗影响核心接口。

当前实现已落地预览链路和基础生命周期：`file_versions` 保存预览状态，`preview_artifacts` 保存私有产物元数据与 `last_accessed_at`；上传成功后写入 `preview.render_requested`，`preview.dispatch_outbox` 在 `preview` 队列使用 Pillow、Poppler 和 LibreOffice 生成 `previews/{tenant_id}/{node_id}/{version_id}/image.webp`。非支持格式、工具缺失、源文件或输出超限会记录明确状态，外部命令与 Celery 任务均有超时和速率边界。`GET /api/v1/files/{node_id}/preview` 校验权限、刷新最后访问时间并返回短期 URL。`preview.cleanup_artifacts` 周期删除非当前旧版本且超过保留期的产物记录/对象，并按受控前缀扫描超期孤儿预览对象，写入审计、维护结果和 `preview_artifact_cleanup_total`。

## 12. 搜索系统实现

### 12.1 索引结构

OpenSearch index：`drive_files_v1`。

```json
{
  "mappings": {
    "properties": {
      "tenant_id": {"type": "keyword"},
      "space_id": {"type": "keyword"},
      "node_id": {"type": "keyword"},
      "parent_id": {"type": "keyword"},
      "name": {"type": "text", "analyzer": "ik_max_word"},
      "name_keyword": {"type": "keyword"},
      "extension": {"type": "keyword"},
      "mime_type": {"type": "keyword"},
      "size_bytes": {"type": "long"},
      "owner_id": {"type": "keyword"},
      "created_by": {"type": "keyword"},
      "updated_at": {"type": "date"},
      "tags": {"type": "keyword"},
      "acl_tokens": {"type": "keyword"},
      "deny_acl_tokens": {"type": "keyword"},
      "content": {"type": "text", "analyzer": "ik_max_word"},
      "is_deleted": {"type": "boolean"}
    }
  }
}
```

### 12.2 权限过滤方案

搜索不能先返回所有结果再在应用层过滤，因为这样会泄露总数、性能也差。建议索引中写入 `acl_tokens` 和 `deny_acl_tokens`：

```text
user:{user_id}
group:{group_id}
department:{department_id}
space:{space_id}:role:{role}
```

公共空间、外链访问和 external subject 应在分享模块建模后再加入独立 token，不在当前内部用户 ACL token builder 中默认生成：

```text
public:{tenant_id}
```

查询时根据当前用户展开 subject tokens，加入 filter：

```json
{
  "bool": {
    "must": [{"match": {"content": "合同"}}],
    "filter": [
      {"term": {"tenant_id": "..."}},
      {"term": {"is_deleted": false}},
      {"terms": {"acl_tokens": ["space:s1:role:viewer", "user:u1", "department:d1", "group:g1"]}},
      {"bool": {"must_not": [{"terms": {"deny_acl_tokens": ["user:u1", "department:d1", "group:g1"]}}]}}
    ]
  }
}
```

当前实现已提供 `GET /api/v1/search?q=...&limit=...&cursor=...`，查询层使用租户、未删除、`acl_tokens` allow 和 `deny_acl_tokens` 排除过滤，分页使用绑定查询词/排序值的签名 cursor，结果返回 HTML 编码高亮；应用层按 PostgreSQL `read_meta` 二次权限校验。`search.extract_requested` 支持 UTF-8 文本、可复制正文 PDF、DOCX/PPTX/XLSX、图片 OCR、扫描 PDF OCR，以及经 LibreOffice 转换为 PDF 的旧 `.doc/.xls/.ppt` 和 ODF 文档。PDF 先使用 `pypdf`，正文为空时再由 Poppler 渲染并交给 Tesseract；OCR 默认语言为 `eng+chi_sim`。复杂源文件体量、页数、像素、渲染字节、正文字符数和命令超时均有边界，不支持或超限标记为 `skipped`，解析/工具错误标记为 `failed`，对象存储读取错误继续由 outbox 退避重试。

### 12.3 索引一致性

1. 文件上传完成、重命名、移动、删除、恢复和彻底删除后写 outbox event：`search.index_requested`。
2. 文件上传完成后写 outbox event：`search.extract_requested`，抽取任务独立于元数据索引，避免上传事务等待对象读取或文本解析。
3. Worker 从 outbox 消费索引事件，重新加载 PostgreSQL 文件元数据、当前版本、blob、空间成员和 ACL 后写 OpenSearch；如果文件已删除、已彻底删除或不再活跃，则删除对应索引文档。
4. Worker 从 outbox 消费抽取事件，按 `DRIVE_SEARCH_TEXT_EXTRACT_MAX_BYTES` 限制读取文本内容，写入版本抽取状态并刷新索引正文。
5. 权限变更后同事务写入 `permission.changed` 和 `search.acl_rebuild_requested`；搜索 worker 消费后按 space 或 node 子树保守重建受影响文件的 `acl_tokens` / `deny_acl_tokens`。
6. 定时任务比对 PostgreSQL 和 OpenSearch 的更新时间，修复漏索引。
7. 提供 `scripts/rebuild_search_index.py --space-id ...` 用于手动重建。

## 13. 审计与 Outbox

### 13.1 审计事件结构

```json
{
  "event_id": "uuid",
  "tenant_id": "uuid",
  "actor_id": "uuid",
  "actor_type": "user",
  "action": "file.download",
  "resource_type": "file",
  "resource_id": "uuid",
  "space_id": "uuid",
  "result": "allowed",
  "risk_level": "low",
  "request_id": "req_...",
  "ip": "10.0.0.1",
  "user_agent": "...",
  "metadata": {
    "version_id": "uuid",
    "share_id": null
  },
  "created_at": "2026-06-30T10:00:00Z"
}
```

当前实现已提供 `GET /api/v1/admin/audit-logs`。接口仅允许当前租户的系统管理员访问，查询固定带 `tenant_id` 边界，支持按用户、主体类型、动作、资源类型/ID、结果、风险、request_id 和 UTC 时间范围组合筛选，并使用 `created_at DESC, id DESC` 的签名 cursor 分页。成功查询、普通用户越权查询和非法时间范围都会写入 `admin.audit_logs.queried` 审计及 outbox event；响应可包含 metadata、IP 和 User-Agent，因此不得向普通空间管理员或客户端用户开放。

### 13.2 Outbox 表

```sql
create table outbox_events (
  id uuid primary key,
  tenant_id uuid not null,
  event_type varchar(128) not null,
  aggregate_type varchar(64) not null,
  aggregate_id uuid not null,
  payload jsonb not null,
  status varchar(32) not null default 'pending',
  retry_count integer not null default 0,
  next_retry_at timestamptz not null default now(),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index idx_outbox_pending
  on outbox_events(status, next_retry_at, created_at);
```

业务事务内插入 outbox。后台 dispatcher 负责投递到 Celery、日志平台或搜索任务。这样可以避免“数据库写成功但消息没发出去”的经典问题。

## 14. 异步任务与队列

### 14.1 队列划分

| 队列 | 任务 | 优先级 |
| --- | --- | --- |
| `audit` | 审计投递、风险事件 | 高 |
| `permission` | 权限缓存失效、搜索 ACL 更新 | 高 |
| `preview` | Office 转 PDF、图片缩略图、音视频元信息 | 中 |
| `search` | 文本抽取、索引写入、索引重建 | 中 |
| `maintenance` | 过期上传清理、回收站清理、过期外链、容量校准 | 低 |

### 14.2 任务规范

1. 每个任务必须有全局唯一 task_id。
2. 任务参数只传 ID，不传大对象内容。
3. 任务执行前从数据库重新加载最新状态。
4. 任务必须幂等，重复执行不会产生重复副作用。
5. 失败使用指数退避，超过最大重试进入 dead-letter 状态。
6. 任务日志必须包含 task_id、tenant_id、resource_id、request_id。

## 15. API 契约

### 15.1 通用规则

1. API 前缀：`/api/v1`。
2. 分页：列表接口统一使用 cursor pagination，不使用深 offset。
3. 幂等：创建、完成上传、批量操作支持 `Idempotency-Key`。
4. 错误响应统一包含 `code`、`message`、`request_id`、`details`。
5. 所有返回时间使用 ISO 8601 UTC。
6. 用户可见错误不泄露内部对象存储 key、SQL 错误和文件真实存在性。

### 15.2 核心接口清单

| 方法 | 路径 | 权限动作 | 说明 |
| --- | --- | --- | --- |
| GET | `/spaces` | list | 当前用户可访问空间 |
| POST | `/spaces` | manage | 创建团队或项目空间 |
| GET | `/spaces/{space_id}/members` | manage | 查看空间成员 |
| POST | `/spaces/{space_id}/members` | grant | 添加空间成员 |
| PATCH | `/spaces/{space_id}/members/{user_id}` | grant | 调整空间成员角色 |
| DELETE | `/spaces/{space_id}/members/{user_id}` | grant | 移除空间成员 |
| GET | `/files/{node_id}/acl` | grant | 查看节点 ACL |
| POST | `/files/{node_id}/acl` | grant | 创建节点 ACL |
| PATCH | `/files/{node_id}/acl/{entry_id}` | grant | 调整节点 ACL |
| DELETE | `/files/{node_id}/acl/{entry_id}` | grant | 删除节点 ACL |
| GET | `/files` | list | 查询目录子节点 |
| POST | `/files/folders` | upload | 创建文件夹 |
| PATCH | `/files/{node_id}` | update | 重命名、移动、标签更新 |
| DELETE | `/files/{node_id}` | delete | 删除到回收站 |
| DELETE | `/files/{node_id}/purge` | delete | 彻底删除回收站节点 |
| POST | `/files/{node_id}/restore` | restore | 恢复文件 |
| GET | `/files/trash` | restore/delete | 按删除批次根节点查询回收站 |
| POST | `/files/batch-delete` | delete | 批量删除到回收站 |
| POST | `/files/batch-move` | update | 批量移动节点 |
| POST | `/files/batch-restore` | restore | 批量恢复回收站节点 |
| POST | `/files/batch-purge` | delete | 批量彻底删除回收站节点 |
| GET | `/files/operations/{operation_id}` | owner | 查询大目录后台操作状态 |
| POST | `/files/operations/{operation_id}/retry` | owner | 重试失败的大目录后台操作 |
| GET | `/files/{node_id}/versions` | read_meta | 文件版本列表 |
| GET | `/files/{node_id}/versions/{version_id}/download` | download | 获取指定历史版本下载地址 |
| POST | `/files/{node_id}/versions/{version_id}/rollback` | update | 把历史内容回滚为递增新版本 |
| POST | `/uploads/init` | upload | 初始化上传 |
| POST | `/uploads/{id}/complete` | upload | 完成上传 |
| GET | `/files/{node_id}/download` | download | 获取下载地址 |
| POST | `/shares` | share | 创建分享 |
| GET | `/shares/{id}` | read_meta | 分享详情 |
| POST | `/shares/{id}/revoke` | share | 撤销分享 |
| GET | `/search` | list/preview | 搜索文件 |
| GET | `/admin/audit-logs` | super_admin | 按用户、资源、动作、结果、风险、请求 ID 和时间查询租户审计日志 |

### 15.3 详细接口契约示例

#### 文件列表

```http
GET /api/v1/files?space_id={space_id}&parent_id={parent_id}&limit=100&cursor={cursor}
```

响应：

```json
{
  "items": [
    {
      "id": "uuid",
      "type": "file",
      "name": "合同.docx",
      "size_bytes": 1048576,
      "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
      "extension": "docx",
      "updated_at": "2026-06-30T10:00:00Z",
      "owner": {"id": "uuid", "display_name": "张三"},
      "current_version_id": "uuid",
      "preview_status": "ready",
      "permissions": {
        "list": true,
        "preview": true,
        "download": true,
        "upload": false,
        "update": true,
        "delete": false,
        "share": true,
        "grant": false
      }
    }
  ],
  "next_cursor": "opaque-cursor"
}
```

#### 创建文件夹

```http
POST /api/v1/files/folders
Idempotency-Key: client-generated-key
```

```json
{
  "space_id": "uuid",
  "parent_id": "uuid",
  "name": "项目资料",
  "conflict_policy": "fail"
}
```

文件树创建、移动、恢复及对应批量入口已支持 `fail`、`keep_both`、`replace`。`keep_both` 生成 `名称 (n)`，文件保留扩展名；`replace` 先把当前同名节点移动到回收站，再完成新操作，不做不可恢复覆盖。上传初始化当前仍保持 fail-only，后续若把 replace 映射为文件新版本，必须继续复用版本、容量和审计语义。

#### 重命名节点

```http
PATCH /api/v1/files/{node_id}
```

```json
{
  "name": "新名称"
}
```

根目录不允许重命名。同目录同名由数据库唯一索引兜底。

#### 移动文件

```http
POST /api/v1/files/{node_id}/move
Idempotency-Key: client-generated-key
```

```json
{
  "target_parent_id": "uuid",
  "new_name": "新名称.docx",
  "conflict_policy": "keep_both"
}
```

`new_name` 可省略，省略时保留原名称。根目录不允许移动，目录不能移动到自身或自身子目录。

#### 删除与恢复

```http
DELETE /api/v1/files/{node_id}
POST /api/v1/files/{node_id}/restore
```

```json
{
  "target_parent_id": "uuid",
  "new_name": "恢复后的名称"
}
```

删除进入回收站并写入 `deleted_at`、`deleted_by`。恢复时若原父目录已删除或不存在，必须指定新的 `target_parent_id`；同名冲突不覆盖已有节点。

#### 批量操作

```http
POST /api/v1/files/batch-delete
```

```json
{
  "node_ids": ["uuid", "uuid"],
  "mode": "trash"
}
```

批量接口返回每个对象的结果，不应一个失败导致全部无响应：

```json
{
  "results": [
    {"node_id": "uuid", "status": "success"},
    {"node_id": "uuid", "status": "failed", "code": "PERMISSION_DENIED"}
  ]
}
```

`BE-037` 已按该契约完成四类批量入口。每个请求最多处理 100 个节点，每项在独立数据库 savepoint 中复用单项权限、审计、搜索、容量和 blob 引用语义；单项失败只记录对应错误码，不回滚其他成功项。四个入口强制使用 `Idempotency-Key`，`file_batch_operations` 按租户、用户、操作和 key hash 唯一保存请求 hash 与最终响应；相同请求直接重放，不同请求复用同 key 返回 `IDEMPOTENCY_KEY_REUSED`。

### 15.4 Cursor 分页规范

cursor 必须是不透明字符串，前端不能解析。建议内容：

```json
{
  "sort": "updated_at_desc",
  "last_updated_at": "2026-06-30T10:00:00Z",
  "last_id": "uuid"
}
```

服务端 base64url 编码并签名，防止客户端篡改分页条件。文件列表默认排序：文件夹在前，更新时间倒序，id 作为稳定 tie-breaker。

### 15.5 错误码细化

| 错误码 | HTTP | 场景 |
| --- | --- | --- |
| AUTH_REQUIRED | 401 | 未登录 |
| SESSION_EXPIRED | 401 | 登录会话过期 |
| CSRF_TOKEN_INVALID | 403 | CSRF 校验失败 |
| PERMISSION_DENIED | 403 | 权限不足，但不涉及文件存在性隐藏的场景 |
| FILE_NOT_FOUND | 404 | 文件不存在或无权访问 |
| FILE_CONFLICT | 409 | 同目录重名冲突 |
| UPLOAD_SESSION_EXPIRED | 409 | 上传会话过期 |
| UPLOAD_COMPLETING | 409 | 上传正在完成中 |
| HASH_MISMATCH | 422 | 文件哈希不匹配 |
| QUOTA_EXCEEDED | 422 | 容量不足 |
| SHARE_EXPIRED | 410 | 分享过期 |
| SHARE_REVOKED | 410 | 分享撤销 |
| SHARE_PASSCODE_INVALID | 403 | 分享提取码错误 |
| SHARE_VIEW_LIMIT_EXCEEDED | 410 | 分享访问次数耗尽 |
| RATE_LIMITED | 429 | 触发限流 |
| STORAGE_UNAVAILABLE | 503 | 对象存储不可用 |

### 15.6 后续正式 API 闭环

以下接口已经进入 Sprint 9 至 Sprint 12，不再只作为示例或概念保留：

| 领域 | 计划接口 | 任务 |
| --- | --- | --- |
| 文件版本 | `GET /files/{node_id}/versions` | `BE-036` |
| 文件版本 | `GET /files/{node_id}/versions/{version_id}/download` | `BE-036` |
| 文件版本 | `POST /files/{node_id}/versions/{version_id}/rollback` | `BE-036` |
| 回收站 | `GET /files/trash?space_id=...&cursor=...` | `BE-037`（已完成） |
| 批量操作 | `POST /files/batch-delete`、`POST /files/batch-move`、`POST /files/batch-restore`、`POST /files/batch-purge` | `BE-037`（已完成） |
| 内部分享 | `GET /shares/received`、`GET /shares/created` | `BE-038`（已完成） |
| 内部分享 | `GET /shares/{share_id}/items`、`POST /shares/{share_id}/download` | `BE-038`（已完成） |
| 账号安全 | `GET /auth/password/policy`、`POST /auth/password/change`、`GET /auth/sessions`、`DELETE /auth/sessions/{session_id}` | `BE-041`（已完成） |
| 管理员密码 | `POST /admin/users/{user_id}/password-reset`、`POST /admin/users/{user_id}/unlock` | `BE-040`、`BE-041`（已完成） |
| OIDC | provider 列表、登录/绑定 start、callback、logout、identity links | `BE-042`（已完成） |
| OIDC 配置 | `GET/POST/PATCH /admin/identity/oidc/providers`、`POST /admin/identity/oidc/providers/{provider_id}/test` | `BE-042`（已完成） |
| LDAP 配置 | `GET/POST/PATCH /admin/identity/ldap/sources`、`POST /admin/identity/ldap/sources/{source_id}/test` | `BE-043`（已完成） |
| LDAP 同步 | `POST /admin/identity/ldap/sources/{source_id}/sync`、`GET /admin/identity/ldap/runs`、`GET /admin/identity/ldap/runs/{run_id}/conflicts` | `BE-043`（已完成） |
| 管理 API | `/admin/users`、`/admin/departments`、`/admin/groups`、`/admin/spaces`、`/admin/quotas` | `BE-044`（已完成） |
| 管理治理 | `/admin/stats`、`/admin/maintenance`、`/admin/exports` | `BE-045`（已完成） |
| 生命周期 | `/admin/lifecycle/policies`、`/admin/lifecycle/runs` | `BE-047` |
| Outbox | `/admin/outbox/dead-letters`、`POST /admin/outbox/{event_id}/replay` | `BE-050` |

批量写接口已使用持久化 `Idempotency-Key`，响应按对象返回成功或失败结果。版本回滚创建新版本，不重写历史版本；内部分享下载仍执行当前权限和分享接收人双重校验。所有管理接口使用管理员权限、cursor 分页、审计和统一导出任务模型。OIDC client secret、LDAP bind password 等凭据只允许写入受保护的 secret 引用或加密字段，读取接口不得回显明文；连接测试、配置变更和同步运行必须写审计。

## 16. 认证与安全实现

### 16.1 登录与会话

当前支持本地账号、OIDC/OAuth 2.1 + PKCE 和 LDAP 只读目录同步。浏览器认证统一采用 BFF + HttpOnly Cookie Session：

1. 服务端生成 opaque random session token，只保存哈希到 `auth_sessions`，原始值只写入 `drive_session` HttpOnly Cookie。
2. CSRF token 写入可读 Cookie `drive_csrf`，所有 `POST`、`PUT`、`PATCH`、`DELETE` 请求必须通过 `X-CSRF-Token` 请求头回传。
3. 会话轮换通过 `POST /api/v1/auth/session/rotate` 签发新的 session token 和 CSRF token，旧 session 立即标记为 replaced。
4. 检测到旧 session 被复用时，吊销整个 session family。
5. 项目未上线阶段不保留 JWT、Bearer token、refresh token 或 `/auth/refresh` 兼容路径。
6. OIDC 在 BFF 层完成 Authorization Code + PKCE、一次性 state/nonce、issuer/JWKS/签名/claims 校验后继续签发本项目服务端 session cookie；provider token 不进入 Web Storage。
7. LDAP 只负责目录事实和本地对象映射，不能绕过本地用户停用、权限、审计或会话撤销。

密码哈希：Argon2id。管理员重置密码后必须强制用户下次登录修改。用户改密、管理员重置、用户停用和 LDAP 离职同时吊销浏览器与桌面设备会话。

### 16.2 输入安全

1. 文件名最大 255 字符，禁止 `/`、`\`、控制字符、NUL、路径穿越片段。
2. Unicode 文件名统一 normalize，避免肉眼相同但二进制不同导致绕过重名检查。
3. MIME 类型以服务端嗅探为准，客户端传值只作为参考。
4. 压缩包预览必须限制展开数量、总大小、递归深度和路径。
5. 所有 SQL 使用 SQLAlchemy 参数化，禁止字符串拼接 SQL。
6. 外部 URL 预览或远程拉取默认不做，避免 SSRF。

### 16.3 限流

| 场景 | 建议限制 |
| --- | --- |
| 登录失败 | 用户 + IP 维度，连续失败锁定或验证码 |
| 初始化上传 | 用户维度每分钟限流 |
| 分片签名 | session + 用户维度限流 |
| 下载 | 用户、IP、文件维度限流 |
| 外链访问 | IP 总量和 token + IP 维度限流 |
| 搜索 | 用户维度限流，复杂查询限制频率 |

当前实现已完成登录、上传初始化、分片签名、下载预签名、搜索查询和外链访问的 Redis 固定窗口基础限流：登录按来源 IP 和规范化账号哈希双维计数，上传初始化按 `tenant + user` 计数，分片签名按 `tenant + user + upload_session` 计数，下载预签名按 `tenant + user + node + IP` 计数，搜索查询按 `tenant + user + search + IP` 计数，外链访问按 IP 总量和 `token + IP` 计数；触发限流返回 HTTP 429 和 `RATE_LIMITED`。Sprint 11 进一步在用户行锁内实现失败窗口、阶梯延迟、可插拔验证码、临时锁定、`423 ACCOUNT_LOCKED`/`Retry-After` 和管理员乐观解锁。

### 16.4 生产配置自校验

`DRIVE_ENVIRONMENT=production` 时，Pydantic `Settings` 在 API、Worker、beat、migration 和 seed 共同入口执行 fail-fast 校验。应用会拒绝 debug、关闭限流、示例或过短 secret、无强密码 PostgreSQL/Redis/Celery URL、Wildcard Trusted Hosts、带凭据/路径/query/fragment 的 CORS/S3 公共端点，以及非回环 HTTP CORS/S3 或未启用 Secure Cookie 的公网配置；S3 公网端点只允许 443。纯 localhost/回环地址继续支持 Windows Docker Desktop 的 `18080/19000` HTTP 基线。校验错误隐藏原始输入，不在异常文本中回显 secret；宿主 `manage.ps1 -Tls` 继续负责 bind、HSTS、`DRIVE_S3_CORS_ALLOWED_ORIGINS` 对齐、DNS、证书和 Certbot 邮箱等部署层门禁。

## 17. 容量与配额

### 17.1 容量账本

不要只在 `spaces.used_bytes` 上直接加减。建议使用容量流水：

```sql
create table quota_ledger (
  id uuid primary key,
  tenant_id uuid not null,
  account_type varchar(32) not null,
  account_id uuid not null,
  delta_bytes bigint not null,
  reason varchar(64) not null,
  ref_type varchar(64) not null,
  ref_id uuid not null,
  created_at timestamptz not null default now()
);
```

`quota_accounts.used_bytes` 是当前快照，`quota_ledger` 是可审计流水。高并发上传完成时，使用行锁或原子 update 防止超配额。

### 17.2 检查逻辑

```sql
update quota_accounts
set used_bytes = used_bytes + :size
where id = :account_id
  and used_bytes + :size <= limit_bytes
returning used_bytes;
```

返回 0 行表示容量不足。当前策略是删除到回收站不释放容量，彻底删除回收站节点时释放对应文件版本容量并写入负向流水；blob 引用计数同步扣减，但最终对象物理删除仍交给后续垃圾回收和对象生命周期任务。

### 17.3 多维配额运行时与后续治理

`BE-033` 已使用 `quota_accounts` 和 `quota_ledger` 统一表达空间、用户、租户和策略账户。上传初始化执行快速检查；秒传和 multipart complete 创建版本时按固定维度顺序执行数据库原子条件更新，并为每个账户写入 `ref_type=file_version` 的正向流水；彻底删除和回收站保留期清理按这些正向流水释放全部关联账户。`quota_policies` 按租户隔离并按优先级匹配文件扩展名或 MIME 前缀，可限制累计额度和单文件大小。用户/租户默认额度为 `0` 时不创建对应账户。

当前 `quota_policies` 由数据库事实表提供，系统管理员账户/策略 HTTP API 和显式变更审计已完成；既有账户不会因环境默认值变化被自动覆盖，数据库中已建立的用户/租户账户优先于环境默认额度。空间容量校准保持现状，多维通用校准、部门额度、临时上传占用和服务端复杂内容分类仍需后续任务完成。

调度层面，容量校准和对象清理不能只停留在手动任务或 worker 可消费状态。生产环境应通过 CronJob 或等价调度器按租户分批执行，输出 Prometheus 指标，记录清理审计，并对连续失败、扫描积压、对象存储删除失败、容量漂移超过阈值等情况告警。

## 18. 前端协作契约

### 18.1 上传队列前端要求

1. 上传队列支持暂停、继续、取消、失败重试。
2. 每个文件显示初始化、上传中、合并中、处理中、完成、失败状态。
3. 分片上传失败时只重试失败分片。
4. 刷新页面后可通过 upload session 恢复未完成上传。
5. 秒传时不要显示长时间进度条，直接进入完成状态。

### 18.2 文件列表交互契约

后端列表接口返回：

```json
{
  "items": [
    {
      "id": "uuid",
      "type": "file",
      "name": "方案.docx",
      "size_bytes": 1234,
      "mime_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
      "updated_at": "2026-06-30T10:00:00Z",
      "permissions": {
        "preview": true,
        "download": true,
        "upload": false,
        "delete": false,
        "share": true
      },
      "preview_status": "ready"
    }
  ],
  "next_cursor": "..."
}
```

前端按钮状态必须以后端返回的 `permissions` 为准，但这只是体验控制，安全仍由后端接口校验。

### 18.3 Rust 桌面客户端契约

二期桌面客户端采用 Rust stable、Cargo workspace 和 Tauri 2，Windows 11 优先。Tauri 界面层只负责展示状态、接收用户操作和调用受控命令；API client、同步引擎、传输队列、本地索引、文件系统监听、系统凭据和更新验签必须在 Rust 层实现。正式文件传输规范为 `docs/drive-transfer-protocol-v1.md`；当前后端已支持 `X-Drive-Transfer-Protocol: DTP/1` 协商，未知版本返回 HTTP 426，上传和下载响应返回 `protocol_version=DTP/1`。

推荐工程结构：

```text
desktop/
  Cargo.toml
  apps/
    drive-desktop/
  crates/
    drive-api-client/
    drive-device-session/
    drive-sync-engine/
    drive-local-index/
    drive-transfer/
    drive-platform/
    drive-diagnostics/
```

桌面客户端开工前，后端必须先固定以下契约：

1. 桌面设备注册、opaque device session、轮换、设备列表、单设备吊销和全设备吊销；浏览器 Cookie Session 与桌面设备会话保持不同边界。
2. 按租户、用户和同步根目录隔离的增量变更接口，使用不透明签名 cursor 返回新增、修改、移动、删除 tombstone、版本号和权限变化。
3. 上传、移动、重命名和删除请求携带幂等客户端操作 ID；覆盖或修改已有文件必须携带预期 `version_id` 或等价前置条件。
4. 服务端明确 cursor 失效、权限撤销、节点被彻底删除、版本冲突和需要重新全量索引的错误码。
5. OpenAPI 契约生成 Rust 类型或通过独立契约测试锁定，客户端不得复制维护另一套字段和错误码定义。
6. 固定 `Drive Transfer Protocol v1` 应用层契约：控制面通过版本化 HTTPS API 完成上传初始化、批量分片签名、断点状态查询、幂等 complete/abort 和错误码协商；数据面通过短期预签名 HTTPS 直接访问 S3 兼容对象存储，不让大文件正文经过 FastAPI。
7. 协议必须返回或协商 `protocol_version`、`upload_session_id`、`part_size`、`max_parallelism`、已确认分片、校验算法、整文件 SHA-256、签名过期时间、重试提示和幂等客户端操作 ID。客户端可按文件大小和网络质量调整分片、并发和带宽，但不得绕过服务端容量、权限、审计和 hash 校验。
8. 下载继续使用短期预签名 HTTPS 和 HTTP Range；HTTP/2 或 HTTP/3 只作为网关/客户端的透明承载升级并保留回退，不自研 TCP/UDP、TLS、QUIC 或私有可靠传输层。

本地同步状态使用 SQLite 保存远端 node/version、规范化相对路径、本地文件标识、大小、mtime、hash、同步 cursor、待执行操作和失败重试状态。SQLite 是离线恢复与调度状态，不是权限或远端文件状态的事实来源；凭据不得写入 SQLite、日志或普通配置文件。

同步处理规则：

1. 初次同步先拉取远端快照并建立本地索引，再启动文件系统监听和增量 cursor 消费，禁止以高频全量扫描替代远端变更协议。
2. 下载写入同目录随机临时文件，校验大小和 SHA-256 后原子替换；异常退出后清理或恢复未完成临时文件。
3. 上传复用现有秒传、multipart、断点续传和服务端 hash 校验，客户端按已确认分片恢复，不在本地假定 complete 成功。
4. 本地和远端同时修改时保留双方内容，生成包含设备名与 UTC 时间的冲突副本；删除与修改、目录移动与子项修改等冲突进入显式状态，不静默丢弃数据。
5. Windows 首发必须集中处理大小写折叠、保留设备名、尾随点/空格、Unicode normalization、长路径、文件锁、符号链接、junction 和 reparse point；默认不跟随链接。
6. 权限撤销后停止对应下载、上传和本地变更上送，清理活动会话并向用户显示明确状态；本地已有文件是否保留由管理员策略控制并写审计。
7. 客户端提供暂停、继续、取消、重试、带宽限制、并发限制、选择性同步、忽略规则和诊断包导出；诊断包必须脱敏 token、Cookie、文件内容和绝对个人路径。
8. 安装包、自动更新清单和更新包必须签名，客户端在替换二进制前验签；更新失败保留上一可用版本并输出可审计错误。

首个版本采用普通同步目录，不实现虚拟盘占位文件。Windows Cloud Files API、macOS File Provider 和 Linux 虚拟文件系统适配在普通目录双向同步稳定后单独立项。

### 18.4 Web 用户端与管理后台契约

Web 客户端采用 TypeScript、React 和 Vite，使用 npm 锁定依赖。测试采用组件/单元测试和 Playwright E2E。默认工程结构：

```text
frontend/
  package.json
  package-lock.json
  src/
    app/
    api/
      generated/
    modules/
      auth/
      spaces/
      files/
      uploads/
      versions/
      trash/
      shares/
      search/
      account/
      admin/
    components/
    styles/
  tests/
    e2e/
```

前端契约要求：

1. `src/api/generated/` 从归档 OpenAPI 生成，不手工复制 request/response 类型；CI 检测生成结果漂移和 breaking change。
2. 浏览器认证只使用 BFF Cookie Session 和 CSRF，禁止把 session、JWT、refresh token 或等价 bearer 凭据写入 localStorage、sessionStorage 或 IndexedDB。
3. API client 默认发送 credentials，所有副作用请求自动读取 CSRF Cookie 并写入 `X-CSRF-Token`；错误页展示 `request_id`，不展示栈、SQL、storage key 或敏感响应。
4. 用户端页面覆盖登录、空间、文件列表、批量操作、上传队列、预览、下载、搜索、回收站、文件版本、分享创建、分享给我的、通知中心和账号会话。
5. 管理路由覆盖用户、部门、用户组、空间、配额、账号安全、OIDC/LDAP 身份源、审计、统计、维护任务、生命周期和导出；路由守卫只改善体验，后端仍执行管理员权限校验。
6. 文件列表和批量工具栏只使用服务端返回的 `permissions`；批量操作逐项展示成功、失败和错误码，不因单项失败丢失其他结果。
7. 上传队列支持暂停、继续、取消、失败分片重试、刷新恢复和秒传状态；浏览器不在内存中一次性读取大文件计算完整内容。
8. 所有列表使用 cursor，URL 只保存可公开筛选条件，不保存 token、提取码、文件内容或敏感审计条件。
9. 用户端和管理后台构建为静态资源，通过 Compose 内部 Web 服务交给 gateway；Web 服务不发布宿主端口，同一站点的 `/api/v1` 由 gateway 代理到 API。
10. E2E 至少覆盖 Chromium、Firefox、WebKit、键盘操作、常见 Windows 缩放比例、会话过期、CSRF 失败、权限撤销和网络中断恢复。
11. Sprint 11 已补齐验证码/锁定反馈、用户改密、首次改密、会话吊销、OIDC 登录回调、管理员解锁/重置、OIDC 提供商和 LDAP 目录源配置；敏感配置只显示是否已设置，不回显 secret。
12. Sprint 12 已在文件树删除/恢复/彻底删除后台任务基础上完成大目录权限重算与治理页面、生命周期策略与运行记录、审计归档/投递状态、Outbox dead-letter 查询/重放和治理告警页面；legal hold、高级内容分类和复杂 DLP 继续归远期 `GOV-001`。

首个 Web 版本使用中文界面并预留 i18n key，不把中文显示值作为后端枚举提交。管理导出使用后端异步导出任务，不在浏览器拼接大规模 CSV。

## 19. 测试策略

### 19.1 单元测试重点

1. 文件名 normalization 和重名冲突策略。
2. 权限决策：allow、deny、继承、拒绝优先、空间角色、目录 ACL。
3. 容量检查：并发扣减、回滚、删除释放、版本容量。
4. 分享策略：过期、次数耗尽、撤销、提取码错误。
5. 上传状态机：init、complete、abort、expired、idempotent complete。

### 19.2 集成测试重点

使用 testcontainers 或 Docker Compose 拉起 PostgreSQL、Redis、SeaweedFS、OpenSearch。

1. 上传初始化创建 multipart upload。
2. 完成上传后写入 blob、version、node、audit、outbox。
3. 权限变更后缓存失效。
4. 搜索索引写入后按权限过滤。
5. 审计 outbox 投递失败后可重试。

### 19.3 并发测试重点

1. 同目录并发上传同名文件，不能破坏唯一约束。
2. 同一 upload session 并发 complete，只能创建一个版本。
3. 多用户同时写入同一空间，容量不能超扣或漏扣。
4. 外链下载次数并发扣减不能超过 max_downloads。
5. 权限变更与下载并发时，高危下载必须重新校验。

### 19.4 测试用例矩阵

| 模块 | 用例 | 期望 |
| --- | --- | --- |
| 文件树 | 同目录创建同名文件夹 | 返回 FILE_CONFLICT 或按策略重命名 |
| 文件树 | 移动目录到自身子目录 | 返回非法操作 |
| 文件树 | 删除父目录后访问子文件 | 不可访问且搜索不可见 |
| 上传 | complete 接口重复调用 | 只创建一个版本，返回相同结果 |
| 上传 | 分片缺失时 complete | 返回 PART_MISSING，不创建文件 |
| 上传 | 容量不足时 init | 返回 QUOTA_EXCEEDED，不创建 session |
| 下载 | 无权限用户猜测 file_id | 返回 FILE_NOT_FOUND 或无权访问统一错误 |
| 权限 | 父目录 allow、子目录 deny | 子目录 deny 生效 |
| 权限 | 用户从部门移除 | 权限缓存失效后不可访问 |
| 分享 | 外链过期 | 返回 SHARE_EXPIRED |
| 分享 | 下载次数并发耗尽 | 下载次数不超过 max_downloads |
| 搜索 | 权限撤销后搜索 | 不返回无权限文件，或二次校验过滤 |
| 审计 | 下载失败 | 记录 denied 审计日志 |
| 容量 | 并发上传超过剩余额度 | 只有部分成功，总容量不超限 |

### 19.5 契约测试

前后端接口变更必须通过契约测试。建议流程：

1. 后端生成 OpenAPI JSON。
2. CI 比较 OpenAPI diff，发现 breaking change 时阻断合并。
3. 前端基于 OpenAPI 生成 TypeScript client。
4. 后端为核心接口提供示例响应 snapshot。
5. 错误码和枚举值变更必须同步更新文档和前端类型。

### 19.6 性能基准测试

一期最低基准：

| 场景 | 数据规模 | 目标 |
| --- | --- | --- |
| 文件列表 | 单目录 10,000 节点 | P95 < 500 ms |
| 权限批量评估 | 100 个节点 x 6 个动作 | P95 < 200 ms |
| 初始化上传 | 100 RPS | P95 < 300 ms |
| 完成上传 | 50 RPS | P95 < 800 ms，不含对象存储合并耗时 |
| 搜索 | 100 万索引文档 | P95 < 800 ms |
| 审计查询 | 1,000 万日志 | 常见筛选 P95 < 1 s |

2026-07-31 已建立基于 Locust 的 `smoke`、`baseline` 和显式 `target` 可重复入口，并在真实隔离 Docker Compose 中完成 100 目录、2 用户、20 秒的首份 smoke：170 次请求、0 失败，文件列表/上传初始化/搜索/审计 P95 为 32/92/71/25 ms，fixture 和 Docker 资源全部清理。随后新增 `performance.target_data`，按专属 OpenSearch index/action 分阶段生成目标规模数据，支持确定性 ID、checkpoint 恢复、资源节流和只清理自身数据。2026-08-03 已完成目标规模门禁：报告环境确认 10,000 节点、100 万 OpenSearch 文档和 1,000 万审计日志；search、audit、mixed 和 upload-init 复用此前通过工件，只补未通过的 `upload_complete`。显式 warm-up 5 秒并重置统计后，计入 1,936 个 complete、0 失败，吞吐 `58.364 RPS`，不含 storage merge 的 API P95 `790 ms`，端到端 P95 `840 ms`，storage merge P95 `71 ms`，`report.json passed=true`；2,000/2,000 个准备节点已 purge，隔离容器、卷和网络均清理为 0。

### 19.7 Web、身份与治理测试

1. Web 使用生成 client 的契约测试、组件测试和 Playwright E2E，覆盖三种浏览器引擎、会话过期、CSRF、权限撤销、网络中断、上传恢复和批量部分失败。
2. 身份测试已覆盖登录爆破、锁定/解锁、首次改密、全会话吊销、OIDC state/nonce/PKCE、开放重定向、LDAP 外部 ID 冲突和离职禁用；最终用户端/管理后台 E2E 统计由 Sprint 11 集成门禁插槽回填。
3. 大目录测试覆盖 100,000 节点后台删除/恢复、任务中断、游标恢复、重复投递和权限重算。
4. OCR 测试使用正常、超大、损坏、加密和恶意构造样本，验证页数、像素、时限、内存和临时磁盘边界。
5. 审计与 Outbox 测试覆盖分区切换、保留归档、外部投递故障、dead-letter 查询、重复重放和幂等。
6. 备份测试覆盖来源签名篡改、错误密钥、密钥轮换、完整包加密、离线副本和跨版本 Redis/OpenSearch 恢复。

## 20. CI/CD 与部署

### 20.1 CI 流程

```text
checkout
  -> changes（按文件范围选择门禁）
  -> backend：
       setup uv -> uv sync --frozen --all-extras --dev
       -> ruff / format / bandit / pip-audit / mypy
       -> PostgreSQL + SeaweedFS -> Alembic -> pytest
       -> OpenAPI diff + response snapshots
       -> Compose / TLS / monitoring / Nginx -> Docker smoke + image build
  -> frontend：
       setup Node.js + npm ci
       -> generate TypeScript API client
       -> lint + typecheck + unit test + build + Playwright E2E
  -> desktop：
       setup Rust -> cargo fmt + clippy + test
       -> push 或安装包相关变更时 build/sign desktop installer
  -> windows：
       PowerShell 5.1 parser + TLS/backup-restore/governance guards
  -> supply-chain：
       cargo-deny
       -> SeaweedFS immutable image policy
       -> Syft SPDX SBOM + Grype Critical-zero gate
```

相同分支的 `push` 与 `pull_request` 使用同一并发组，新提交自动取消旧运行。SeaweedFS
和 Rust 依赖策略每周定时执行，避免固定 digest 或锁文件不变时遗漏漏洞数据库漂移；
`workflow_dispatch` 和 CI 路由器自身变更执行完整 scope。只修改文档时不启动耗时
job。

### 20.2 Dockerfile 要点

1. 使用多阶段构建。
2. 构建阶段运行 `uv sync --frozen`。
3. 运行镜像使用非 root 用户。
4. 不把 `.env`、测试文件、缓存目录复制到生产镜像。
5. 健康检查使用 `/healthz` 和 `/readyz`。
6. API、Celery Worker 和 Celery beat 复用相同应用镜像，以 command 区分进程。
7. Preview Worker 镜像必须包含 LibreOffice、Poppler 和常用中文字体。
8. `/readyz` 必须执行真实 PostgreSQL 探针，数据库不可用时返回非 2xx；容器回环 healthcheck 在公网 Trusted Hosts 模式下必须携带 API 域名 Host。

### 20.3 Windows 11 正式部署拓扑

正式部署宿主机为 Windows 11，运行 Docker Desktop 的 WSL2 后端和 Linux containers。仓库根 `compose.windows.yml` 是唯一正式编排入口，至少包含：

1. `gateway`：Compose 内 Nginx，唯一发布宿主端口的服务。
2. `api`：FastAPI 应用，不直接发布宿主端口。
3. `migration`：发布时一次性执行 Alembic migration。
4. `seed`：migration 后一次性创建或校准初始管理员。
5. `storage-init`：一次性创建 bucket，并验证签名 PUT/GET/DELETE、匿名访问拒绝、
   CORS allow/deny 和删除后 404；任一失败阻止业务服务启动。
6. `worker-preview`：重 CPU/内存转码任务，独立 `preview` 队列。
7. `worker-search`：文本抽取和索引。
8. `worker-audit`、`worker-permission`、`worker-maintenance`：按职责隔离审计、权限失效和治理队列。
9. `beat`：独立运行 Celery beat，周期调度过期上传、回收站保留期、blob/object 清理和容量校准。
10. PostgreSQL、Redis、SeaweedFS、OpenSearch。
11. Sprint 10 新增 `web`：静态 Web 用户端与管理后台服务，只加入 Compose 网络，由 gateway 代理，不发布宿主端口。

`gateway` 是唯一宿主端口入口。默认本机部署由 gateway 提供 `http://localhost:18080` Web/API 和 `http://localhost:19000` S3 预签名端点；内部依赖不发布宿主端口。公网域名模式由 `manage.ps1 -Tls` 切换到 ACME/TLS Nginx 模板，bind 使用 `0.0.0.0` 或其他非回环 IPv4 地址，通过共享 webroot 完成 HTTP-01、把证书持久化到只读挂载给 gateway 的 named volume，并在 `80/443` 按 API 域名与存储域名的 Host 分流；HTTP 使用 `308` 跳转到 HTTPS，存储转发保留原始 Host、查询参数、方法和请求体。首次签发或扩域使用同一 gateway service 的临时 one-off bootstrap 容器，已有 gateway 容器保留并在签发失败时恢复。证书续期由 Certbot 命令执行，成功后对 Nginx 做配置检查和热重载，可注册为 Windows 计划任务；续期和任务注册前必须确认对应 Certbot renewal lineage 存在，手工挂载或自签名证书不视为可自动续期证书。

Sprint 10 增加 `web` 后，默认服务总数、备份 manifest service 列表、image ID 门禁、fake Docker fixture、integration 断言和部署文档必须同步更新；不得继续把历史 15 个默认服务数量当作新版本常量。

当前 `v1.0.0` 候选实现保留上述同步：Compose 共声明 20 个服务，其中 16 个无 profile
默认服务；`web` 仅在内部网络监听 `8080` 并提供 `/web-healthz`，gateway 是唯一宿主
端口发布者。对象存储服务使用
`chrislusf/seaweedfs:4.40@sha256:52194fba4fecd0083c842158b3a902ba6e04a63619b2b0efcd08007bdb6a4602`
（OCI revision `875cd1f67ea25e8965a4f5ba1e6aaf501ba6b6fa`）、`seaweedfs`、
`storage-init` 和新的 `seaweedfs-data`。身份安全参数、maintenance LDAP task 和
Sprint 12 审计/治理任务由同一 Compose 配置透传。

S3 必须区分：

- 内部端点：API/Worker 使用 `http://seaweedfs:9000`。
- 外部端点：默认 `http://localhost:19000`；公网 TLS 模式使用独立存储域名，例如 `https://storage.example.com`，并要求 `DRIVE_S3_CORS_ALLOWED_ORIGINS` 与 API CORS origin 集合精确一致且只包含 HTTPS origin。
- 外部端点不使用 `/s3` 等 base path，避免破坏 S3/SigV4 签名。

正式环境模板为根 `.env.windows.example`，真实配置保存为未提交的 `.env.windows`。宿主机运维入口为 `deploy/windows/manage.ps1`，命令默认使用 PowerShell；生命周期使用 `config`、`up`、`down`、`status`、`logs`，可用 `-Monitoring` 启用 Prometheus/Alertmanager/Grafana profile；备份治理使用 `backup`、`backup-verify`、`restore`、`backup-retention`、`restore-drill` 及各自计划任务注册/删除；公网初始化使用 `tls-init -Tls`，续期使用 `tls-renew -Tls`，证书检查、续期任务和真实公网验收分别使用 `tls-certificates -Tls`、`tls-register-renewal -Tls`、`tls-validate-public`。普通 `up` 固定使用 `--no-build --pull never`，镜像拉取、runtime/preview 构建、服务启动和备份恢复门禁必须分阶段执行；只有显式 `up -Build` 才允许构建项目镜像。

PostgreSQL、Redis、SeaweedFS 和 OpenSearch 使用 named volumes。SeaweedFS 使用新的
`seaweedfs-data`；旧 `minio-data` 只允许由旧提交和固定 MinIO 镜像读取，并通过
S3 级迁移导入，禁止直接挂载给 SeaweedFS。API 使用受控 SQLAlchemy QueuePool；
Celery Worker 的同步任务入口通过 `asyncio.run()` 建立任务事件循环，因此必须使用
NullPool，禁止跨任务事件循环复用 asyncpg QueuePool。Celery beat 的可重建 schedule
文件放在容器临时目录，任务事实仍以数据库和审计为准。Preview Worker 必须限制
CPU、内存、临时磁盘、并发数和子进程生命周期。停止、更新和回滚不得默认删除
volumes。

自动化备份从 Compose JSON 读取真实 project、network、service image 和 physical volume
name，在 project-scoped 与逐 physical volume Windows named mutex 内静默写入面，使用
PostgreSQL custom-format `pg_dump`，并归档停止状态的 SeaweedFS、Redis、
OpenSearch 与 TLS 证书卷。对象存储替换后的逻辑卷和工件已改为
`seaweedfs-data`/`volumes/seaweedfs-data.tar.gz`。SeaweedFS 版完整 Windows
backup/restore 兼容演练已单独完成，包含发布前校验、隔离 target 数据/全栈健康和
对象恢复点对账，不复用旧 MinIO 演练结论。UTF-8
manifest v2 继续记录 16 个无 profile 默认服务实际 Compose 容器的 image ID、工件
大小/SHA-256、精确 Compose SHA-256、Git commit、项目版本、S3 bucket、OpenSearch
index、`DRIVE_TLS_CERT_NAME` lineage 名称、Alembic revision 和 WAL LSN；可选
detached CMS `manifest.p7s` 认证来源，可选 AES-256-CBC + HMAC-SHA256 +
RSA-OAEP-SHA256 envelope 保护完整 payload。备份输出先写入自动应用 restricted ACL
的 `.partial-*` staging，通过 `backup-verify` 后再原子发布；备份失败必须恢复并对账
source 原运行、退出与健康状态。所有辅助容器必须禁止隐式拉取，并设置 CPU、
memory、memory-swap 和 PID 上限。离线副本使用 inventory digest 和原子发布；
Redis/OpenSearch 跨版本升级使用 RDB/settings/mappings/bulk 可移植导出、目标
rollback export 和自动回退。

`backup-verify` 与恢复必须要求当前 Compose SHA-256、Git commit、项目版本、bucket/index/TLS lineage 配置和全部 16 个默认服务 image reference/image ID 与 manifest 精确一致。卷 tar 必须先在无网络、只读根文件系统、只读备份挂载、drop all capabilities 和 `no-new-privileges` 的临时容器/卷中预解包扫描，拒绝路径穿越、硬链接、特殊文件、悬空或越界 symlink。

备份轮换只允许处理目录名、manifest backup ID、manifest SHA-256、restricted ACL 和 reparse point 检查均通过的托管备份；按保留天数和最少份数取并集保留，删除前再次确认候选位于备份根内。每次预览/执行写 JSON 记录。周期恢复演练选择最新或指定备份，使用随机隔离 Compose project 调用同一完整恢复门禁，保持 target 停止，随后删除 target 容器/卷并确认无残留；成功或失败记录写入仓库外 restricted ACL 目录。两类操作都支持当前 Windows 用户的 ScheduledTasks 注册/幂等删除。

可选 monitoring profile 包含 Prometheus、Alertmanager 和 Grafana，三项服务只加入 Compose 内网并使用独立 named volumes。Prometheus 抓取 API、5 个 Worker、自身和 Alertmanager 并加载 maintenance/platform 规则；Alertmanager 从只读 URL 文件向企业 webhook 投递；Grafana 只由 API Host 的 `/grafana/` 代理。正式启用必须拒绝示例 Grafana 密码、示例/localhost webhook，公网模式的 Grafana root URL 必须是 API HTTPS origin。CI 使用镜像内 `promtool`、`amtool`、dashboard JSON 解析和 healthcheck binary 检查。

备份根目录必须是仓库外的绝对专用目录，不得是卷根、仓库目录或仓库祖先；既有非空目录必须已经应用本项目 restricted ACL，避免脚本直接重写任意宽范围目录 ACL。恢复必须使用与 source 不同且已停止的 Compose project，并拒绝 physical volume 重叠、错误 Compose 卷标签和 foreign container attachment。默认拒绝已有容器和非空卷；`-ForceRestore` 只允许清理已停止的 target 容器或非空卷，不跳过任何一致性门禁，并在清空原非空卷前创建 restricted ACL rollback archive。恢复中途失败时必须停止 target、删除本轮新卷、清空原空卷并还原原非空卷；回滚异常时保留并报告归档路径。恢复提交后的 rollback archive 清理异常只能报告维护失败并保留归档，不得反向清空或回滚已恢复数据。`-NoStartAfterRestore` 用于在 PostgreSQL 和 Alembic revision 恢复后保持服务停止。

`-RestoreEnvironmentOutput` 必须是仓库和备份目录外的绝对新文件路径，父目录预先存在且路径链无 reparse point；CMS 明文只保存在内存中，并在本次恢复模式的数据、Alembic revision 和镜像门禁全部校验成功后的最后一步，通过 restricted ACL 临时文件原子发布。未使用 `-NoStartAfterRestore` 时还必须先完成全栈健康与实际容器 image ID 对账。若发布竞态中目标路径被其他进程创建，恢复失败清理必须保留该 foreign file。备份目录、staging/正式备份、rollback archive 和最终 CMS 输出都自动关闭 ACL 继承，仅允许当前用户、SYSTEM、Administrators 完全控制。

Windows CMS 只加密 `.env.windows`，CMS 私钥必须单独导出并保存在受保护介质中。
PostgreSQL dump、SeaweedFS/Redis/OpenSearch 原始卷归档和包含私钥的 TLS 证书卷归档
仍依赖 BitLocker、restricted NTFS ACL 与加密外部介质，不属于完整包级应用层加密。
`manifest.sha256` 和工件 SHA-256 只校验完整性，不认证备份制作者身份。
Redis/OpenSearch 原始卷恢复只支持相同 image reference、相同 image ID、单节点同
拓扑；跨版本或拓扑变化使用对应产品支持的迁移或快照机制。`-ForceRestore`
rollback 属于尽力恢复，卷驱动、磁盘或 Docker 异常时仍可能需要人工处理。旧 MinIO
`16/9` Critical 已通过 SeaweedFS 正式运行时替换关闭；SeaweedFS 本地 Grype
`v0.115.0` 为 `0 Critical / 1 High`，`GHSA-hrxh-6v49-42gf` 仍需修复或外部风险
接受。Run `31031565671` 已生成当前 digest 的远端 SBOM/Grype 证据；正式 `v1.0.0`
还要求全量迁移、最终完整工件、升级/RPO-RTO 和生产签字，具体风险登记见
`docs/minio-security-risk.md`。

### 20.4 本地依赖开发 Compose

`backend/docker-compose.yml` 继续用于开发机单独拉起依赖，不作为正式部署编排。至少包含：

```text
postgres:16
redis:7
seaweedfs:4.40
opensearch
opensearch-dashboards optional
mailhog optional
```

本地开发启动目标：

```powershell
Set-Location backend
docker compose up -d postgres redis seaweedfs opensearch
uv sync --all-extras --dev
uv run alembic upgrade head
uv run python -m scripts.seed_admin
uv run fastapi dev app/main.py --host 127.0.0.1 --port 18080
```

### 20.5 发布顺序

1. 合并代码前：ruff、mypy、pytest、OpenAPI diff、前端 lint/typecheck/test/build、Playwright、cargo fmt/clippy/test。
2. 运行 `docker compose --env-file .env.windows -f compose.windows.yml config --quiet` 并构建 API、Worker、Preview、Web 镜像与桌面安装包：固定锁文件，生成 SBOM，执行漏洞扫描和签名检查。
3. 更新前执行 `manage.ps1 backup`，随后立即执行 `manage.ps1 backup-verify`；备份 PostgreSQL、SeaweedFS、Redis、OpenSearch、环境配置和 TLS 证书，并记录当前镜像 reference/image ID、migration 与 Compose 版本。
4. 执行一次性 migration 服务：只允许向前兼容 migration。
5. 启动 API，真实 `/readyz` PostgreSQL 探针通过后由 gateway 接流量。
6. 启动 Worker 与 Celery beat，按队列逐类检查，避免任务中断扩大。
7. 启动 Web 服务并通过 gateway 验证用户端、管理后台、`/api/v1` 和 S3 外部端点；桌面端验证安装、登录、同步和升级。
8. 验证默认 `localhost:18080` API/Web、`localhost:19000` S3 外部端点或生产 Host 分流。
9. 观察指标：错误率、延迟、队列积压、数据库慢查询、对象存储异常、Web 静态资源错误、客户端版本分布、容器重启次数和 Windows 宿主磁盘空间。
10. 发布完成：标记版本、归档 OpenAPI、记录 migration、Compose、Web 构建和桌面签名版本。

### 20.6 回滚原则

1. 应用通过上一可用镜像 tag 和上一版 `compose.windows.yml` 快速回滚。
2. 数据库 migration 原则上只做向前兼容，避免依赖回滚 DDL。
3. 如果必须执行破坏性迁移，必须先做影子字段和双写验证。
4. Worker 任务 payload 必须兼容至少一个旧版本，避免发布过程中旧任务无法消费。
5. named volumes 默认保留，不执行 `docker compose down -v`；数据库和数据卷恢复必须经过明确维护窗口，并先在不同 Compose project 使用 `-NoStartAfterRestore` 完成隔离验证。
6. Kubernetes、systemd 是未来可选迁移方案，不是当前默认部署路径。

## 21. 可观测性

### 21.1 日志字段

每条结构化日志至少包含：

```text
timestamp, level, service, env, request_id, task_id, trace_id, span_id,
tenant_id, user_id, module, action, resource_type, resource_id, status, latency_ms
```

`BE-027` 已完成上述基础关联：HTTP 请求结束日志使用路由模板并记录状态与延迟，认证成功后补入租户和用户上下文；Celery task 日志补入 task ID、队列、状态和耗时。OpenTelemetry 已接入 FastAPI 与 Celery，默认保留 trace/span 关联但不向外部发送；可通过环境配置切换 Console 或 OTLP/HTTP exporter。

### 21.2 指标

| 指标 | 类型 | 说明 |
| --- | --- | --- |
| `http_requests_total` | counter | API 请求数 |
| `http_request_duration_seconds` | histogram | API 延迟 |
| `upload_sessions_total` | counter | 上传会话数量 |
| `upload_failures_total` | counter | 上传失败数 |
| `download_requests_total` | counter | 下载次数 |
| `permission_decision_duration_seconds` | histogram | 权限判断耗时 |
| `worker_tasks_total` | counter | 任务数量 |
| `worker_task_duration_seconds` | histogram | 任务耗时 |
| `preview_failures_total` | counter | 预览失败数 |
| `orphan_object_cleanup_total` | counter | 孤儿最终对象扫描和清理计数 |
| `trash_cleanup_total` | counter | 回收站保留期清理计数 |
| `trash_cleanup_released_bytes_total` | counter | 回收站清理释放字节数 |
| `search_index_lag_seconds` | gauge | 搜索索引延迟 |
| `outbox_pending_total` | gauge | 待投递事件数 |

正式 API 以 Prometheus multiprocess 模式聚合多个 Uvicorn worker，HTTP 指标只使用方法、完整路由模板和状态码等有界标签；`/metrics` 抓取本身不计入 HTTP 指标。API 抓取时以短超时刷新 Outbox 状态和待处理搜索事件最大延迟，数据库不可用时仍返回已有进程指标。Celery Worker 是独立容器，各 Worker 在 Compose 内部 `9100` 暴露 `worker_tasks_total`、`worker_task_duration_seconds` 及本进程业务指标，监控系统必须按服务分别抓取，不能假设 API `/metrics` 跨容器聚合 Worker。

### 21.3 告警

1. API 5xx 错误率 5 分钟超过 2%。
2. 上传完成失败率 10 分钟超过 5%。
3. outbox pending 数量持续增长。
4. preview 队列积压超过阈值。
5. PostgreSQL 慢查询或连接池耗尽。
6. 对象存储 5xx 或延迟异常。
7. 磁盘、临时目录或对象存储容量超过 80%。

## 22. 开发里程碑

### 22.1 Sprint 1：工程底座

1. uv 工程、FastAPI app、配置、日志、错误响应、request_id middleware。
2. SQLAlchemy、Alembic、PostgreSQL、Redis、S3 兼容对象存储本地 compose。
3. 用户登录、BFF Cookie 会话、基础用户表和管理员 seed。
4. CI：ruff、mypy、pytest。

完成标准：本地 `uv sync` 后一条命令启动 API 和依赖，健康检查通过。

### 22.2 Sprint 2：空间和文件树

1. space、node、file_blob、file_version model 和 migration。
2. 文件夹创建、文件列表、重命名、移动、删除到回收站。
3. 同目录重名策略和 cursor pagination。
4. 基础审计日志。

完成标准：可通过 API 创建空间、建目录、列目录、删除恢复。

### 22.3 Sprint 3：上传下载

1. upload_session、multipart presign、complete、abort。（已完成接口闭环）
2. 秒传、对象存储适配、容量初版、服务端 hash 校验和最终对象 key 规整。（已完成秒传、对象存储适配、空间容量账本初版、multipart complete 后 `sha256` 校验和 `objects/{tenant_id}/{hash_prefix}/{content_hash}` 归档）
3. 过期上传清理任务。（已完成 `upload.expire_sessions`，按租户扫描过期会话并清理临时对象）
4. 基础限流。（已完成上传初始化、分片签名和下载预签名的 Redis 固定窗口限流）
5. 容量校准和 blob/object/回收站清理。（已完成 `quota.reconcile_space_usage`、`file.cleanup_unreferenced_blobs` 和 `file.cleanup_expired_trash` 维护任务）
6. 下载预签名 URL。（已完成当前版本下载签名）
7. 上传下载审计。（已完成上传初始化、秒传、complete、abort、expired 和下载成功/拒绝审计）
8. 企业级补强：已补齐孤儿最终对象扫描、回收站保留期清理、标准 S3 HTTP multipart 控制面、同 hash 首次上传竞争、真实 PostgreSQL/S3 Docker 验证、配额账户/策略管理 API、文件安全策略、图片/PDF 水印、关键字 DLP 和目标规模性能门禁。

完成标准：1 GB 文件可稳定分片上传、刷新后继续、完成后下载。

### 22.4 Sprint 4：权限系统

1. 空间成员和角色。（已完成用户维度 `space_members` 基础表、创建空间 owner 成员写入、空间级角色检查和 owner/admin 成员管理 API）
2. 目录 ACL、权限继承、拒绝优先。（已完成用户、部门、用户组三类节点 ACL 主体，管理 API、继承开关、deny 优先和文件/上传/下载关键入口校验）
3. 权限缓存和失效事件。（已完成文件列表批量权限评估、`permission.changed` outbox 事件写入和 Redis 缓存失效 worker）
4. 部门/用户组 ACL 主体。（已完成 org 基础事实表、repository 和权限判断主体展开）
5. 搜索 ACL 事件、文件索引写入和查询过滤。（已完成 ACL token builder、`search.acl_rebuild_requested` outbox event、上传完成、重命名、移动、删除、恢复和彻底删除后的 `search.index_requested` 事件、上传完成后的 `search.extract_requested` 文本/PDF/DOCX/PPTX/XLSX 抽取入口、OpenSearch 文件索引写入/删除入口、ACL 变更后的保守范围重建，以及 `GET /api/v1/search` 查询层 allow/deny token 过滤、搜索限流和应用层二次权限校验）
6. 高危操作二次查库。（已完成；权限读路径直接使用 PostgreSQL 事实，授权管理和文件高危操作均在接口执行时重新调用权限服务）
7. 用户、部门和用户组管理。（已完成系统管理员专用的列表、筛选、详情、创建、更新、停用及部门/用户组成员管理；按租户隔离，写操作使用 `version` 前置条件并写入审计和租户级权限变更事件）

完成标准：权限专项测试覆盖核心动作，越权下载失败。

### 22.5 Sprint 5：分享、预览、搜索

1. 内部分享和外链分享。（已完成创建者列表、“分享给我的”、接收人详情/下载、通知和授权重算）
2. 提取码、过期、次数限制、撤销。（已完成内外部次数原子扣减、撤销和过期分享周期失效）
3. 预览 Worker、Office 转 PDF、图片缩略图。（已完成图片 WebP、PDF 首页 WebP、Office 转换和预览产物基础生命周期）
4. OpenSearch 索引和权限过滤。（已完成 allow/deny token、PostgreSQL 二次权限、图片/扫描 PDF OCR 和旧 Office/ODF 抽取）

完成标准：分享链接可控访问，主流文档可预览，搜索不泄露无权限文件。

### 22.6 Sprint 6：管理、治理、上线

1. 管理员审计查询，以及用户、部门、用户组、空间、配额、文件安全、统计、维护和导出管理 API 已完成；`BE-044`、`BE-045` 已闭环，管理写操作统一使用租户隔离、管理员权限、乐观前置条件和审计。
2. 用户/租户/策略多维配额核心运行时。（已完成 `BE-033`，并补齐账户/策略管理 API；部门/临时额度、通用校准和容量治理看板继续后续治理）
3. 运维监控、任务定时调度、失败告警、清理指标和备份恢复脚本。（API/Worker 指标、OpenTelemetry、结构化日志、维护任务连续失败/stale/结果指标、Prometheus/Alertmanager/Grafana `monitoring` profile、公网 TLS 验收入口、备份轮换和随机隔离恢复演练均已完成；真实 webhook、DNS 和受信证书记录由生产环境验收）
4. 压测、安全测试、UAT 修复。
5. Windows 11 Docker Desktop 正式部署：根 Compose、Nginx gateway、PowerShell 管理脚本、named volumes、备份恢复和上线手册。（本机 HTTP、公网 TLS 配置和自动化隔离备份恢复已完成；真实受信证书/双域名验收待生产环境执行）
6. Kubernetes、systemd 作为未来可选迁移说明。

完成标准：试点环境上线，监控告警可用，备份恢复演练通过。

### 22.7 Sprint 7：Rust 桌面客户端基础

1. 建立 `desktop/` Cargo workspace 和 Tauri 2 Windows 应用骨架。
2. 完成设备会话、系统凭据存储、Rust API client 和 OpenAPI 契约测试。
3. 完成本地 SQLite 索引、迁移、操作日志和异常退出恢复。
4. 固定 `Drive Transfer Protocol v1`，完成登录、空间/目录浏览、预签名 HTTPS 直传上传下载队列、暂停续传、同步目录选择、托盘和诊断导出。
5. 后端交付增量变更 cursor、删除 tombstone、版本前置条件和幂等客户端操作 ID。
6. CI 交付 fmt、Clippy、测试、依赖/许可证检查和 Windows 安装包构建。

完成标准：Windows 11 可安装运行，登录后可浏览空间，1 GB 文件上传下载可暂停、恢复并通过 hash 校验，凭据只进入系统凭据库。

### 22.8 Sprint 8：Rust 双向同步与桌面发布

1. 远端增量变更消费和本地文件系统监听。（已完成 `notify` 监听、本地扫描补偿、服务端 cursor/tombstone 消费和后台同步循环）
2. 离线操作队列、网络恢复、进程重启恢复和失败退避。（已完成 SQLite 操作/传输持久化、异常退出恢复、Range/multipart 续传和有界指数退避）
3. 冲突副本、删除/修改冲突、目录移动冲突和权限撤销处理。（已完成双端修改、删除/修改、目录移动/重命名和目标占用冲突；旧操作会进入 conflict，设备吊销会停止全部同步根）
4. 选择性同步、忽略规则、带宽/并发限制和单文件状态诊断。（已完成 include/ignore glob、限速、1 至 16 并发、逐文件状态和冲突列表）
5. Windows 路径兼容、安全边界、签名安装包和更新验签。（已完成保留名、尾随点/空格、Unicode NFC、长路径、链接/reparse point 边界、原子替换、Ed25519 清单/包验签、Authenticode 指纹校验和 watchdog 回退）
6. Windows 稳定后评估 macOS 和 Linux 平台适配。（保持后续平台边界，不进入当前 Windows 首发）

完成标准：已覆盖 10,000 文件初始索引、1 GiB 中断恢复状态和真实进程重启 Range 续传；双端并发修改与目录移动/重命名冲突不丢失本地内容，异常退出后队列可恢复，设备吊销后客户端停止全部同步，签名清单/包篡改会拒绝且更新健康检查失败可回退。

### 22.9 Sprint 9：核心产品能力闭环

1. 文件版本列表、指定版本下载和回滚为新版本。（`BE-036` 已完成签名 cursor、DTP/1 历史下载、前置条件、配额与审计核心链路）
2. 回收站列表、批量删除、移动、恢复和彻底删除。（`BE-037` 已完成签名 cursor、逐项结果、savepoint 部分失败和持久化幂等重放）
3. 内部分享的创建者列表、分享给我的、接收人访问/下载和站内通知。（`BE-038`、`BE-039` 已完成）
4. 用户、部门、用户组、空间、配额、审计、统计、维护和导出管理 API。（`BE-044`、`BE-045` 已完成租户隔离、cursor、乐观更新、审计、持久化异步任务和 CSV 导出闭环）
5. 所有新增写接口接入权限、审计、容量、幂等、OpenAPI 和并发测试。

完成标准：版本回滚不改写历史，批量操作逐项返回结果，内部分享接收人可完成受控访问，管理员可通过 API 完成基础组织和治理操作。

### 22.10 Sprint 10：Web 用户端与管理后台

1. 建立 `frontend/` TypeScript + React + Vite 工程和 OpenAPI 生成 client。（`FE-001`、`FE-002` 已完成；归档为 83 paths / 110 operations / 132 schemas）
2. 完成登录、空间、文件、批量操作、上传、预览、搜索、回收站、版本、分享、分享给我的和通知页面。（`FE-003` 至 `FE-005` 已完成）
3. 完成用户、部门、组、空间、配额、审计、统计、维护和导出管理页面。（`FE-006`、`FE-007` 已完成）
4. 完成 BFF Cookie、CSRF、权限枚举、统一错误和 request_id 体验。（`FE-002`、`FE-008` 已完成）
5. 完成 Compose 内部 Web 服务、gateway 路由、静态资源缓存和发布回滚。（`FE-009` 代码侧已完成：内部 `web:8080`，gateway 唯一发布 `18080/19000`）
6. CI 完成 lint、类型检查、单元测试、构建、OpenAPI diff 和 Playwright E2E。（`FE-008`、`FE-009` 已完成；本地与远端受影响三引擎均为 `13 passed, 2 skipped`，最终 run `30893658311` 全绿）

完成标准：用户和管理员主要流程可从浏览器完成，Web 服务不发布宿主端口，三种浏览器引擎 E2E 通过。提交 `8da1abe` 对应 `backend-ci` run `30893658311` 的 9 个 job 全部成功，Sprint 10 已完成。

### 22.11 Sprint 11：身份与账号安全

1. 登录失败限流、阶梯延迟、临时锁定、管理员解锁和可插拔验证码已完成。
2. 用户改密、管理员重置、首次登录强制改密、密码策略、浏览器会话管理和浏览器/桌面全会话吊销已完成。
3. OIDC/OAuth 2.1 + PKCE provider 配置、连接测试、账号绑定、登录回调、issuer/JWKS/claims 校验和单点登出已完成。
4. LDAP 用户、部门、组和成员 claim 的 dry-run、全量/增量同步、稳定映射、禁用/离职和冲突报告已完成；异步任务运行在 maintenance 队列。
5. 身份与会话审计、指标和 Prometheus 告警已接入；账号安全专项与受影响回归已通过，最终契约/E2E/迁移/CI 统计使用文档插槽回填。
6. Web 用户端已完成锁定/验证码反馈、改密、强制改密、会话管理、OIDC 登录/绑定；管理后台已完成解锁、密码重置、OIDC provider、LDAP Source、连接测试、同步 run 和冲突页面。
7. 项目版本已统一为 `0.8.0`，migration head 为 `20260804_0024`；运行时 OpenAPI 为 105 paths / 134 operations / 162 schemas。

完成标准：代码侧已满足本地账号、OIDC 和 LDAP 映射边界明确，锁定和重置流程可审计，身份提供商故障不绕过本地权限。最终提交 `e6b4f6d`，`backend-ci` run `30922718148` 成功；changes/backend 成功，7 个未受影响 job 按 scope 跳过，frontend 已由 run `30919983108` 验证，Rust/MinIO/Windows/安装包已由 run `30917345858` 验证。真实企业 provider/LDAPS 仍属于生产验收。

### 22.12 Sprint 12：规模化治理与内容能力

1. `BE-046` 已完成：删除、恢复和彻底删除继续使用既有后台批处理；权限重算新增持久 operation、固定批次、稳定 cursor、租户隔离、失败重试和进度 API。运行期间出现更高 `permission_version` 时从新快照重启，避免旧 ACL token 覆盖新权限事实。
2. `BE-047` 已完成：新增每租户唯一生命周期策略，统一回收站/预览保留期和临时上传、过期分享、无引用 blob、孤儿对象开关；策略使用乐观版本，dry-run 与正式运行都持久化。legal hold 不在本 Sprint，继续归 `GOV-001`。
3. `BE-048` 已在 Sprint 5 提前完成：图片 OCR、扫描 PDF、旧 Office/ODF 抽取和资源上限保持现有实现；高级内容分类和复杂 DLP 不在本 Sprint，避免重复开发和重复测试。
4. `BE-049` 已完成：`audit_logs` 使用 PostgreSQL 月分区，周期创建未来分区；保留期数据先生成签名归档再按配置删除源记录，外部 HTTP 投递使用 HMAC 签名并按错误类型进入 Outbox 状态机。
5. `BE-050` 已完成：Outbox 增加 transient/permanent 分类、指数退避 jitter、处理超时恢复、最老积压指标、dead-letter 列表/详情和幂等重放；管理员响应不回显完整 payload。
6. `OPS-001` 至 `OPS-003` 已完成：manifest v2 detached CMS 来源签名、可选 AES-256-CBC + HMAC-SHA256 + RSA-OAEP-SHA256 完整包保护、离线副本原子复制/盘点/轮换，以及 Redis RDB、OpenSearch settings/mappings/bulk 的可移植导出、跨 major 门禁和自动回退。
7. `QA-001`/`QA-002` 已接入：后端 CI 在同一次 pytest 中运行 PostgreSQL、Redis、S3 兼容对象存储、OpenSearch 四依赖故障/恢复矩阵并上传报告；OpenAPI 归档、TypeScript client、route matrix 和 breaking-change 检查继续作为契约门禁。已通过的 `BE-029` 目标规模性能工件复用，不重复运行无关 target。
8. `FE-012` 已完成：管理后台覆盖大目录任务、权限重算、生命周期策略/运行、审计归档/投递、Outbox dead-letter 和治理告警；Grafana 新增治理看板，Prometheus 新增权限重算、生命周期失败和 Outbox 最老积压告警。

完成标准：代码侧已完成；项目版本为 `0.9.0`，migration head 为 `20260804_0026`，OpenAPI 为 116 paths / 148 operations / 178 schemas。最终完成仍以本轮新增和直接受影响本地门禁、统一 `dev` 推送及该提交的远端 CI 全绿为准；真实生产密钥保管、离线介质移交和外部审计接收端属于上线验收。

### 22.13 Sprint 13：稳定版发布

1. 后端、Web 与 Rust 桌面端统一契约、版本和发布工件。
2. 完整 UAT、性能、安全、依赖、镜像、恢复、浏览器和桌面升级验收。
3. 固定告警阈值、RPO/RTO、已接受风险和运维责任。
4. 合并 `main`，发布 `v1.0.0`、OpenAPI、SBOM、镜像、Web 工件、桌面安装包和验收报告。

完成标准：试点和正式发布门禁通过，升级与回滚路径经过演练，发布工件可追踪且可验证。

2026-08-05 当前进度：

- 正式对象存储已改为 SeaweedFS `4.40` 固定 digest、`seaweedfs`、
  fail-closed `storage-init` 和新的 `seaweedfs-data`。旧 MinIO `16/9` Critical 已在
  正式运行时替换层面关闭。
- 既有真实 S3 集成 `3 passed`；`storage-init` 的签名读写删除、匿名拒绝、CORS
  allow/deny 和删除后检查通过；一次真实 MinIO→SeaweedFS 单对象迁移保持 size、
  SHA-256、Content-Type、用户 metadata 和 tags。
- 本地与远端 Grype `v0.115.0` 均为 `0 Critical / 1 High`；High
  `GHSA-hrxh-6v49-42gf` 的报告修复版本为 gRPC `1.82.1`，仍需修复或外部风险接受。
- 旧 `minio-data` 只允许由旧提交和固定 MinIO 镜像读取，再按
  `docs/object-storage-seaweedfs-migration.md` 走 S3 级迁移，禁止直接挂载给
  SeaweedFS。
- 功能提交 `ba378cf2f18a` 已推送，受影响 run `31031565671` 全绿；Windows 备份恢复
  兼容也已通过。完整全量迁移、UAT、soak、升级/RPO-RTO、最终完整工件、真实
  DNS/TLS、OIDC/LDAPS、告警接收和责任签字仍未完成，因此不创建 tag/Release。

### 22.14 详细工程任务拆分

以下任务可直接拆成 issue。估时按熟练工程师计算，实际根据团队经验调整。

| 编号 | 任务 | 依赖 | 估时 | 验收 |
| --- | --- | --- | --- | --- |
| BE-001 | 初始化 uv + FastAPI 工程 | 无 | 1d | healthz、readyz、OpenAPI 可访问 |
| BE-002 | 配置系统和结构化日志 | BE-001 | 1d | request_id 贯穿日志和响应 |
| BE-003 | PostgreSQL/Redis/S3 兼容 compose | BE-001 | 1d | 本地一键启动依赖 |
| BE-004 | SQLAlchemy session 和 Alembic | BE-003 | 1d | migration 可升级回滚测试库 |
| BE-005 | 用户、密码、Cookie 会话认证 | BE-004 | 3d | 登录、会话轮换、登出、CSRF 和复用检测测试通过 |
| BE-006 | tenant/user/department/group 模型 | BE-004 | 2d | seed 用户和部门树 |
| BE-007 | space 和 space_member 模型 | BE-006 | 2d | 创建个人/团队空间 |
| BE-008 | node/file_blob/file_version 模型 | BE-007 | 2d | migration 和基础 repository 测试 |
| BE-009 | 文件夹创建和列表 | BE-008 | 3d | cursor 分页、重名冲突测试 |
| BE-010 | 文件移动/重命名/删除/恢复 | BE-009 | 4d | 目录移动和回收站测试 |
| BE-011 | S3 StorageAdapter | BE-003 | 3d | S3 multipart 集成测试 |
| BE-012 | upload_session 初始化 | BE-011 | 3d | 权限、容量、秒传分支测试 |
| BE-013 | 分片签名和状态查询 | BE-012 | 2d | 断点续传测试 |
| BE-014 | complete upload | BE-013 | 4d | 幂等、缺片、对象合并测试 |
| BE-015 | 下载预签名 URL | BE-014 | 2d | 权限和审计测试 |
| BE-016 | quota_account 和 ledger | BE-012 | 3d | 并发容量扣减测试 |
| BE-017 | 权限角色和 ACL 表 | BE-010 | 3d | 角色授权和目录 ACL 测试 |
| BE-018 | PermissionService check/batch_check | BE-017 | 5d | 继承、deny 优先、批量评估测试 |
| BE-019 | 权限缓存和失效事件 | BE-018 | 3d | 权限变更后缓存失效 |
| BE-020 | share 外链 token 和提取码 | BE-018 | 4d | 过期、撤销、次数限制测试 |
| BE-021 | audit_logs 和 outbox | BE-004 | 3d | 业务事务内写审计和 outbox |
| BE-022 | outbox dispatcher | BE-021 | 3d | 失败重试和 dead-letter 测试 |
| BE-023 | preview worker | BE-014 | 5d | Office/PDF/图片预览状态 |
| BE-024 | OpenSearch indexer | BE-014,BE-018 | 5d | 权限过滤搜索测试 |
| BE-025 | 管理员审计查询 | BE-021 | 2d | 按用户、文件、时间筛选 |
| BE-026 | lifecycle cleanup jobs | BE-010,BE-016 | 3d | 已完成过期上传、blob 清理、回收站保留期清理和真实 PostgreSQL Docker 测试 |
| BE-027 | metrics/tracing 接入 | BE-001 | 3d | 已完成 API 多进程 Prometheus、Worker 内部指标、OpenTelemetry FastAPI/Celery、日志 trace 关联和真实 Docker smoke |
| BE-028 | Windows Docker Compose 正式部署 | BE-001 | 4d | 根 Compose 构建启动、gateway、真实 readiness、Worker/beat、备份回滚验证成功 |
| BE-029 | 性能压测脚本 | BE-014,BE-018 | 3d | 已完成 10,000 节点、100 万索引、1,000 万审计数据和完整 target 门禁；`upload_complete` 58.364 RPS、API P95 790 ms、0 失败 |
| BE-030 | 安全测试修复 | 全部核心模块 | 5d | 已完成依赖/静态扫描、登录双维限流、敏感输入脱敏、production Settings fail-fast、36 route 身份/CSRF、真实跨租户/撤权、恶意文件/Range/预签名 URL/外链穷举、Host/CORS 和真实 Nginx 原始 HTTP 门禁 |
| BE-031 | 孤儿最终对象扫描 | BE-014,BE-026 | 2d | 已完成 dry-run、删除、审计、指标、非受控 key 跳过、失败保留和跨批次扫描测试；生产告警归 BE-035 |
| BE-032 | 对象存储集成测试 | BE-011,BE-014 | 2d | 已完成公共预签名 + 标准 S3 HTTP multipart、真实 S3/SeaweedFS、异常 complete 恢复、幂等 abort、失败清理和同 hash PostgreSQL 并发门禁 |
| BE-033 | 多维配额策略 | BE-016 | 4d | 已完成用户/租户/策略账户、原子扣减、按版本流水释放、账户/策略管理 API、乐观前置条件和定向回归；通用校准继续后续治理 |
| BE-034 | 高密级下载代理 | BE-015,BE-018 | 4d | 已完成内部流式代理、单段 Range、独立限流、自动选路、图片/PDF 水印、关键字 DLP 和范围审计；外链代理与复杂内容识别继续后续治理 |
| BE-035 | 维护任务调度告警 | BE-026,BE-027 | 3d | 已完成五个周期任务调度、Redis 连续失败状态、结构化告警、stale/结果指标和 Prometheus 规则 |
| BE-036 | 文件版本列表、下载与回滚 | BE-008,BE-014,BE-016,BE-018 | 4d | 已完成签名 cursor、指定版本 DTP/1 下载、不可变回滚、前置条件、多维配额、审计和事件定向测试 |
| BE-037 | 回收站列表与批量文件操作 | BE-010,BE-018 | 5d | 已完成删除批次根节点 cursor、四类批量入口、持久化幂等键、逐项结果和部分失败测试 |
| BE-038 | 内部分享接收端 | BE-006,BE-018,BE-020 | 4d | 已完成创建者/接收人列表、详情、DTP/1 下载、次数限制、撤销和成功/拒绝审计测试 |
| BE-039 | 内部分享通知与成员变化失效 | BE-006,BE-019,BE-038 | 3d | 已完成站内通知、已读/失效、部门/组变化和物化授权重算测试 |
| BE-040 | 登录失败防护与账号锁定 | BE-005,BE-027 | 3d | 已完成用户/IP 限流、失败窗口、阶梯延迟、验证码、锁定、解锁、指标和审计 |
| BE-041 | 密码与会话管理 | BE-005,BE-006 | 4d | 已完成用户改密、管理员重置、强制改密、浏览器会话管理和浏览器/桌面全会话吊销 |
| BE-042 | OIDC/OAuth 2.1 + PKCE | BE-005,BE-041 | 5d | 已完成 provider、state/nonce/PKCE、绑定、回调、ID token 校验和登出安全边界 |
| BE-043 | LDAP 目录同步 | BE-006,BE-041 | 5d | 已完成连接测试、dry-run、全量/增量、稳定映射、成员 claim、禁用/离职、冲突和审计 |
| BE-044 | 用户组织空间配额管理 API | BE-006,BE-007,BE-016,BE-018 | 6d | 已完成用户、部门、用户组、空间和配额管理；覆盖租户隔离、cursor、筛选、详情、原子创建、乐观更新、停用、owner/root 一致性、审计和并发边界 |
| BE-045 | 管理统计、维护与导出 API | BE-025,BE-027,BE-035,BE-044 | 5d | 已完成租户统计、维护任务状态与异步运行、审计/空间/用户 CSV 导出、短期下载、过期清理、任务持久化和审计 |
| BE-046 | 大目录后台操作 | BE-010,BE-022,BE-027 | 6d | 已完成删除/恢复/彻底删除与权限重算的持久任务、固定批次、稳定 cursor、权限版本重启、进度/失败恢复和治理 API |
| BE-047 | 完整生命周期治理 | BE-026,BE-035 | 5d | 已完成租户策略、乐观版本、回收站/预览保留期、上传/分享/blob/孤儿对象开关、dry-run/正式运行记录和治理页面；legal hold 归 `GOV-001` |
| BE-048 | OCR 与复杂格式抽取 | BE-023,BE-024,BE-027 | 5d | 图片/扫描 PDF、旧 Office/ODF、资源限制、失败状态和索引测试已提前完成；高级内容分类/复杂 DLP 归 `GOV-001` |
| BE-049 | 审计分区、保留与外部投递 | BE-021,BE-022,BE-025 | 6d | 已完成月分区/未来分区、保留归档、签名元数据、可选源删除、外部 HMAC 投递和治理摘要 |
| BE-050 | Outbox dead-letter 与重放 | BE-022,BE-027 | 4d | 已完成错误分类、jitter、处理超时恢复、最老积压指标、dead-letter 查询和幂等重放 |
| DC-001 | Rust Cargo workspace + Tauri 2 骨架 | Sprint 6 | 2d | Windows 应用可构建、启动和显示版本 |
| DC-002 | 桌面设备会话后端契约 | BE-005 | 4d | 注册、轮换、列表、单设备/全设备吊销测试 |
| DC-003 | 增量变更与 tombstone API | BE-008,BE-021 | 5d | cursor、移动、删除、权限变化和失效测试 |
| DC-004 | Rust API client 与契约生成 | DC-001,DC-002 | 3d | OpenAPI 变更可检测，错误码映射完整 |
| DC-005 | SQLite 本地索引与操作日志 | DC-001 | 4d | migration、崩溃恢复和 cursor 持久化测试 |
| DC-006 | Rust 上传下载队列与 HTTPS 传输协议 | DC-004,BE-014,BE-015 | 5d | `Drive Transfer Protocol v1`、批量签名、1 GB 暂停续传、Range、hash 和取消测试 |
| DC-007 | 文件系统监听与双向同步 | DC-003,DC-005,DC-006 | 7d | 已完成本地/远端增量、SQLite 离线队列、网络/进程恢复、限速并发和逐文件状态测试 |
| DC-008 | 冲突与 Windows 路径策略 | DC-007 | 5d | 已完成双端修改、删除/修改、目录移动/重命名、目标占用、保留名、Unicode、长路径和链接边界测试 |
| DC-009 | 系统凭据、安装签名和安全更新 | DC-002,DC-004 | 4d | 已完成系统凭据、Ed25519 清单/包验签、Authenticode 指纹、暂存安装、健康标记和回退测试 |
| DC-010 | 桌面端 CI、安装包和验收 | DC-001..DC-009 | 4d | 已完成 fmt、Clippy、核心测试、Windows release/NSIS、签名安装包和更新工件门禁配置；最终以本次 `dev` CI 全绿收尾 |
| FE-001 | TypeScript + React + Vite 工程 | BE-001 | 2d | npm 锁文件、开发构建、目录结构和 CI 基线通过 |
| FE-002 | 登录、OpenAPI client 与 Cookie/CSRF 壳 | FE-001,BE-005 | 3d | 本地登录/登出、类型生成、credentials、CSRF、request_id 和会话过期测试 |
| FE-003 | 文件浏览、批量操作与上传下载队列 | FE-002,BE-009,BE-014,BE-015,BE-037 | 6d | 列表、批量删除/移动、上传恢复、秒传、下载和权限状态 E2E |
| FE-004 | 搜索预览版本与回收站页面 | FE-003,BE-023,BE-024,BE-036,BE-037 | 5d | 搜索、预览、版本回滚、批量恢复/彻底删除 E2E |
| FE-005 | 分享、分享给我的与通知页面 | FE-002,BE-038,BE-039 | 4d | 内外分享、提取码、接收人、通知已读/失效和撤销 E2E |
| FE-006 | 用户组织空间配额管理页面 | FE-002,BE-044 | 5d | 管理权限、分页、筛选和变更确认 E2E |
| FE-007 | 审计统计维护与导出页面 | FE-002,BE-045 | 4d | 审计筛选、统计、维护任务状态和异步导出 E2E |
| FE-008 | 权限体验、可访问性与错误恢复 | FE-003..FE-007 | 3d | 键盘、缩放、权限撤销、网络失败和错误码展示测试 |
| FE-009 | Web E2E、Compose 与发布 | FE-001..FE-008,BE-028 | 5d | 三浏览器引擎、gateway、缓存、升级和回滚通过 |
| FE-010 | 用户账号安全与 OIDC 页面 | FE-009,BE-040,BE-041,BE-042 | 4d | 已完成锁定/验证码反馈、改密、强制改密、会话吊销、OIDC 登录/绑定和回调页面 |
| FE-011 | 管理员账号安全与身份源页面 | FE-009,BE-040..BE-043 | 5d | 已完成解锁、重置、OIDC/LDAP 配置、secret 状态、连接测试、同步 run 和冲突页面 |
| FE-012 | 生命周期与治理页面 | FE-007,BE-046,BE-047,BE-049,BE-050 | 4d | 已完成后台任务、权限重算、生命周期运行、审计归档/投递、dead-letter 重放和告警聚焦 E2E |
| OPS-001 | 备份来源签名与完整包加密 | BE-028 | 5d | 已完成 manifest v2 detached CMS 签名、完整包加密/认证、v1 兼容和专项正负例 smoke |
| OPS-002 | 备份保留、离线副本与周期恢复 | OPS-001,BE-035 | 4d | 已完成离线副本原子复制、inventory digest、保留轮换和 restricted ACL 治理记录 |
| OPS-003 | Redis/OpenSearch 跨版本迁移 | BE-028,OPS-002 | 5d | 已完成 RDB/可移植索引导出、major 版本门禁、应用、自动回退和数据一致性 JSON 报告 |
| QA-001 | 全依赖集成矩阵 | BE-032,BE-049,BE-050 | 5d | 已接入 PostgreSQL、Redis、S3 兼容对象存储、OpenSearch 健康、逐项故障探测、恢复和最终全健康 CI 报告 |
| QA-002 | OpenAPI diff 与契约快照 | BE-001,FE-002,DC-004 | 3d | 已同步 OpenAPI/client/route matrix/version，继续由 breaking-change 和核心响应 snapshot 门禁阻断漂移 |
| REL-001 | 全产品 UAT | Sprint 7..Sprint 12 | 5d | 用户、管理员、Web、桌面和运维场景验收 |
| REL-002 | 性能与安全发布门禁 | BE-029,BE-030,QA-001 | 5d | 目标基准、故障注入和高危问题清零 |
| REL-003 | 安装升级与回滚演练 | FE-009..FE-012,DC-010,OPS-003 | 4d | Web、桌面、镜像、migration 和数据回滚演练 |
| REL-004 | 运维责任、风险与 RPO/RTO | OPS-002,REL-001..REL-003 | 3d | 告警阈值、值班手册、风险接受和恢复目标签字 |
| REL-005 | `v1.0.0` 正式发布 | REL-001..REL-004 | 2d | tag、Release、OpenAPI、SBOM、镜像、安装包和报告齐全 |

### 22.15 每个 Sprint 的技术验收物

| Sprint | 主要任务 | 技术验收物 |
| --- | --- | --- |
| Sprint 1 | `BE-001` 至 `BE-006` | 可运行工程、依赖服务、登录、CI、基础日志 |
| Sprint 2 | `BE-007` 至 `BE-010` | 核心表 migration、空间、文件树、目录操作 API |
| Sprint 3 | `BE-011` 至 `BE-016` | S3 上传下载、秒传、断点续传、容量账本和服务端 hash 校验 |
| Sprint 4 | `BE-017` 至 `BE-019` | 权限服务、ACL、缓存失效、批量权限评估 |
| Sprint 5 | `BE-020` 至 `BE-024` | 分享、预览、搜索、Outbox、异步任务 |
| Sprint 6 | `BE-025` 至 `BE-035` | 管理 API、企业级传输/配额/治理补强、压测、安全修复、Windows Docker Compose 正式部署和运维文档 |
| Sprint 7 | `DC-001` 至 `DC-006` | Rust/Tauri 桌面骨架、设备会话、增量同步契约、本地索引、单向传输和 Windows 安装包 |
| Sprint 8 | `DC-007` 至 `DC-010` | 双向同步、离线恢复、冲突处理、选择性同步、签名更新和桌面端验收报告 |
| Sprint 9 | `BE-036` 至 `BE-039`、`BE-044` 至 `BE-045` | 版本、回收站、批量操作、内部分享接收端和完整管理 API |
| Sprint 10 | `FE-001` 至 `FE-009` | Web 用户端、管理后台、批量操作、分享通知、OpenAPI client、Playwright E2E 和 Compose Web 发布 |
| Sprint 11 | `BE-040` 至 `BE-043`、`FE-010` 至 `FE-011` | 登录安全、密码与会话管理、OIDC、LDAP、身份操作页面和身份安全报告 |
| Sprint 12 | `BE-046` 至 `BE-050`、`FE-012`、`OPS-001` 至 `OPS-003`、`QA-001` 至 `QA-002` | 大目录权限/治理收尾、完整生命周期、OCR、审计/Outbox、治理页面、备份安全和全依赖集成 |
| Sprint 13 | `REL-001` 至 `REL-005` | UAT、性能安全、升级回滚、RPO/RTO、`v1.0.0` 发布工件和验收报告 |

Sprint 10 当前实现证据：`FE-001` 至 `FE-009` 已完成；`npm ci`、lint、typecheck、production build、OpenAPI 归档/client 检查、14 个单元测试和 6 个 breaking-change checker 用例已通过。Playwright 仅运行新增和受影响流程并按失败项收敛，Chromium、Firefox、WebKit 本地与远端均为 `13 passed, 2 skipped`；提交 `8da1abe` 对应 `backend-ci` run `30893658311` 的 9 个 job 全部成功。

Sprint 11 当前实现证据：`BE-040` 至 `BE-043`、`FE-010` 至 `FE-011` 已完成代码侧交付；账号安全专项 4 个用例与受影响 Auth/限流/管理员/设备回归 12 个用例通过，OIDC/LDAP 10 项、route matrix 134 条路由及 6 项集合校验、前端三浏览器 12 场景均通过，Ruff/format、Mypy、锁文件、OpenAPI/client、PostgreSQL migration、Compose config、Cargo metadata 和差异检查均通过。最终提交 `e6b4f6d` 对应成功的 `backend-ci` run `30922718148`；frontend 已在 run `30919983108` 成功，Rust/MinIO/Windows/安装包已在 run `30917345858` 成功，未复用 Sprint 10 的提交/run 作为 Sprint 11 证据。

Sprint 12 当前实现证据：治理后端专项 `4 passed`；审计/Outbox 定向用例通过；前端 lint/typecheck 和 Chromium 治理 E2E `1 passed`；版本一致性 `1 passed`、`uv lock --check` 与 Cargo metadata 通过；真实 PostgreSQL 空库升级到 `20260804_0026` 并完成 `0026 -> 0024 -> 0026` 往返，分区专项 `1 passed`；四依赖故障/恢复与 Redis/OpenSearch 双 project 导出、应用、回退集成通过；12 个 Sprint 12 相关 PowerShell 脚本 parser、来源签名/完整包加密和离线/迁移专项 smoke 通过，直接受影响的 backup/governance smoke 保持通过；监控规则/看板、最终 OpenAPI/client、route matrix、Compose、actionlint 和 CI scope 均通过本地门禁。不重复运行未受影响的 Sprint 10、MinIO、性能或桌面历史全集，最终远端证据以本轮推送触发的 CI 为准。

### 22.16 远期正式 Backlog

远期能力进入正式 Backlog，必须有编号、入口条件和退出条件，不再只以“未来可选”描述：

| 编号 | 能力 | 入口条件 | 首要验收 |
| --- | --- | --- | --- |
| PLAT-001 | Windows Cloud Files API 虚拟盘 | Sprint 8 普通目录同步稳定 | 占位、按需下载、脱机、回收和升级测试 |
| PLAT-002 | macOS File Provider / Linux VFS | Windows 客户端稳定且平台需求确认 | 平台文件语义、签名、升级和冲突测试 |
| PLAT-003 | WebDAV 网关 | 权限、Range、审计和配额契约稳定 | 客户端兼容、锁、Range、审计和限流测试 |
| PLAT-004 | SMB 网关 | 网络隔离和身份映射方案通过评审 | ACL 映射、文件锁、审计和故障恢复测试 |
| PLAT-005 | Kubernetes / systemd 迁移 | Windows 正式路径稳定且出现明确平台需求 | 等价 readiness、Worker、存储、备份和回滚 |
| CLIENT-001 | Android / iOS / 移动 Web | Web API 与设备会话稳定，完成产品调研 | 上传下载、分享、预览、离线和推送范围决策 |
| COLLAB-001 | 在线 Office 协同与审批流 | 版本、锁、冲突和审计模型成熟 | 协同会话、版本合并、审批状态和审计验证 |
| GOV-001 | 复杂 DLP 与 legal hold | 文件分类、OCR、审计和策略框架稳定 | 内容识别、阻断、保留、申诉和审计测试 |
| DR-001 | 跨地域双活 | 单地域 RPO/RTO 和演练持续达标 | 元数据、对象、队列和搜索一致性演练 |
| BILL-001 | 多租户计费 | 多维配额和租户隔离稳定 | 计量、对账、账单和租户边界测试 |

### 22.17 Issue 模板

```markdown
## 背景
说明业务场景和技术目标。

## 范围
- 本 issue 要做什么
- 本 issue 不做什么

## 接口/数据变更
列出 API、schema、migration、事件和配置变更。

## 验收标准
- 单元测试
- 集成测试
- 权限测试
- 审计日志
- OpenAPI 文档

## 风险
并发、一致性、权限、安全、性能风险。
```

## 23. 技术风险清单

| 风险 | 具体表现 | 开发应对 |
| --- | --- | --- |
| 元数据与对象不一致 | 对象上传成功但 DB 失败 | 补偿任务、孤儿对象扫描、事务边界清晰 |
| 孤儿最终对象治理缺口 | 对象复制成功但 DB 最终化失败，`objects/...` 中没有 DB 元数据引用 | 已接入 `file.cleanup_orphaned_objects`，完成 BE-031/032 的真实 S3 主路径验收，并纳入 maintenance 调度、Prometheus 告警和 Grafana 看板 |
| SDK multipart 升级风险 | multipart 控制面依赖 MinIO Python SDK 公共预签名 API 与标准 S3 HTTP/XML 行为 | 依赖限定在 `7.x`、控制面窄封装、真实 S3 兼容门禁和显式升级评审 |
| 权限缓存过期 | 用户权限被撤销后仍可访问 | 版本化缓存、高危操作二次查库、失效事件 |
| 搜索泄露 | 搜到无权限文件名或内容 | 索引 ACL token + 应用层二次校验 |
| 目录并发冲突 | 同名文件并发创建 | 数据库唯一索引 + 冲突重试 |
| 同 hash 首次上传竞争 | 多个会话同时归档同一内容，可能同时尝试创建 blob | 唯一约束 + savepoint 解析、对象归档幂等、真实 PostgreSQL 双会话测试和失败清理 |
| 大文件压垮 API | 后端代理所有文件流量 | 默认客户端直传对象存储，API 只签名 |
| 高密级下载后续边界 | 内部自动选路、Range、图片/PDF 水印、关键字 DLP、外链水印和搜索 OCR 已实现；仍缺外链代理、历史版本专用水印、legal hold 和高级内容分类 | 后续补外链代理流、历史版本水印、legal hold 和复杂 DLP |
| 转码拖垮系统 | LibreOffice 占满 CPU/内存 | Worker 隔离、资源限制、队列限流 |
| 审计丢失 | 日志平台不可用 | PostgreSQL 月分区 `audit_logs` + Outbox 分类/jitter/DLQ + 签名归档 + 外部 HMAC 投递；外部失败不删除本地事实 |
| 容量统计漂移 | 并发上传删除导致用量不准 | 容量账本 + 原子扣减 + 定期校准 |
| 多维配额后续边界 | 核心运行时和账户/策略管理 API 已完成，但仍缺部门/临时额度和通用校准 | 后续补部门额度、临时占用上限、多维校准和容量治理看板 |
| 监控通知配置失效 | `monitoring` profile、规则和看板已存在，但生产 webhook、值班路由或 Grafana URL 可能配置错误 | 使用 `manage.ps1 status -Monitoring`、Prometheus target/rule、Alertmanager 配置与测试告警完成上线验收 |
| 真实存储升级兼容风险 | SeaweedFS 直接 S3、单对象迁移和 Windows 备份恢复兼容已通过，但旧 MinIO 全量数据、正式升级/RPO-RTO 和长期运行尚未验证 | 对 SeaweedFS 执行全量 S3 迁移/inventory、真实 multipart、预签名、copy/delete/list、孤儿扫描、soak 和正式升级/RPO-RTO |
| 备份测试耗尽宿主资源 | 镜像构建、全栈启动、高压缩和重复校验串行混在一个长任务中 | 构建/拉取与测试分离、local image preflight、Compose 串行限制、helper cgroup 上限、低压缩等级、分阶段耗时和默认数据服务恢复验证 |
| 备份机密性边界 | 未启用完整包保护时，数据库、数据卷和 TLS 私钥归档仍可能明文落盘 | Sprint 12 已支持可选 AES-256-CBC + HMAC-SHA256 + RSA-OAEP-SHA256 完整包保护；未启用时继续依赖 BitLocker、restricted NTFS ACL、加密介质和离线保管 |
| 备份来源认证不足 | SHA-256 可被可写入主体同时替换 manifest 后重算 | Sprint 12 已支持 detached CMS `manifest.p7s` 和预期 thumbprint；组织 PKI、吊销、双人保管和介质交接仍需上线流程 |
| 原始卷跨版本恢复风险 | Redis/OpenSearch 原始卷跨 image reference/image ID 或拓扑恢复失败 | 同版本恢复继续锁定镜像/拓扑；升级改用 Redis RDB 和 OpenSearch settings/mappings/bulk 可移植导出，导入前创建 rollback export 并自动回退 |
| ForceRestore 回滚残余风险 | 卷驱动、磁盘或 Docker 故障可能导致 rollback archive 自动回灌失败 | 保留脚本报告的 restricted ACL 归档和绝对路径，进入人工恢复流程 |
| 对象存储供应链风险 | 旧 MinIO `16/9` Critical 已退出正式运行时；当前 digest 的远端 Grype 为 `0 Critical / 1 High`，High 为 `GHSA-hrxh-6v49-42gf` | 修复到包含 gRPC `1.82.1` 的镜像或外部风险接受，并通过全量迁移、最终完整工件和升级/RPO-RTO |
| 桌面同步覆盖数据 | 本地与远端并发修改、删除或重命名时静默覆盖 | 已交付版本前置条件、服务端变更日志、冲突副本、旧操作 conflict 终止和双端/目录竞争状态机测试 |
| Windows 路径语义差异 | 大小写、保留设备名、尾随点空格、长路径或 reparse point 导致错误映射 | 已由 Rust 平台适配层集中规范化，默认不跟随链接，并覆盖保留名、NFC、长路径、冲突命名和原子替换 |
| 桌面凭据泄露 | token 写入 SQLite、日志、配置或诊断包 | 已使用 opaque device session、系统凭据库和日志脱敏；设备吊销会清除内存 token 并停止全部同步 |
| 客户端更新被替换 | 安装包或更新清单被篡改后执行未知二进制 | 已交付 Authenticode、Ed25519 清单与包签名、SHA-256、证书指纹和失败回退；私钥只存在 CI Secrets |
| 全量轮询拖垮服务 | 客户端频繁扫描完整文件树获取变化 | 已使用增量 cursor、tombstone、监听补偿、退避、限流和 cursor 失效后的受控重建 |
| Web 契约漂移 | 前端手工维护类型导致字段、错误码或权限枚举失配 | OpenAPI 生成 client、diff 门禁、核心响应 snapshot 和 E2E |
| 浏览器凭据泄露 | session 或 bearer token 被写入 Web Storage | 只使用 HttpOnly Cookie、CSRF、CSP、依赖审计和 XSS 测试 |
| 批量操作重复执行 | 网络重试导致重复删除、移动、恢复或容量变更 | Idempotency-Key、逐项结果、幂等记录和并发测试 |
| 版本回滚破坏历史 | 回滚覆盖原版本或重复计算容量 | 回滚创建新版本、不可变历史、原子容量流水和审计 |
| 身份映射接管 | OIDC/LDAP 外部标识冲突绑定到错误本地账号 | 稳定 issuer/subject 或目录 ID、显式绑定、冲突隔离和管理员确认 |
| OCR 资源耗尽 | 超大图片、扫描 PDF 或恶意格式耗尽 Worker | 像素/页数/体量限制、隔离队列、超时、临时磁盘和失败熔断 |
| 大目录任务中断 | 后台删除、恢复、彻底删除或权限重算中途失败形成部分状态 | operation_id、稳定 cursor、固定幂等批次、失败重试、权限版本重启和 PostgreSQL 进度事实 |
| 备份加密密钥丢失 | 完整包加密后密钥不可用导致备份永久不可恢复 | 双人保管、离线密钥副本、轮换演练和恢复前密钥可用性检查 |

## 24. Definition of Done

一个后端功能模块完成，必须同时满足：

1. 有 migration、model、schema、service、repository、router 和测试。
2. 有 OpenAPI 文档和错误码。
3. 关键路径写审计日志。
4. 权限校验在 service 层执行，并有越权测试。
5. 事务边界明确，外部调用有超时和错误处理。
6. 日志包含 request_id，异常不会泄露内部实现。
7. 单元测试和必要集成测试通过。
8. ruff、mypy、pytest 在 CI 通过。
9. Windows 部署变更通过 Compose、PowerShell 5.1、镜像策略、SBOM/漏洞基线和 gateway 端口边界检查。
10. 当前 `v1.0.0` 候选继续要求备份恢复通过 16 个默认服务实际 image ID（包括内部 `web`、`storage-init` 和 `seaweedfs`）、精确 Compose/Git/版本/configuration lineage、逐物理卷 mutex、restricted ACL、隔离 tar 预扫描、CMS 延迟原子发布、ForceRestore rollback、source 状态恢复和不同 Compose project 演练；SeaweedFS 版兼容结果已单独完成，正式升级恢复耗时与 RPO/RTO 仍需最终候选签字。Sprint 12 的 manifest v2 来源签名/可选完整包保护、离线 inventory/轮换和 Redis/OpenSearch 可移植迁移/自动回退门禁继续保持。
11. 身份能力还必须完成密码/会话撤销、OIDC 重放/开放重定向/绑定冲突、LDAP dry-run/稳定映射/禁用/离职/手工成员保留、secret 不回显和真实 provider/目录验收边界记录。

一个 Web 功能完成，还必须满足：

1. OpenAPI 生成 client 与当前后端契约一致，没有未提交生成漂移。
2. Cookie Session、CSRF、权限枚举、错误码和 request_id 行为经过单元与 E2E 验证。
3. Chromium、Firefox、WebKit 的核心流程通过，键盘和常用 Windows 缩放比例可操作。
4. 浏览器存储中没有 session、bearer token、提取码或敏感审计条件。
5. 构建产物通过 gateway 访问，Web 服务没有宿主端口，发布和回滚可重复。

一个跨后端、Web 或桌面的产品能力完成，还必须满足：

1. 后端权限、审计、容量、幂等和事务规则是唯一事实，客户端没有复制实现另一套业务规则。
2. OpenAPI、Rust/TypeScript client、文档、错误码和版本兼容窗口同步。
3. 网络中断、权限撤销、会话过期、重复请求、异常退出和升级回滚均有验证。
4. `PROJECT_PLAN.md`、`PROJECT_PROGRESS.md`、README、发布说明和 Git 状态同步。

## 25. 附录：配置样例

```env
APP_ENV=local
APP_NAME=enterprise-drive
DRIVE_DATABASE_URL=postgresql+asyncpg://drive:drive@localhost:5432/drive
DRIVE_REDIS_URL=redis://localhost:6379/0
DRIVE_S3_ENDPOINT_URL=http://localhost:9000
DRIVE_S3_ACCESS_KEY_ID=drive
DRIVE_S3_SECRET_ACCESS_KEY=REPLACE_WITH_RANDOM_SECRET
DRIVE_S3_REGION=us-east-1
DRIVE_S3_BUCKET=enterprise-drive
DRIVE_SESSION_COOKIE_NAME=drive_session
DRIVE_CSRF_COOKIE_NAME=drive_csrf
DRIVE_CSRF_HEADER_NAME=X-CSRF-Token
DRIVE_SESSION_COOKIE_SAMESITE=lax
DRIVE_SESSION_COOKIE_SECURE=false
DRIVE_SESSION_DAYS=30
DRIVE_LOGIN_FAILURE_LOCK_THRESHOLD=5
DRIVE_LOGIN_FAILURE_WINDOW_SECONDS=900
DRIVE_LOGIN_LOCK_SECONDS=900
DRIVE_LOGIN_DELAY_BASE_SECONDS=0.25
DRIVE_LOGIN_DELAY_MAX_SECONDS=4.0
DRIVE_LOGIN_CAPTCHA_AFTER_FAILURES=3
DRIVE_PASSWORD_MIN_LENGTH=12
DRIVE_PASSWORD_REQUIRE_UPPERCASE=true
DRIVE_PASSWORD_REQUIRE_LOWERCASE=true
DRIVE_PASSWORD_REQUIRE_DIGIT=true
DRIVE_PASSWORD_REQUIRE_SPECIAL=true
DRIVE_OIDC_STATE_TTL_SECONDS=600
DRIVE_IDENTITY_ALLOWED_REDIRECT_PATHS=["/","/account","/auth/oidc/callback","/admin/identity"]
DRIVE_IDENTITY_HTTP_TIMEOUT_SECONDS=10.0
DRIVE_LDAP_SYNC_PAGE_SIZE=500
OIDC_CLIENT_SECRET=
LDAP_BIND_PASSWORD=
DRIVE_UPLOAD_SESSION_TTL_MINUTES=1440
DRIVE_UPLOAD_PART_SIZE_BYTES=8388608
UPLOAD_MAX_FILE_SIZE=21474836480
DRIVE_UPLOAD_PRESIGN_EXPIRES_SECONDS=900
PRESIGN_DOWNLOAD_EXPIRES_SECONDS=120
DRIVE_OPENSEARCH_URL=http://localhost:9200
DRIVE_CELERY_BROKER_URL=redis://localhost:6379/1
DRIVE_CELERY_RESULT_BACKEND=redis://localhost:6379/2
```

## 26. 附录：开发检查清单

### 26.1 接口开发检查

1. 是否有权限校验。
2. 是否写审计日志。
3. 是否有 request_id。
4. 是否有统一错误码。
5. 是否支持幂等键，若为写操作。
6. 是否有分页限制，若为列表接口。
7. 是否避免泄露文件存在性。
8. 是否有单元测试和至少一个异常分支测试。

### 26.2 数据库开发检查

1. 查询是否带 tenant_id。
2. 大表查询是否命中索引。
3. 是否存在 N+1 查询。
4. migration 是否向前兼容。
5. 是否需要分区或后台回填。
6. JSONB 字段是否被滥用于高频查询。
7. 并发写是否有唯一索引或行锁兜底。

### 26.3 对象存储开发检查

1. bucket 是否私有。
2. key 是否避免暴露文件名和目录路径。
3. presign URL 是否短期有效。
4. multipart upload 失败是否能 abort。
5. 临时对象是否有生命周期清理。
6. 对象存储异常是否有清晰错误码和重试策略。

### 26.4 权限开发检查

1. 是否服务端校验，不依赖前端按钮隐藏。
2. 是否考虑目录继承和 deny 优先。
3. 是否考虑用户部门、用户组和空间角色。
4. 权限变更后是否失效缓存。
5. 搜索和分享是否做了单独权限处理。
6. 下载、删除、授权等高危动作是否二次确认。

### 26.5 Worker 开发检查

1. 任务是否幂等。
2. 是否有最大重试和 dead-letter。
3. 是否只传 ID，不传大 payload。
4. 是否设置外部命令超时。
5. 是否记录 task_id、resource_id、tenant_id。
6. 是否避免 preview 队列阻塞 audit 或 permission 队列。

## 27. 结论
开发实现时，最重要的不是先把页面功能堆满，而是把文件对象、元数据、权限、容量、审计和异步任务这些底层关系做稳。企业网盘的复杂度主要来自“大文件 + 权限 + 审计 + 一致性”，而不是普通 CRUD。

一期建议严格按模块化单体推进：先打牢 uv 工程底座和数据模型，再完成文件树、上传下载、权限、分享、预览搜索和管理能力。每个模块都必须有明确事务边界、权限测试、审计事件和可观测指标。Sprint 7/8 的 Rust Windows 桌面客户端、Sprint 9 的版本/批量操作/内部分享/完整管理 API、Sprint 10 的 Web 用户端与管理后台、Sprint 11 的登录安全/密码/OIDC/LDAP，以及 Sprint 12 的大目录权限治理、生命周期、审计/Outbox、治理页面、备份安全和四依赖矩阵均已完成代码侧交付；Sprint 13 已完成 SeaweedFS 正式运行时替换、直接 S3/迁移验证和 Windows 备份恢复兼容，下一步以全量迁移、完整 UAT、soak、安全、正式升级回滚和 RPO/RTO 门禁发布 `v1.0.0`。图片/扫描 PDF OCR 和旧 Office/ODF 抽取已提前完成；虚拟盘、移动端、WebDAV/SMB、在线协同、复杂 DLP、跨地域双活和计费均已进入有编号、有入口条件和验收标准的远期 Backlog。
